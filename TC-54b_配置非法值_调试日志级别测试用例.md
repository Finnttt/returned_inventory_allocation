# TC-54b 配置非法值（调试日志级别）测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000054 |
| 场景编号 | TC-54b |
| 场景名称 | 输入_配置中调试日志级别取值非法时，配置读取失败并给出可定位错误 |
| 测试目的 | ① 验证 `调试日志级别=开启` 触发配置读取错误 ② 验证错误信息包含配置项名与允许取值范围 ③ 验证运行在配置阶段中止 |
| 覆盖规则 | §6.7.6 配置区域（调试日志级别仅允许 关闭/简版/详细）、M02 配置合法性校验 |
| 预期错误码 | 配置读取错误（非 E01~E12） |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| `调试日志级别 = 开启` | 非法枚举值，应报错 |
| `最大回溯次数 = 200`（合法） | 排除其他配置项干扰，确保只命中一个异常 |

---

## 2. 输入数据（最小集）

### 2.1 输入_配置（关键字段）

| 物流单号 | TC编号 | 最大回溯次数 | 调试日志级别 | 批号比较模式 | 无保质期哨兵值 |
|---------|--------|--------------|--------------|--------------|----------------|
| SF3190000000054 | TC-54b | 200 | 开启 | 不敏感 | 2099/01/01 |

---

## 3. 预期结果

1. 读取配置失败；
2. 错误信息包含：`调试日志级别`、`关闭/简版/详细`；
3. 不进入后续数据加载与校验。

---

## 4. 补充说明

- 本用例与 TC-54a 成对使用，避免“一次多个非法值导致首错短路”影响断言稳定性。
