# TC-50 R070 分配后校验（M12）测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 场景编号 | TC-50 |
| 场景名称 | M12 `ValidatePost`：数量守恒、QC 一致性、退单号状态一致性 |
| 测试目的 | ① 正常分配结果 → `ValidatePost` **不误报** ② 人为构造错误 finalResult → 应检测出 POST 错误 ③ 整单回滚的物流单号不参与后校验 |
| 覆盖规则 | §4.3 分配后校验（R070） |
| 测试方式 | **纯 VBA 单元测试**（无 Excel DataSet） |
| 执行入口 | `modTestRunner.bas` → `RunPostValidateTests()`（包含 TC-50a/TC-50b 等故障注入测试） |

> **为何不建 Excel DataSet**：M12 验证的是分配**输出结果**的内部一致性。正常 Excel 输入若分配算法正确，finalResult 必然通过；要测“检出错误”的能力，需在单元测试中**故障注入**（故意构造错误的 finalResult）。

---

## 2. 子场景

### TC-50a：正确 finalResult 不误报

| 项目 | 内容 |
|------|------|
| 构造方式 | 模拟 SF0000 成功分配：1 行退单 qty=2，明细合计 alloc=2，QC 一致 |
| 期望 | `ValidatePost.HasFailures = False` |

### TC-50b：错误 finalResult 应报错

| 注入缺陷 | 期望 POST 错误 |
|---------|---------------|
| 明细合计 qty ≠ 退单 qty | 数量不守恒 |
| 同一退单行号对应两种 QC | QC 冲突 |

---

## 3. 实现位置（代码阶段）

| 文件 | 内容 |
|------|------|
| `modTestRunner.bas` | 已新增 `TestPostValidate_Success`、`TestPostValidate_QtyMismatch`、`TestPostValidate_TwoQcsSameLine`、`TestPostValidate_SkipRollbackShipment` |
| `modPostValidate.bas` | 被测函数 `ValidatePost` |

---

## 4. 映射表状态

| TC | Rule | Module | DataSet | 状态 |
|----|------|--------|---------|------|
| TC-50 | R070 | M12 | —（纯 VBA） | 已完成（单元测试） |

---

## 5. 与 Full Run 的关系

- Full Run 末尾已调用 `ValidatePost`，但结果**仅记录、不阻断输出**（见 `modRunner.bas` 注释）。
- 本 TC 确保该守卫在正常/异常数据下行为正确，不依赖 Excel 端到端场景。
