# TC-43 R127 重跑覆盖（清空输出表、保留历史）测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 场景编号 | TC-43 |
| 场景名称 | 连续两次 Full Run：输出表清空重写，运行历史累积追加 |
| 测试目的 | ① 验证 R127：重跑时清空分配状态汇总/成功明细/数据异常/调试日志等输出表 ② 验证输入表、配置表、**运行历史记录表不被清空** ③ 验证第二次运行后运行历史有 **2 行**（每次运行 +1） |
| 覆盖规则 | §6.7.4 重跑覆盖（R127） |
| 复用 DataSet | SF0000（TC-01/TC-30） |
| 入口函数 | `RunFullAllocation(wb)` |

---

## 2. 操作步骤

### 准备

1. 打开 SF0000 工作簿（或仅含 SF0000 数据的测试副本）。
2. 在以下输出表第 2 行人工写入**脏数据**（用于验证会被清空）：
   - `分配状态汇总表`
   - `成功分配明细表`
   - `数据异常明细表`
3. 在 `运行历史记录表` 末尾人工写入一行标记，例如：`[人工标记-运行前]`。

### 第一次运行

4. 点击 **开始分配**（Full Run）。
5. 记录运行历史表总行数 = N1。

### 第二次运行

6. **不关闭工作簿**，再次点击 **开始分配**。
7. 记录运行历史表总行数 = N2。

---

## 3. 预期结果

### 3.1 第一次运行后

| 检查项 | 预期 |
|--------|------|
| 分配状态汇总表 | 脏数据被清空；仅有 SF0000 成功 1 行 |
| 成功分配明细表 | 脏数据被清空；仅有 1 行成功明细 |
| 数据异常明细表 | 空（仅表头） |
| 运行历史记录表 | 保留人工标记行；**追加 1 行** Full Run 统计 |
| 输入_退单表 / 输入_质检库存表 / 输入_配置 | **内容不变** |

### 3.2 第二次运行后

| 检查项 | 预期 |
|--------|------|
| 输出表（汇总/明细/异常/调试） | 再次被清空并重写；**不应残留第一次运行的多余行** |
| 运行历史记录表 | **N2 = N1 + 1**（累积 2 条 Full Run 记录 + 原人工标记行） |
| 输入表与配置表 | 仍不变 |

---

## 4. 验收判定

- **通过**：第二次运行后汇总表仍只有 1 行成功记录，运行历史比第一次多 exactly 1 行。
- **失败**：第二次运行后汇总/明细出现重复行、脏数据未清空、或运行历史被覆盖（行数未增加）。

---

## 5. 补充说明

- 本 TC 验证的是 **M14 `ClearOutputSheets`** 与 **`AppendRunHistory`** 的协作行为。
- 与 TC-44 的关系：TC-44 验证“单次运行追加 1 行历史”；本 TC 验证“多次运行历史累积、输出表不累积”。
