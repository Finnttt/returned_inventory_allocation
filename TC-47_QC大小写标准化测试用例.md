# TC-47 QC大小写标准化测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000047 |
| 场景编号 | TC-47 |
| 场景名称 | 质检库存中 QC 情况字段填写小写（`zp`）与混合大小写（`Qc`），R022 Trim→UCase→校验后合法识别，不触发 E03，分配成功，输出 QC 为大写 |
| 测试目的 | ① 验证 QC 情况字段 `zp`（全小写）经 R022（Trim→UCase→校验）后 = `ZP`，校验通过，不触发 E03 ② 验证 QC 情况字段 `Qc`（混合大小写）经 R022 后 = `QC`，校验通过，不触发 E03 ③ 验证 Trim→UCase→校验的**顺序正确**（若先校验再 UCase，含小写的值会误触发 E03） ④ 验证标准化后的 ZP / QC 参与分配，成功匹配各自的行，输出 QC 字段为大写 |
| 覆盖规则 | §4.0 数据标准化表格 QC 情况行（R022）、§4.1 E03 校验（R043） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | 无（全部成功） |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 质检库存行1 QC 情况 = `zp`（全小写）→ R022 Trim→UCase = `ZP`→ 合法，E03 不触发 | §4.0（R022）、§4.1（R043） |
| 质检库存行2 QC 情况 = `Qc`（混合大小写）→ R022 Trim→UCase = `QC`→ 合法，E03 不触发 | §4.0（R022）、§4.1（R043） |
| 三步顺序（Trim → UCase → 校验）保证：若先校验，`zp` 不在 {ZP/QC/NG} 集合中，会误触发 E03 | §4.0 顺序说明 |
| ZP 库存 T=D=3（第一行），QC 库存 T=D=2（第二行），各自策略一匹配成功，输出 QC 为大写 | §5.2 输出验证 |

---

## 2. 输入数据

### 2.1 退单表（2行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|-----------|-----|------|------|---------|
| TK10000470 | H000000047 | 00001 | 3 | SF3190000000047 |
| TK10000470 | H000000047 | 00002 | 2 | SF3190000000047 |

### 2.2 质检库存表（2行，QC情况大小写不同）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 | DataSet 制作说明 |
|---------|-----|--------|------|------|------|----------------|
| SF3190000000047 | H000000047 | zp | LA01 | 2029/06/15 | 3 | ⚠️ **QC情况填写小写 `zp`**，单元格格式为文本，输入原始小写值，勿让 Excel 自动修改 |
| SF3190000000047 | H000000047 | Qc | LA02 | 2029/06/15 | 2 | ⚠️ **QC情况填写混合大小写 `Qc`**，单元格格式为文本 |

> **DataSet 核心**：两行 QC 情况字段均为非标准大小写（`zp`、`Qc`），模拟手工录入时大小写不规范的场景。R022 标准化后，两行 QC 分别为 `ZP` 和 `QC`（不同类型），五元组不合并。
>
> **数量校验**：H000000047 退单合计 = 3+2 = 5；库存合计 = 3+2 = 5 ✓
> **五元组汇总**：两行 QC 类型不同（标准化后 ZP ≠ QC），五元组唯一，无需合并
> **分配前校验预判**：E01~E11 全部通过（QC 标准化后为 ZP/QC，均在合法集合 {ZP, QC, NG} 内，E03 不触发）

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01~E05）数据完整性 | 关键字段非空、行号格式、QC合法性、数量为正整数、效期格式 | 通过（`zp` → UCase → `ZP` ∈ {ZP,QC,NG} ✓；`Qc` → UCase → `QC` ∈ {ZP,QC,NG} ✓；E03 均不触发；所有其他字段合法） |
| 第2层（E06/E07）物流单号一致性 | 退单表与质检库存表物流单号集合比对 | 通过 |
| 第3层（E08）数量一致性 | 同物流单号+SKU 两表数量合计比对 | 通过（退单 H000000047 合计 5 = 库存 H000000047 合计 3+2=5）|
| 第4层（E11）QC碎片可分配性 | 是否存在 0 < T < groupMinQty 的QC类型 | 通过（groupMinQty = min(3, 2) = 2；ZP T=3：0 < 3 < 2？否 ✓；QC T=2：0 < 2 < 2？否（不满足严格小于）✓）|

> **结论**：四层全部通过，进入分配阶段。

> **R022 顺序验证点**：若实现为"先校验再 UCase"（顺序错误），则 `zp` 不在 {ZP/QC/NG} 中 → 误触发 E03。本 TC 通过无 E03 输出来验证三步顺序（Trim → UCase → 校验）执行正确。

---

## 4. 分配过程推导

### 初始库存状态（R022 标准化后）

> R022 处理：`zp` → Trim → UCase → `ZP`；`Qc` → Trim → UCase → `QC`

| QC情况（标准化后） | 批号 | 效期 | 数量（T） |
|------------------|------|------|----------|
| ZP | LA01 | 2029/06/15 | 3 |
| QC | LA02 | 2029/06/15 | 2 |

### SF3190000000047 — SKU H000000047

本 SKU 组包含退单 TK10000470 下的 2 行（00001/D=3，00002/D=2）。

#### 4.1 静态排序

各行的初始 `nextMinQty` = 组内其他行需求量最小值：
- 行 00001：nextMinQty = min(D₀₀₀₀₂) = 2，D + nextMinQty = 3 + 2 = 5
- 行 00002：nextMinQty = min(D₀₀₀₀₁) = 3，D + nextMinQty = 2 + 3 = 5

| 处理序 | 行号 | D | 初始 nextMinQty | D + nextMinQty | 可用QC类型数 | 排序依据 |
|--------|------|---|----------------|---------------|-------------|---------|
| 1 | 00001 | 3 | min(2) = 2 | 5 | 1（ZP: T=3=D ✓；QC: T=2≠D=3 且 T<5 ✗ → 仅ZP）| 可用QC数升序→D降序→行号升序 |
| 2 | 00002 | 2 | min(3) = 3 | 5 | 1（ZP: T=3≠D=2 且 T<5 ✗；QC: T=2=D ✓ → 仅QC）| 同 |

> 两行可用QC类型数相同（均为 1）→ D 降序：00001（D=3）先，00002（D=2）后。

#### 4.2 分配过程

**第一轮：TK10000470-00001（D=3，处理序=1）**

当前可用：ZP-LA01(T=3)、QC-LA02(T=2)；nextMinQty=2，D+nextMinQty=5

可用条件（T ≥ D+nextMinQty=5 或 T=D=3）：
- ZP-LA01：T=3=D=3 ✓ → 可用
- QC-LA02：T=2；2≥5？否；2=3？否 ✗ → 不可用

仅 ZP 可用 → **策略一直接选中**

**选中**：ZP/LA01/2029/06/15 → 分配 3，T = 3→0

行状态：**批量导入**（1种批号/效期组合）

> **R022 核心验证**：此处 ZP 库存来自原始输入 `zp`（小写），系统通过 R022 标准化为 `ZP` 后正确参与筛选和匹配。

**第二轮：TK10000470-00002（D=2，处理序=2）**

当前可用：ZP-LA01(T=0，已耗尽)、QC-LA02(T=2)；00002 为最后一行，nextMinQty = ∅

可用条件（最后一行仅需 T=D）：
- ZP-LA01：T=0，已耗尽 ✗
- QC-LA02：T=2=D=2 ✓ → 可用

唯一候选 → **策略一直接选中**

**选中**：QC/LA02/2029/06/15 → 分配 2，T = 2→0

行状态：**批量导入**

> **R022 核心验证**：此处 QC 库存来自原始输入 `Qc`（混合大小写），系统通过 R022 标准化为 `QC` 后正确参与匹配。

#### 4.3 分配结果

| 行 | 选中 QC/批号/效期 | 分配量 | 库存变化 | 行状态 |
|----|-----------------|--------|---------|--------|
| TK10000470-00001 | ZP / LA01 / 2029/06/15 | 3 | LA01：3→0 | 批量导入 |
| TK10000470-00002 | QC / LA02 / 2029/06/15 | 2 | LA02：2→0 | 批量导入 |

**退单号状态聚合**：TK10000470 全部行"批量导入" → 退单号状态 = **批量导入**

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|-----------|-----------|------|
| SF3190000000047 | TK10000470 | 批量导入 | |

### 5.2 成功分配明细表（2行）

| 物流单号 | WMS退单号 | SKU | 行号 | 退单数量 | QC情况 | 批号 | 效期 | 分配数量 | 行状态 | 退单号状态 |
|---------|-----------|-----|------|----------|--------|------|------|----------|--------|-----------|
| SF3190000000047 | TK10000470 | H000000047 | 00001 | 3 | ZP | LA01 | 2029/06/15 | 3 | 批量导入 | 批量导入 |
| SF3190000000047 | TK10000470 | H000000047 | 00002 | 2 | QC | LA02 | 2029/06/15 | 2 | 批量导入 | 批量导入 |

> **核心验证**：
> - 行 00001 输出 QC = `ZP`（原始输入 `zp`，标准化为大写后输出）
> - 行 00002 输出 QC = `QC`（原始输入 `Qc`，标准化为大写后输出）
> - 两行均无 E03，证明 Trim→UCase→校验的顺序正确（若顺序错误，`zp` 和 `Qc` 会在校验时报 E03）
> - 分配成功，证明标准化后的 QC 值正确参与了库存匹配

### 5.3 数据异常明细表

**不生成**（无数据异常，所有校验通过）

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表（每次运行均需验证）

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 2 |
| 输入：质检库存表行数 | 2 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 0 |
| 分配成功物流单号数 | 1 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | （空） |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |
| 追加验证 | 运行后总行数 = 运行前总行数 + 1 |

---

## 6. 补充说明

### 6.1 与 TC-15 的区别

| | TC-15 | TC-47 |
|---|---|---|
| QC 输入值 | 真正非法（如 `QM`、`XX`） | 合法但大小写不规范（`zp`、`Qc`） |
| 预期结果 | 触发 E03（QC 非法） | 不触发 E03（标准化后合法）|
| 验证重点 | E03 能识别非法 QC | R022 容错手工录入的大小写不规范 |

### 6.2 三步顺序的重要性

需求文档明确说明 R022 的三步顺序"不可颠倒"：

1. `Trim`：去除首尾空格（如 ` zp` → `zp`）
2. `UCase`：统一转大写（`zp` → `ZP`，`Qc` → `QC`）
3. 校验是否为 {ZP, QC, NG} 之一（`ZP` ∈ 集合 ✓）

若先校验再 Trim/UCase（顺序错误），`zp` 不在集合中，会误触发 E03，导致合法输入被拒绝。本 TC 通过验证无 E03 输出，来隐式验证顺序正确。

### 6.3 DataSet 制作注意事项

QC 情况列单元格格式应设为"文本"，避免 Excel 对文本字段做自动更正（如将首字母自动大写）。输入 `zp` 后确认单元格显示为 `zp`（全小写）；输入 `Qc` 后确认为 `Qc`（混合大小写）。
