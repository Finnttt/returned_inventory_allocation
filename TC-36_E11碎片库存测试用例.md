# TC-36 E11 QC库存碎片无法分配测试用例

## 物流单号

SF3190000000036

## 场景编号

| TC-36 |
|-------|
| 分配前校验第四层：QC库存碎片（0 < T < groupMinQty）→ E11（§4.1 R049）|

## 测试目的

1. 验证分配前校验第四层（E11）能在任何分配操作启动前，正确识别"碎片库存"（存在至少一种QC类型的可用数量 T 满足 0 < T < groupMinQty）并触发 E11
2. 验证 E11 触发后，该物流单号下所有行均标记为失败，系统不进入分配阶段

## 覆盖规则

§4.1 第四层校验 E11—QC碎片无法分配（R049）

---

## 一、测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000036 |
| 场景编号 | TC-36 |
| 场景名称 | 分配前校验 E11：ZP和QC库存均为碎片（T=1 < groupMinQty=D=2），触发 E11 |
| 测试目的 | 见上节 |
| 覆盖规则 | §4.1 E11（R049） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志 | 开启（分配未启动，日志表不生成，但配置本身需验证） |
| 预期错误码 | E11 |
| 是否触发整单回滚 | 否（物流单号在校验阶段即失败，未进入分配阶段，整单回滚仅适用于分配阶段）|

### 关键触发点

| 触发点 | 需求节号 | 说明 |
|--------|---------|------|
| 退单1行（D=2），库存 ZP=1/QC=1 | 数据设计 | groupMinQty=D=2；ZP(T=1): 0<1<2 → E11；QC(T=1): 0<1<2 → E11 |
| E08 先通过：退单合计=2=库存合计=2 | §4.1 第三层 | E11 是第四层，须先通过第三层 E08 才能触及第四层 |
| ZP(1)+QC(1)=2=D，但两者均为碎片 | §4.1 E11 | E11 拦截"每种QC单独不够用，合并后理论够用"的碎片场景 |

---

## 二、输入数据

### 退单表（1行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|----------|-----|------|------|---------|
| TK00000036 | H000000001 | 00001 | 2 | SF3190000000036 |

### 质检库存表（2行，每行已是唯一五元组）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000036 | H000000001 | ZP | LA01 | 2029/01/01 | 1 |
| SF3190000000036 | H000000001 | QC | LA01 | 2029/01/01 | 1 |

> **数量校验**：退单 H000000001 合计 = 2；库存 H000000001 合计 = 1+1 = 2 ✓
> **五元组汇总**：两行 QC情况 不同（ZP ≠ QC），五元组唯一，无需合并
> **E11 预判**：groupMinQty = D = 2（仅1行）；ZP(T=1): 0<1<2 → **触发 E11**；QC(T=1): 0<1<2 → **触发 E11**

---

## 三、分配前校验结论

| 校验层 | 结果 |
|--------|------|
| 第一层：关键字段完整性（E01~E02） | 通过 |
| 第二层：字段值合法性（E03~E05） | 通过 |
| 第三层：跨表一致性（E06~E08） | 通过（退单合计=库存合计=2） |
| 第四层：库存碎片预检（E11） | **命中 E11**（ZP: 0<T=1<groupMinQty=2；QC: 0<T=1<groupMinQty=2） |

**结论**：第四层命中 E11，该物流单号所有行标记失败，**不进入分配阶段**。

---

## 四、分配过程推导

校验未通过，不进入分配阶段。

---

## 五、预期结果

### 5.1 分配汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|----------|-----------|------|
| SF3190000000036 | TK00000036 | 无法分配 | E11 - QC库存碎片无法分配（0 < T < groupMinQty） |

### 5.2 分配明细表

**空**（校验阶段 E11 失败，无成功分配记录）

### 5.3 数据异常明细表

**不生成**（E11 不产生数据异常明细表记录，错误仅出现在汇总表中）

### 5.4 调试日志表

**不生成**（E11 在分配前校验阶段触发，分配操作未启动，因此不产生调试日志）

---

## 六、补充说明

### 6.1 E11 与 TC-06（预检测A）的区别

| 对比项 | TC-36（本用例，E11）| TC-06（SF0003，预检测A / E09）|
|--------|---------------------|-------------------------------|
| 检测阶段 | 分配前校验第四层（校验层）| 分配阶段预检测A（分配层）|
| 触发时机 | 分配循环启动前，由静态库存充分性判断触发 | 静态排序后、进入分配循环前，由可用QC数=0 触发 |
| 错误码 | E11 | E09（子类型：预检测A）|
| 原因 | 碎片库存：T=1 满足 0<T<groupMinQty=2，库存太少，无法独立完成分配 | 无可用QC：任何单一QC类型均不能满足当前行的需求 |
| 是否产生调试日志 | **否**（分配未启动）| **是**（预检测A命中后仍输出日志，回溯次数=0）|

### 6.2 数据设计说明

本用例数据（ZP=1, QC=1, D=2）与 TC-06/TC-39（SF0003）原始参考数据结构相同，但以独立物流单号 SF0036 呈现，专注于验证 E11 校验逻辑。两个用例共同说明：

- **SF0036（TC-36）**：E11 在校验层拦截"碎片库存"，分配从未启动
- **SF0003（TC-06/TC-39）**：在库存更充裕的设计下（ZP=5, QC=6, D=10/1），E11 不触发，分配进入预检测A阶段（E09）

### 6.3 groupMinQty 计算说明

`groupMinQty = min(D) for all lines in the same 物流单号+SKU group`

本用例仅1行（00001，D=2），因此 `groupMinQty = 2`。

E11 判断：对每个 QC 类型检查是否存在 `0 < T < groupMinQty`：

- ZP：T=1，0 < 1 < 2 → 命中 E11
- QC：T=1，0 < 1 < 2 → 命中 E11

两种 QC 类型均触发 E11，整个物流单号下所有行失败。
