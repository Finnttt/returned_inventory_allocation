# TC-46 批号大小写标准化测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000046 |
| 场景编号 | TC-46 |
| 场景名称 | 质检库存中批号大小写不同（`a01` 与 `A01`），R021 Trim+UCase 标准化后合并为同一五元组，分配成功，输出批号统一为大写 |
| 测试目的 | ① 验证批号 `a01` 经 R021（Trim + UCase）标准化后 = `A01` ② 验证标准化后五元组与批号 `A01` 的另一行完全相同，M06 五元组汇总将两行合并为一条（T = 6+4 = 10），库存不因大小写差异"分裂"为两条 ③ 验证合并后的库存 T=10 覆盖退单合计 10，分配成功，无 E09 ④ 验证成功分配明细表输出批号为大写 `A01`（标准化后的值） |
| 覆盖规则 | §4.2.5 批号读取规范（R021）、§2.2 五元组汇总（R051，辅助验证） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | 无（全部成功） |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 质检库存行1批号 = `a01`（小写）→ R021 Trim+UCase → `A01` | §4.2.5（R021） |
| 质检库存行2批号 = `A01`（大写）→ R021 Trim+UCase → `A01`（不变） | §4.2.5（R021） |
| 两行五元组标准化后完全相同（物流单号/SKU/ZP/A01/2029/06/15）→ M06 合并，T = 6+4 = 10 | §2.2 / §6.2.2（R051） |
| 合并后 T=10 = 退单合计 10，分配成功，输出批号为大写 `A01` | §5.2 输出验证 |

---

## 2. 输入数据

### 2.1 退单表（2行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|-----------|-----|------|------|---------|
| TK10000460 | H000000046 | 00001 | 5 | SF3190000000046 |
| TK10000460 | H000000046 | 00002 | 5 | SF3190000000046 |

### 2.2 质检库存表（2行，原始批号大小写不同，标准化后五元组相同）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 | DataSet 制作说明 |
|---------|-----|--------|------|------|------|----------------|
| SF3190000000046 | H000000046 | ZP | a01 | 2029/06/15 | 6 | ⚠️ **批号填写小写 `a01`**，单元格格式为文本，输入原始小写值，勿让 Excel 自动修改 |
| SF3190000000046 | H000000046 | ZP | A01 | 2029/06/15 | 4 | 批号填写大写 `A01`，单元格格式为文本 |

> **DataSet 核心**：两行批号大小写不同（`a01` vs `A01`），效期和 QC 相同。R021 标准化后批号均变为 `A01`，导致两行五元组完全相同，M06 合并为 T = 6+4 = 10。
>
> **数量校验**：H000000046 退单合计 = 5+5 = 10；库存原始行合计 = 6+4 = 10 ✓
> **五元组汇总**：R021 标准化后，批号 `a01` → `A01`，两行五元组 = (SF3190000000046, H000000046, ZP, A01, 2029/06/15) 完全相同 → M06 合并，T = 6+4 = **10**
> **分配前校验预判**：E01~E11 全部通过（批号标准化后合法，数量一致）

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01~E05）数据完整性 | 关键字段非空、行号格式、QC合法性、数量为正整数、效期格式 | 通过（批号经 R021 标准化后合法；效期 `2029/06/15` 合法；所有字段完整） |
| 第2层（E06/E07）物流单号一致性 | 退单表与质检库存表物流单号集合比对 | 通过 |
| 第3层（E08）数量一致性 | 同物流单号+SKU 两表数量合计比对 | 通过（退单 H000000046 合计 10 = 库存 H000000046 原始行合计 6+4=10）|
| 第4层（E11）QC碎片可分配性 | 是否存在 0 < T < groupMinQty 的QC类型 | 通过（groupMinQty = min(5, 5) = 5；ZP 五元组汇总后 T = 10，不满足 0 < 10 < 5）|

> **结论**：四层全部通过，进入分配阶段。

---

## 4. 分配过程推导

### 初始库存状态（五元组汇总后）

> R021 批号标准化（Trim + UCase）：`a01` → `A01`，`A01` → `A01`（不变）
> M06 五元组汇总：两行五元组相同 → **合并为一条**：

| QC情况 | 批号（标准化后） | 效期 | 数量（T，合并后） |
|--------|----------------|------|-----------------|
| ZP | A01 | 2029/06/15 | 10（= 6 + 4） |

### SF3190000000046 — SKU H000000046

本 SKU 组包含退单 TK10000460 下的 2 行（00001/D=5，00002/D=5）。

#### 4.1 静态排序

| 处理序 | 行号 | D | 初始 nextMinQty | D + nextMinQty | 可用QC类型数 | 排序依据 |
|--------|------|---|----------------|---------------|-------------|---------|
| 1 | 00001 | 5 | min(5) = 5 | 10 | 1（ZP: T=10 ≥ D+nextMinQty=10 ✓）| 可用QC数升序→D降序→行号升序 |
| 2 | 00002 | 5 | min(5) = 5 | 10 | 1（ZP: T=10 ≥ 10 ✓）| 同 |

> 两行可用QC类型数相同（均为 1），D 相同（均为 5）→ 行号升序：00001 先，00002 后。

#### 4.2 分配过程

**第一轮：TK10000460-00001（D=5，处理序=1）**

当前可用：ZP-A01(T=10)；nextMinQty=5

可用条件：T=10 ≥ D+nextMinQty=10 ✓ → ZP-A01 可用，**策略一直接选中**

**选中**：ZP/A01/2029/06/15 → 分配 5，T = 10→5

行状态：**批量导入**（1种批号/效期组合）

**第二轮：TK10000460-00002（D=5，处理序=2）**

当前可用：ZP-A01(T=5)；00002 为最后一行，nextMinQty = ∅

可用条件（最后一行仅需 T=D）：T=5=D=5 ✓ → ZP-A01 可用，**策略一直接选中**

**选中**：ZP/A01/2029/06/15 → 分配 5，T = 5→0

行状态：**批量导入**

#### 4.3 分配结果

| 行 | 选中 QC/批号/效期 | 分配量 | 库存变化 | 行状态 |
|----|-----------------|--------|---------|--------|
| TK10000460-00001 | ZP / A01 / 2029/06/15 | 5 | A01：10→5 | 批量导入 |
| TK10000460-00002 | ZP / A01 / 2029/06/15 | 5 | A01：5→0 | 批量导入 |

**退单号状态聚合**：TK10000460 全部行"批量导入" → 退单号状态 = **批量导入**

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|-----------|-----------|------|
| SF3190000000046 | TK10000460 | 批量导入 | |

### 5.2 成功分配明细表（2行）

| 物流单号 | WMS退单号 | SKU | 行号 | 退单数量 | QC情况 | 批号 | 效期 | 分配数量 | 行状态 | 退单号状态 |
|---------|-----------|-----|------|----------|--------|------|------|----------|--------|-----------|
| SF3190000000046 | TK10000460 | H000000046 | 00001 | 5 | ZP | A01 | 2029/06/15 | 5 | 批量导入 | 批量导入 |
| SF3190000000046 | TK10000460 | H000000046 | 00002 | 5 | ZP | A01 | 2029/06/15 | 5 | 批量导入 | 批量导入 |

> **核心验证**：
> - 输出批号为大写 `A01`，原始输入 `a01`（小写）被正确标准化后输出。
> - 两行分配成功（未出现 E08 数量不匹配，说明合并逻辑正确：`a01` qty=6 + `A01` qty=4 合并为 T=10，与退单合计 10 匹配）。
> - 若 R021 未实现，`a01` 和 `A01` 会被视为两个不同五元组（T=6 和 T=4），均无法满足 D=5+nextMinQty=5=10 的分配条件，导致 E09 分配失败。本 TC 通过分配成功来反证标准化已生效。

### 5.3 数据异常明细表

**不生成**（无数据异常，所有校验通过）

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表（每次运行均需验证）

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 2 |
| 输入：质检库存表行数 | 2 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 0 |
| 分配成功物流单号数 | 1 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | （空） |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |
| 追加验证 | 运行后总行数 = 运行前总行数 + 1 |

---

## 6. 补充说明

### 6.1 反向验证逻辑

本用例通过"分配成功"来**隐式验证** R021 已正确实现：
- 若 R021 未执行 UCase，`a01` 和 `A01` 为两个不同批号，五元组不合并，各自 T=6 和 T=4
- 分配时，每行需求 D=5，nextMinQty=5，可用条件为 T≥10 或 T=D=5：
  - ZP-a01(T=6)：6≥10? 否；6=D=5? 否 → 不可用
  - ZP-A01(T=4)：4≥10? 否；4=D=5? 否 → 不可用
- 两行均无可用 QC → E09 分配路径穷尽
- 因此：**只要分配成功，就说明五元组合并生效，R021 已正确执行**。

### 6.2 配置项"批号比较模式=敏感"的反向测试

本用例默认配置（批号比较模式=不敏感）。若将配置切换为"敏感"模式，应观察到：
- `a01` 和 `A01` 被视为不同批号（不合并）
- 分配失败（E09）

此反向测试不在本 TC 范围内，建议后续单独建立 TC 或在执行时额外验证。

### 6.3 DataSet 制作注意事项

批号列单元格格式应设为"文本"，避免 Excel 对字母序列做自动补全或大小写转换。输入 `a01` 后确认单元格显示为 `a01`（小写），不被自动修改。
