# TC-48 行号文本格式合法性测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000048 |
| 场景编号 | TC-48 |
| 场景名称 | 退单表行号列以文本型单元格存储标准五位前导零格式，验证正常连续行号的合法读入 |
| 测试目的 | ① 验证文本型单元格行号（`"00001"`、`"00002"`、`"00003"`）经 `CStr(cell.Value)` 读取后满足"恰好5位且全为数字字符"，E01 不触发 ② 验证从 `00001` 起的连续行号满足 R042 连续性要求，E02 不触发 ③ 验证系统仅接受文本型五位前导零行号，不对数值型或位数不足的行号自动补零（本 TC 作为正向基线；负向场景见 TC-13；跳号/不连续负向场景见 TC-51） |
| 覆盖规则 | §2.1 行号读取规则（R011）、§4.1 E01 格式校验（R041）、§4.1 E02 行号连续性校验（R042，正向不触发） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | 无（全部成功） |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 行号单元格为文本型 `"00001"`（VarType=vbString） → `CStr("00001")` = `"00001"` → 5位全数字 ✓ | §2.1（R011） |
| 行号单元格为文本型 `"00002"` → 5位全数字 ✓；与 `00001` 构成连续序列，E02 不触发 | §2.1（R011）、§4.1（R042） |
| 行号单元格为文本型 `"00003"` → 5位全数字 ✓；`00001`/`00002`/`00003` 为从 `00001` 起的连续序列，E02 不触发 | §2.1（R011）、§4.1（R042） |
| VBA 不执行自动补零，文本型 `"00001"` 本身即为合法值，无需补零处理 | §2.1 DataSet 制作说明 |

---

## 2. 输入数据

### 2.1 退单表（3行，行号以文本型存储，从 00001 起连续）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|-----------|-----|------|------|---------|
| TK10000480 | H000000048 | 00001 | 5 | SF3190000000048 |
| TK10000480 | H000000048 | 00002 | 5 | SF3190000000048 |
| TK10000480 | H000000048 | 00003 | 5 | SF3190000000048 |

> **DataSet 制作关键**：
> - 行号列（第3列）单元格须设为**文本格式**，然后直接输入 `00001`、`00002`、`00003`（含前导零）。
> - Excel 将底层值存储为字符串，VBA 读取 `cell.Value` 得到字符串 `"00001"`，`VarType = vbString`。
> - **不要**设置为数字格式（包括自定义格式 `00000`），否则底层存储为数值，`CStr()` 后位数不足5位，触发 E01。
> - 三行行号从 `00001` 起连续递增，满足 §2.1 连续性要求，E02 不触发。

### 2.2 质检库存表（1行，每行已是唯一五元组）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000048 | H000000048 | ZP | LA01 | 2029/06/15 | 15 |

> **数量校验**：H000000048 退单合计 = 5+5+5 = 15；库存合计 = 15 ✓
> **五元组汇总**：ZP/LA01/2029/06/15 仅一行，T=15，无需合并
> **分配前校验预判**：E01~E11 全部通过（行号文本型直接读取，均满足5位全数字字符；行号从 `00001` 起连续，E02 不触发）

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01~E05）数据完整性 | 关键字段非空、行号格式、QC合法性、数量为正整数、效期格式 | 通过（`"00001"` ✓、`"00002"` ✓、`"00003"` ✓，均为5位全数字字符；其余字段合法） |
| 第1层（E02）行号连续性 | 同一 WMS 退单号下行号从 `00001` 起连续递增 | 通过（`00001` → `00002` → `00003`，无重复、无跳号） |
| 第2层（E06/E07）物流单号一致性 | 退单表与质检库存表物流单号集合比对 | 通过 |
| 第3层（E08）数量一致性 | 同物流单号+SKU 两表数量合计比对 | 通过（退单 H000000048 合计 15 = 库存 H000000048 合计 15） |
| 第4层（E11）QC碎片可分配性 | 是否存在 0 < T < groupMinQty 的QC类型 | 通过（groupMinQty = min(5,5,5) = 5；ZP T=15，不满足 0 < 15 < 5） |

> **结论**：四层全部通过，进入分配阶段。

---

## 4. 分配过程推导

### 初始库存状态

| QC情况 | 批号 | 效期 | 数量（T） |
|--------|------|------|----------|
| ZP | LA01 | 2029/06/15 | 15 |

### SF3190000000048 — SKU H000000048

本 SKU 组包含退单 TK10000480 下的 3 行（00001/D=5，00002/D=5，00003/D=5）。

#### 4.1 静态排序

| 处理序 | 行号 | D | 初始 nextMinQty | D + nextMinQty | 可用QC类型数 | 排序依据 |
|--------|------|---|----------------|---------------|-------------|---------|
| 1 | 00001 | 5 | min(5,5) = 5 | 10 | 1（ZP: T=15 >= 10 ✓） | 可用QC数升序→D降序→行号升序 |
| 2 | 00002 | 5 | min(5) = 5 | 10 | 1（ZP: T=15 >= 10 ✓） | 同 |
| 3 | 00003 | 5 | ∅（最后一行） | — | 1（ZP: T=15 >= D=5 ✓） | 同 |

> 三行可用QC类型数相同（均为1），D 相同（均为5）→ 行号字符串升序：`"00001"` < `"00002"` < `"00003"`。

#### 4.2 分配过程

**第一轮：TK10000480-00001（D=5，处理序=1）**

当前可用：ZP-LA01(T=15)；nextMinQty=5

可用条件：T=15 >= D+nextMinQty=10 ✓ → ZP-LA01 可用，**策略一直接选中**

**选中**：ZP/LA01/2029/06/15 → 分配 5，T = 15→10

行状态：**批量导入**（1种批号/效期组合）

**第二轮：TK10000480-00002（D=5，处理序=2）**

当前可用：ZP-LA01(T=10)；nextMinQty=5（下一行 00003 的 D=5）

可用条件：T=10 >= D+nextMinQty=10 ✓ → ZP-LA01 可用，**策略一直接选中**

**选中**：ZP/LA01/2029/06/15 → 分配 5，T = 10→5

行状态：**批量导入**

**第三轮：TK10000480-00003（D=5，处理序=3，最后一行）**

当前可用：ZP-LA01(T=5)；最后一行 nextMinQty = ∅

可用条件（最后一行仅需 T=D）：T=5=D=5 ✓ → ZP-LA01 可用，**策略一直接选中**

**选中**：ZP/LA01/2029/06/15 → 分配 5，T = 5→0

行状态：**批量导入**

#### 4.3 分配结果

| 行 | 选中 QC/批号/效期 | 分配量 | 库存变化 | 行状态 |
|----|-----------------|--------|---------|--------|
| TK10000480-00001 | ZP / LA01 / 2029/06/15 | 5 | LA01：15→10 | 批量导入 |
| TK10000480-00002 | ZP / LA01 / 2029/06/15 | 5 | LA01：10→5 | 批量导入 |
| TK10000480-00003 | ZP / LA01 / 2029/06/15 | 5 | LA01：5→0 | 批量导入 |

**退单号状态聚合**：TK10000480 全部行"批量导入" → 退单号状态 = **批量导入**

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|-----------|-----------|------|
| SF3190000000048 | TK10000480 | 批量导入 | |

### 5.2 成功分配明细表（3行）

| 物流单号 | WMS退单号 | SKU | 行号 | 退单数量 | QC情况 | 批号 | 效期 | 分配数量 | 行状态 | 退单号状态 |
|---------|-----------|-----|------|----------|--------|------|------|----------|--------|-----------|
| SF3190000000048 | TK10000480 | H000000048 | 00001 | 5 | ZP | LA01 | 2029/06/15 | 5 | 批量导入 | 批量导入 |
| SF3190000000048 | TK10000480 | H000000048 | 00002 | 5 | ZP | LA01 | 2029/06/15 | 5 | 批量导入 | 批量导入 |
| SF3190000000048 | TK10000480 | H000000048 | 00003 | 5 | ZP | LA01 | 2029/06/15 | 5 | 批量导入 | 批量导入 |

> **核心验证**：
> - 行号 `"00001"`、`"00002"`、`"00003"` 均为文本型，VBA 直接读取，无需补零，均满足校验条件 ✓
> - 三行行号从 `00001` 起连续递增，满足 §2.1 连续性要求，E02 不触发 ✓
> - 分配成功（无 E01/E02），证明文本型五位前导零连续行号是标准合法的输入格式 ✓

### 5.3 数据异常明细表

**不生成**（无数据异常，所有校验通过）

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表（每次运行均需验证）

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 3 |
| 输入：质检库存表行数 | 1 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 0 |
| 分配成功物流单号数 | 1 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | （空） |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |
| 追加验证 | 运行后总行数 = 运行前总行数 + 1 |

---

## 6. 补充说明

### 6.1 文本型与数值型行号的差异（新 R011 设计）

| 场景 | Excel 单元格 | VarType | VBA cell.Value | R011 处理 | 结果 |
|------|------------|---------|---------------|-----------|------|
| **本用例（文本型，合法）** | 格式=文本，值=`"00001"` | vbString(8) | `"00001"`（字符串） | CStr 直接读取，不补零 | **合法** ✓ |
| 数值型（新规则下非法）| 格式=自定义`00000`，值=数值`1` | vbDouble | `1`（数字） | CStr(1) = `"1"`，1位 | **E01** ✗ |
| 文本短格式（非法）| 格式=文本，值=`"1"` | vbString | `"1"`（字符串） | 直接读取 `"1"`，1位 | **E01** ✗ |

### 6.2 行号连续性说明

需求 §2.1 要求同一WMS退单号内行号必须从 `00001` 起连续递增，跳号（如 `00001`、`00003` 缺 `00002`）或不从 `00001` 起（如 `00002`、`00003`）均触发 E02。本 TC 使用 `00001`/`00002`/`00003` 满足此要求（正向基线）；跳号负向测试见 TC-51。

### 6.3 DataSet 制作步骤

1. 在 Excel 退单表中，定位行号列（第3列）
2. 选中三行行号单元格 → 右键 → 设置单元格格式 → **文本**
3. 依次输入 `00001`、`00002`、`00003`（直接输入含前导零的字符串）
4. 确认：在 Excel 公式栏中选中单元格，左上角显示绿色小三角（文本标记），且显示的是 `00001` 而非数字 `1`
5. 可用 VBA 立即窗口验证：`?TypeName(Sheets("输入_退单表").Cells(2,3).Value)` 应返回 `String`，而非 `Double` 或 `Long`
