# TC-53 E12 缺列测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | —（结构级异常，发生在装载前） |
| 场景编号 | TC-53 |
| 场景名称 | 输入表缺少必需列触发 E12，整次运行立即中止 |
| 测试目的 | ① 验证缺少必需列（如“数量”列）触发 E12 ② 验证报错信息可定位到缺失列位点 ③ 验证运行不进入后续校验与分配 |
| 覆盖规则 | §4.1 第0层 E12（表头列不符/列缺失）、§6.3.1.1 步骤1a |
| 配置：最大回溯次数 | 任意 |
| 配置：调试日志级别 | 任意 |
| 预期错误码 | E12 |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| `输入_退单表` 第5列表头留空（缺少“数量”） | E12-① 列缺失 |
| 报错应体现“第5列应为[数量]，实际为空” | E12 可读、可定位要求 |

---

## 2. 输入数据

### 2.1 输入_退单表（示例，故意缺列）

| 物流单号 | WMS退单号 | SKU | 行号 | （空） |
|---------|-----------|-----|------|--------|
| SF3190000000053 | TK00000011 | H000000001 | 00001 | 2 |

### 2.2 输入_质检库存表（示例，内容无关）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000053 | H000000001 | ZP | LA01 | 2029/01/01 | 2 |

---

## 3. 分配前校验结论

| 校验层 | 结果 |
|--------|------|
| 第0层（E12）结构完整性 | **命中 E12：输入表缺列** |
| 第1~4层（E01~E11） | **不执行** |

> 结论：E12 命中后整次运行中止。

---

## 4. 分配过程

不进入分配（E12 中止）。

---

## 5. 预期结果

1. 报错信息包含“缺失列/列号/期望列名”。
2. 不生成任何分配结果输出。
3. 运行状态为“E12 中止”。

---

## 6. 补充说明

- TC-52 强调“表头错值”；TC-53 强调“缺列”，两者都属于 E12，但触发路径不同。
