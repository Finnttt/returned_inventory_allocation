# TC-14 E02 同一退单号行号重复测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000014 |
| 场景编号 | TC-14 |
| 场景名称 | 同一 WMS 退单号下两行行号相同（均为 `00001`），触发 E02（重复子情形） |
| 测试目的 | ① 验证同一 WMS 退单号内出现重复行号时触发 E02 ② 验证两行重复行号均进入数据异常明细表 ③ 验证 E02 触发后，该 WMS 退单号及整个物流单号标记为"无法分配" ④ 与 TC-51（E02 跳号/不从00001起）互为对照，共同覆盖 E02 的所有子情形 |
| 覆盖规则 | §2.1（R011）、§4.1 E02（R042）— 行号重复子情形 |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | E02 |
| 是否触发整单回滚 | 否（E02 属于校验阶段失败，未进入分配，不触发整单回滚） |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| TK10000140 下两行行号均为 `00001`（重复），排序后存在重复值 | §2.1（R011）、§4.1（R042） |
| E02 重复检测：排序后序列中存在相邻相同值即触发 | §4.1 R042 行号重复 |

---

## 2. 输入数据

### 2.1 退单表（2行，同一 WMS 退单号，行号重复）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|----------|-----|------|------|---------|
| TK10000140 | H000000014 | 00001 | 5 | SF3190000000014 |
| TK10000140 | H000000014 | 00001 | 3 | SF3190000000014 |

> **说明**：TK10000140 下两行行号均为 `00001`，构成重复序列，触发 E02。
>
> ⚠️ **DataSet 制作关键**：行号列所有单元格须设为**文本格式**，直接输入 `00001`（含前导零）。

### 2.2 质检库存表（1行）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000014 | H000000014 | ZP | LA01 | 2029/06/15 | 8 |

> **说明**：库存数量 = 退单合计（5+3=8），E08 不触发，E06/E07 不触发，E02 作为唯一错误码出现。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01）关键字段完整性/格式 | 行号格式合法（`00001` 为五位纯数字文本） | 通过 |
| 第1层（E02）行号连续性/重复 | 同一 WMS 退单号内行号不得重复 | **命中 E02**（TK10000140：序列 [1,1]，存在重复） |
| 第2层（E03~E05）字段值合法性 | 继续执行（四层均执行），但此处不命中 | 通过 |
| 第3层（E06~E08）跨表一致性 | 继续执行，但此处不命中 | 通过 |
| 第4层（E11）QC碎片 | 继续执行，但此处不命中 | 通过 |

> **结论**：第一层命中 E02，整个物流单号 SF3190000000014 下所有行标记为"无法分配"，**不进入分配阶段**。

---

## 4. 分配过程

**不进行分配**（E02 在校验阶段已标记失败，未进入分配循环）。

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|----------|-----------|------|
| SF3190000000014 | TK10000140 | 无法分配 | E02 - 退单表行号重复或不连续（行号 `00001` 在同一退单号下出现 2 次） |

### 5.2 成功分配明细表

**不生成**（E02 导致分配失败，无任何成功分配记录）

### 5.3 数据异常明细表

| 来源表 | 原始行号 | 物流单号 | WMS退单号 | SKU | 异常字段名 | 原始值 | 错误码 | 原因说明 |
|--------|----------|---------|-----------|-----|------------|--------|--------|----------|
| 退单表 | 2 | SF3190000000014 | TK10000140 | H000000014 | 行号 | 00001 | E02 | 行号重复：`00001` 在同一退单号下出现 2 次 |
| 退单表 | 3 | SF3190000000014 | TK10000140 | H000000014 | 行号 | 00001 | E02 | 行号重复：`00001` 在同一退单号下出现 2 次 |

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 2 |
| 输入：质检库存表行数 | 1 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 1 |
| 分配成功物流单号数 | 0 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | E02 × 1（TK10000140）|
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |

---

## 6. 补充说明

### 6.1 E02 三种子情形总览

| 子情形 | TC | DataSet | 场景 |
|--------|-----|---------|------|
| **行号重复** | **TC-14**（本用例）| SF0014 | 同一退单号两行均为 `00001` |
| **跳号** | TC-51 | SF0051 | 序列 [00001, 00003]，缺 `00002` |
| **不从 `00001` 起** | TC-51 | SF0051 | 首行为 `00002` |

三种子情形同属 R042，原因说明中通过文字区分。
