# 项目协作规则

## 项目定位

本项目是 Excel VBA 退货入库分配系统。生产代码采用 M01～M15 分层，`modTestRunner.bas` 为 M16 测试入口；`modBatchTestRunner.bas` 为 M17 批量 Excel 回归扩展（2026-07-19 拍板：独立编号，不并入 M16）。

## 权威资料

| 内容 | 权威文件 |
|---|---|
| 业务规则与算法 | `退货入库分配系统_需求与技术方案.md` |
| 模块边界与接口 | `.cursor/plans/模块划分方案_2bd2f2bf.plan.md` |
| 测试覆盖与进度 | `规则-TC-模块-数据集映射表.md` |
| 测试目的 | `测试场景清单` |
| 单个用例的输入和预期 | `TC-*.md` |
| 调试日志字段 | `调试日志19列规格说明.md` |

## 修改红线

1. 不得在没有同步需求和相关 TC 的情况下改变业务规则。
2. 不得破坏 M01～M15 的职责边界；跨模块共用类型和常量优先放在 `modTypes.bas`。
3. 行号按 `CStr` 读取，不自动左补零；合法值必须是五位纯数字文本。
4. 调试日志固定为 19 列，默认级别为“关闭”。
5. `RunAllTests` 不包含文件集成和批量 Excel 回归，不得把它的通过等同于全量验收通过。
6. 修改 Excel 的 PowerShell 脚本前，应确认目标工作簿已关闭并保留备份。
7. 未经确认不得删除 Excel 备份、合并重复脚本或改变 `modBatchTestRunner` 的模块编号。

## 验证顺序

0. 无头快速路径：`powershell -ExecutionPolicy Bypass -File .\运行无头验收.ps1` 一键完成以下 1～4 并给出通过/失败判定。
1. 在 Excel VBA 中运行 `RunAllTests`。
2. 有外部 DataSet 时运行 `RunSingleTest 17`。
3. 运行 `RunSingleTest 18` 验证批量测试器。
4. 按“批量测试计划”执行 `RunBatchTestPlan`。
5. 将执行结果同步到映射表；没有实际运行时，只能写“代码/文档已就绪”，不能写“测试已通过”。

## 文档维护

- 新增或修改 TC 时，同步检查场景清单、映射表和对应 `TC-*.md`。
- 新增错误码、配置项或输出列时，同步检查需求、测试模板和相关脚本。
- 文档以就地更新为主，不追加会过期的会话流水账。
- 项目已纳入 Git 版本管理并推送至 GitHub 私有仓库 `Finnttt/returned_inventory_allocation`（2026-07-20，main 分支）；修改 Excel 前仍需确认工作簿已关闭（相关脚本已内置占用检测与自动备份）。
