# TC-13 E01 关键字段为空或格式异常测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000013 |
| 场景编号 | TC-13 |
| 场景名称 | 退单表行号含非数字字符（格式非法）+ WMS退单号为空，触发 E01 |
| 测试目的 | ① 验证行号含字母时触发 E01（格式非法） ② 验证 WMS退单号为空时触发 E01（字段为空） ③ 验证 E01 后整个物流单号所有行标记为"无法分配" ④ 验证两处 E01 均进入数据异常明细表，便于文员定位 |
| 覆盖规则 | §4.1 第一层校验 E01（R041） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | E01 |
| 是否触发整单回滚 | 否（E01 属于校验阶段失败，未进入分配，不触发整单回滚） |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 退单表第2行：行号 = `1AB23`（含字母，非五位纯数字文本） | §4.1（R041）行号格式非法 |
| 退单表第3行：WMS退单号为空（空字符串） | §4.1（R041）关键字段为空 |

---

## 2. 输入数据

### 2.1 退单表（3行，同一物流单号下含两处 E01 缺陷）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|----------|-----|------|------|---------|
| TK10000130 | H000000013 | 00001 | 3 | SF3190000000013 |
| TK10000130 | H000000013 | 1AB23 | 2 | SF3190000000013 |
| （空） | H000000013 | 00003 | 5 | SF3190000000013 |

> **第2行说明**：行号 `1AB23` 含字母，非五位纯数字文本，触发 E01（格式异常）。
>
> **第3行说明**：WMS退单号为空，触发 E01（字段为空）。
>
> ⚠️ **DataSet 制作关键**：行号列所有单元格须设为**文本格式**，直接输入含前导零的字符串。

### 2.2 质检库存表（1行）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000013 | H000000013 | ZP | LA01 | 2029/06/15 | 10 |

> **说明**：库存数量故意填 10（大于退单合计 3+2+5=10），目的是确保 E06/E07/E08 不触发，让 E01 作为唯一错误码出现，便于验证。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01）关键字段完整性/格式 | 行号须为五位纯数字文本，WMS退单号不得为空 | **命中 E01** × 2（行2：行号格式异常；行3：WMS退单号为空） |
| 第1层（E02）行号连续性 | 依赖 E01 通过的行号参与连续性判断；E01 命中行跳过 | 不适用（整单已因 E01 失败） |
| 第2层（E03~E05）字段值合法性 | 继续执行（四层均执行），但此处不命中 | 通过 |
| 第3层（E06~E08）跨表一致性 | 继续执行，但此处不命中 | 通过（两表物流单号一致，库存≥退单合计） |
| 第4层（E11）QC碎片 | 继续执行，但此处不命中 | 通过 |

> **结论**：第一层命中 E01 × 2，整个物流单号 SF3190000000013 下所有行标记为"无法分配"，**不进入分配阶段**。

---

## 4. 分配过程

**不进行分配**（E01 在校验阶段已标记失败，未进入分配循环）。

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|----------|-----------|------|
| SF3190000000013 | TK10000130 | 无法分配 | E01 - 关键字段为空或格式异常 |
| SF3190000000013 | [N/A] | 无法分配 | E01 - 关键字段为空或格式异常 |

> **说明**：第3行 WMS退单号为空，在汇总表中该列填 `[N/A]`（§5.1 R122 扩展适用）。

### 5.2 成功分配明细表

**不生成**（E01 导致分配失败，无任何成功分配记录）

### 5.3 数据异常明细表

| 来源表 | 原始行号 | 物流单号 | WMS退单号 | SKU | 异常字段名 | 原始值 | 错误码 | 原因说明 |
|--------|----------|---------|-----------|-----|------------|--------|--------|----------|
| 退单表 | 3 | SF3190000000013 | TK10000130 | H000000013 | 行号 | 1AB23 | E01 | 行号格式异常：须为五位纯数字文本（如 `00001`），当前含非数字字符 |
| 退单表 | 4 | SF3190000000013 | [N/A] | H000000013 | WMS退单号 | [N/A] | E01 | 关键字段为空 |

> **原始行号说明**：Excel 数据从第2行开始（第1行为表头），故退单表第1数据行 = 原始行号2，依次类推。

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 3 |
| 输入：质检库存表行数 | 1 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 1 |
| 分配成功物流单号数 | 0 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | E01 × 2 |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |

---

## 6. 补充说明

### 6.1 E01 触发的两种子情形

| 子情形 | 示例（本 TC） | 检测逻辑 |
|--------|------------|---------|
| **格式异常** | 行号 `1AB23` 含字母 | 不满足"五位纯数字文本"格式 |
| **字段为空** | WMS退单号为空字符串 | 必填字段 Trim 后长度 = 0 |

### 6.2 与 TC-48（行号合法正向基线）的关系

TC-48（SF0048）验证行号合法性，TC-13 为其反向对照：行号格式非法时系统能正确拦截。
