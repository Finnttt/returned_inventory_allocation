A. 基础成功场景
TC-01 策略一：单行精确匹配成功
目的：

验证 qty = D 时命中策略一

行状态 = 批量导入

退单号状态 = 批量导入

TC-02 策略二：单行超量匹配成功
目的：

验证 qty > D 时命中策略二

库存部分扣减正确

行状态 = 批量导入

TC-03 策略三：同一QC多行拼凑成功
目的：

验证策略三成功

行状态 = 手工操作

使用多个批号/效期组合

TC-04 同一退单号下多行，全部批量导入
目的：

验证退单号状态聚合 = 批量导入

TC-05 同一退单号下至少一行为手工操作
目的：

验证退单号状态聚合 = 手工操作

B. QC一致性与规则边界场景
TC-06 同一行不能跨QC
目的：

验证同物流单号 + 同WMS退单号 + 同行号只能匹配一种QC

TC-07 同一退单号下不同行可以不同QC
目的：

验证“不同退货行可使用不同QC”这一允许规则

TC-08 同一物流单号下多个WMS退单号共享同一SKU库存池
目的：

验证共享库存池的业务背景成立

为“整单回滚”提供业务支撑

TC-09 策略一平局：同 qty = D，不同 QC 【已有用例→TC-09_12_策略一QC优先级与动态minQty测试用例.md】
目的：

验证优先级 ZP > QC > NG

TC-10 策略一 / 二平局：同 QC，不同效期 【已有用例→TC-10_策略平局测试用例.md】
目的：

验证“效期更晚优先”

TC-11 静态排序验证 【已有用例→TC-11_静态排序四级规则全触发测试用例.md】
目的：

验证处理顺序：

可用QC数升序

数量降序

WMS退单号 + 行号升序

TC-12 动态 minQty 变化导致候选QC变化 【已有用例→TC-12_动态nextMinQty候选QC变化测试用例.md】
目的：

验证动态筛选逻辑真实生效

C. 数据校验失败场景
TC-13 E01：关键字段为空
目的：

验证关键字段为空时直接失败

TC-14 E02：退单表行号重复
目的：

验证同一WMS退单号下行号重复时报错

TC-15 E03：QC情况非法
目的：

验证 QC 非 ZP/QC/NG 时失败

TC-16 E04：数量非法
目的：

验证数量为负数或零时失败

TC-17 E05：效期格式非法
目的：

验证非法日期被识别

TC-18 E06：物流单号仅存在于退单表
目的：

验证物流单号集合不一致

TC-19 E07：物流单号仅存在于质检库存表
目的：

验证物流单号集合不一致

TC-20 E08：同物流单号 + SKU 数量不一致
目的：

验证数量一致性校验

D. 分配失败与回溯场景
TC-21 当前路径失败，回溯后成功 【已有用例→TC-21_回溯成功与五元组汇总测试用例.md】
目的：

验证回溯机制有效

验证前序 QC 改选后能让后续成功

TC-22 当前路径失败，所有回溯路径都失败 【已有用例→TC-22_E09分配路径穷尽测试用例.md】
目的：

验证最终无法分配逻辑

TC-23 E09：候选QC全部不可用且无法回溯 【已有用例→TC-22_E09分配路径穷尽测试用例.md】
目的：

验证分配阶段失败错误码

TC-24 E10：回溯超限 【已有用例→TC-24_E10回溯超限与整单回滚测试用例.md】
目的：

验证回溯次数上限生效

备注：
测试时可临时把“最大回溯次数”调小，例如 2 或 3，方便构造样例。

E. 整单回滚场景
TC-25 同一物流单号下部分行本可成功，但另一部分失败 【已有用例→TC-22_E09分配路径穷尽测试用例.md（整单回滚机制）；TC-24_E10回溯超限与整单回滚测试用例.md（短路总原则）】
目的：

验证整单回滚触发

拆分追踪：

TC-25a 验证部分行成功、部分行失败后的整单回滚（R062）

TC-25b 验证某 SKU 触发 E10 后，后续 SKU 短路并连带回滚（R116）

TC-26 整单回滚后成功明细表不应输出该物流单号任何记录 【已有用例→TC-22_E09分配路径穷尽测试用例.md】
目的：

验证输出结果符合整单回滚规则

TC-27 整单回滚后该物流单号下所有WMS退单号状态 = 无法分配 【已有用例→TC-22_E09分配路径穷尽测试用例.md】
目的：

验证状态聚合符合整单回滚规则

F. 输出与人工处理场景
TC-28 汇总表原因字段格式校验
目的：

验证原因字段为：错误码 - 原因说明

TC-29 调试日志开启时生成日志表 【已有用例→TC-06_同一行不能跨QC与预检测A测试用例.md；TC-21_回溯成功与五元组汇总测试用例.md；TC-22_E09分配路径穷尽测试用例.md；TC-24_E10回溯超限与整单回滚测试用例.md；TC-49_效期标准化测试用例.md】
目的：

验证调试日志功能可用

TC-30 调试日志关闭时不生成日志表
目的：

验证可选输出逻辑

TC-31 人工处理路径验证
目的：

验证失败后文员、客服、仓库、开发的处理路径是否清晰

G. 建议增加的增强场景
TC-32 无保质期商品使用固定效期值
目的：

验证 2099/01/01 之类固定值处理正确

TC-33 同一五元组原始多行汇总后再分配 【已有用例→TC-21_回溯成功与五元组汇总测试用例.md】
目的：

验证库存总逻辑正确

TC-34 同一物流单号下多个SKU分别分配 【已有用例→TC-21_回溯成功与五元组汇总测试用例.md】
目的：

验证 SKU 组之间处理逻辑清晰

TC-35 大小批量混合场景 【已有用例→TC-35_大小批量混合场景测试用例.md】
目的：

验证在接近真实数据的情况下整体结果合理


H. 校验阶段补充场景（E11 与多错误码）

TC-36 E11：QC库存碎片无法分配
目的：

验证第4层校验（E11）能识别"有正库存但小于该组最小行需求"的碎片情形

验证 T=0 的QC类型不触发E11（只有 0 < T < groupMinQty 才触发）

验证 E11 触发后该物流单号所有行标记为"无法分配"

验证汇总表中 E11 的原因格式为"E11 - QC库存碎片无法分配"

备注：
E11 属于"充分条件检测"，命中必然失败，但未命中不代表一定成功。
可构造场景：同物流单号+SKU，退货行需求均为5，某QC总库存为3（0 < 3 < 5，触发E11）。

TC-37 同一物流单号命中多个错误码
目的：

验证校验阶段"记录所有命中错误码"的规则——不因命中第一个就跳过后续校验

验证汇总表原因字段格式：多错误码按升序排列、同码去重、分号分隔
（如 "E01 - 关键字段为空或格式异常; E04 - 数量非法"）

备注：
可构造场景：同一物流单号在退单表有行号格式错误（E01）、数量为负（E04），两个错误同时存在。

TC-38 E07 [N/A] 占位符验证
目的：

验证 E07（物流单号仅存在于质检库存表）在汇总表中的专用记录格式

验证 WMS退单号字段填写 [N/A]（因退单表中无对应记录，无法关联退单号）

验证原因字段为 "E07 - 物流单号仅存在于质检库存表"

备注：
TC-19 已验证 E07 错误码本身能触发，本用例专门验证汇总表输出中 [N/A] 占位符的正确性。


I. E09 分配预检测场景

TC-39 E09 预检测A：初始可用QC数=0
目的：

验证静态排序后、进入主分配循环前，预检测A能识别"某行初始可用QC数=0"的情形

验证命中后该(物流单号+SKU)组所有行立即标记为 E09，不进入分配循环、不触发回溯

验证调试日志中 E09失败子类型 = "预检测A（初始可用QC=0）"

备注：
与 TC-23 的区别：TC-23 测试的是"进入分配循环后动态无可用QC"；本用例测试的是
"尚未进入循环、静态预检测阶段即已拦截"，代表完全不同的代码路径。
可构造场景：某行需求量D=10，所有QC总库存均满足 0 < T < D+nextMinQty 且 T ≠ D，
则该行初始可用QC=0，预检测A命中。

TC-40 E09 预检测B：多行强制竞争同一QC、库存不足
目的：

验证预检测B能识别"多行初始可用QC数=1且QC类型相同，但该QC总库存不足以覆盖这些行总需求"的情形

验证触发条件：T ≠ S 且 T < S + minQtyOther（S=强制竞争行需求之和，minQtyOther=非强制行最小需求）

验证命中后错误码=E09，调试日志中 E09失败子类型 = "预检测B（强制竞争库存不足）"

备注：
可参考需求文档 §4.2.3 中的示例：需求1/2/5/6，库存ZP-7/QC-4/NG-3。
D=5 和 D=6 均只能用ZP，S=11，T=7，7<11+1=12，预检测B命中。


J. 输出格式与运行机制验证场景

TC-41 数据异常明细表（§5.4）生成与字段验证
目的：

验证命中 E01/E02/E03/E04/E05/E07 时，数据异常明细表正确生成

验证表字段完整性：来源表、原始行号、物流单号、WMS退单号、SKU、异常字段名、原始值、错误码、原因说明

验证 E06/E08/E11 不产生数据异常明细表记录（仅出现在汇总表中）

验证E02时，重复的两行均被记录（无法判断哪行正确）

验证校验全部通过时，本表为空或不生成

TC-42 连带回滚原因格式验证
目的：

验证整单回滚时，"直接触发错误的退单号"原因格式为：
"错误码 - 原因说明"（如 "E10 - 回溯超限"）

验证整单回滚时，"未直接触发错误、仅被连累的退单号"原因格式为：
"整单回滚（触发原因：E10）"

备注：
TC-27 验证了连带回滚后状态=无法分配，本用例专门验证两种原因格式的区分是否正确。
可构造场景：同一物流单号下两个退单号，退单号A的SKU触发E10，退单号B的SKU本可成功，
验证汇总表中两行的原因字段格式不同。

TC-43 重跑覆盖机制验证 【已有用例→TC-43_R127重跑覆盖测试用例.md】
目的：

验证点击"开始分配"时，上次运行产生的所有输出表数据被清空（保留表头）

验证运行历史记录表不被清空（追加式保留）

验证配置工作表不被清空

验证上次"详细"模式产生的调试日志分表（如"调试日志_2"）在本次运行前被删除

TC-44 运行历史记录表（§5.6）追加写入验证
目的：

验证每次运行（Dry Run 或 Full Run）结束后，运行历史记录表自动追加一条记录

验证 Dry Run 时，运行类型字段 = "Dry Run"，分配相关字段（分配成功数、分配失败数、总回溯次数）填0

验证 Full Run 时，运行类型字段 = "Full Run"，各统计字段（错误码分布、回溯次数等）正确填写

验证多次运行后，表中记录按时间顺序逐行追加，历史记录不丢失

TC-45 Dry Run（仅运行校验）模式 【已有用例→TC-45_R128_DryRun测试用例.md】
目的：

验证点击"仅运行校验"时，只执行 §4.1 的四层校验，不执行分配逻辑

验证输出：生成"数据异常明细表"和"分配状态汇总表"（仅含校验错误码 E01~E08/E11）

验证不输出：不生成"成功分配明细表"、不生成"调试日志表"

验证汇总表中"通过校验的物流单号"不出现在本次汇总表中（避免误以为已分配）

验证运行历史表中记录一条 Dry Run 记录


K. 数据标准化边界场景

TC-46 批号大小写不敏感处理
目的：

验证质检库存表中批号 "a01" 与 "A01" 在默认"不敏感"模式下被视为同一批号

验证标准化后（Trim + UCase）的批号参与五元组汇总，库存不会因大小写差异"分裂"为两条

验证输出的批号统一为大写形式

备注：
配置项"批号比较模式=敏感"时应反向验证（"a01"与"A01"视为不同批号）。

TC-47 QC字段小写输入标准化（容错验证）
目的：

验证质检库存表中 QC 字段填写小写（如 "zp"、"Qc"、"nG"）时，经 Trim→UCase→校验后合法识别，不触发 E03

验证标准化后参与分配的 QC 值为大写（ZP/QC/NG）

备注：
与 TC-15 的区别：TC-15 测试的是真正非法的QC值（如"QM"）触发E03；
本用例测试大小写容错，输入应合法通过而非报错。

TC-48 行号文本格式合法性（正向基线）
目的：

验证 Excel 单元格以文本型存储 "00001"、"00002"、"00003" 时，
VBA 经 CStr 读取后保持五位纯数字文本，视为合法，不触发 E01

验证行号从 "00001" 起连续递增，不触发 E02

备注：
系统不会自动左补零。数值型单元格即使通过自定义格式显示为 "00001"，
底层值仍为数字 1，CStr 后不足五位，应触发 E01；对应负向场景见 TC-13。

TC-49 效期字段多格式读取与标准化 【已有用例→TC-49_效期标准化测试用例.md】
目的：

验证 Excel 日期型（vbDate=7）的效期被正确读取并标准化为 YYYY/MM/DD 格式

验证 文本型 YYYY/MM/DD（vbString=8）的效期通过字符串级校验后正确标准化

验证 文本型 YYYY-MM-DD（vbString=8，含连字符分隔）的效期通过字符串级校验后正确标准化

验证三种格式标准化后可正确参与效期比较（策略一平局时效期更晚选中，以 R105 兜底证明标准化有效）

备注：
效期单元格格式（日期型 vs 文本型）影响 Excel 存储的 VarType，DataSet 制作时须先设置单元格格式再输入值，
顺序颠倒可能导致 Excel 自动转换类型，使测试意义丢失。
与 TC-17 的区别：TC-17 验证效期格式非法（E05）触发报错；本用例专门验证合法的多种格式输入均被正确标准化、且标准化结果可参与比较和输出。

TC-50 分配后校验（M12）
目的：

验证正确的最终分配结果不会被误报；
通过故障注入验证数量不守恒、同一退单行出现两种 QC 时，ValidatePost 能检出错误；
验证整单回滚的物流单号不参与分配后校验。

备注：
这是纯 VBA 单元测试，无 Excel DataSet，执行入口为 RunPostValidateTests()。

TC-51 行号跳号与不连续触发 E02
目的：

验证同一 WMS 退单号内行号跳号，或首行不从 "00001" 开始时触发 E02；
验证相关退单行进入数据异常明细表，且不产生成功分配明细。

备注：
与 TC-48 的合法连续行号正向基线互为对照。

TC-52 输入表表头错位触发 E12
目的：

验证必需表头名称错误时触发 E12；
验证错误信息包含工作表、列号、期望表头和实际表头；
验证运行在数据加载前中止。

TC-53 输入表缺少必需列触发 E12
目的：

验证输入表缺少"数量"等必需列时触发 E12；
验证错误信息能够定位缺失列，且不进入后续校验和分配。

TC-54a 最大回溯次数配置非法
目的：

验证最大回溯次数为负数等非正整数时，配置读取失败；
验证错误信息包含配置项名称、非法值和合法范围。

TC-54b 调试日志级别配置非法
目的：

验证调试日志级别不是"关闭/简版/详细"时，配置读取失败；
验证错误信息包含配置项名称和允许取值。

备注：
TC-54a 与 TC-54b 分开测试，避免多个非法值同时出现时因首错短路导致断言不稳定。

TC-55 WMS 退单号跨物流单号重复触发 E12
目的：

验证同一 WMS 退单号出现在两个不同物流单号下时触发 E12；
验证错误信息包含退单号、两个物流单号和当前行号；
验证整次运行立即中止。

TC-56 库存账本单元测试（M06）
目的：

验证相同五元组汇总、不同批号独立存储、非法行跳过；
验证库存扣减、撤销、快照与查询结果保持一致。

备注：
这是纯 VBA 单元测试，无 Excel DataSet，执行入口为 RunLedgerTests()。