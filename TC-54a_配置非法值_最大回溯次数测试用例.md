# TC-54a 配置非法值（最大回溯次数）测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000054 |
| 场景编号 | TC-54a |
| 场景名称 | 输入_配置中最大回溯次数为负数时，配置读取失败并给出可定位错误 |
| 测试目的 | ① 验证 `最大回溯次数=-1` 触发配置读取错误 ② 验证错误信息包含配置项名与非法值 ③ 验证运行在配置阶段中止 |
| 覆盖规则 | §6.7.6 配置区域（最大回溯次数为正整数）、M02 配置合法性校验 |
| 预期错误码 | 配置读取错误（非 E01~E12） |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| `最大回溯次数 = -1` | 非正整数，配置非法 |
| `调试日志级别 = 关闭`（合法） | 排除其他配置项干扰，确保只命中一个异常 |

---

## 2. 输入数据（最小集）

### 2.1 输入_配置（关键字段）

| 物流单号 | TC编号 | 最大回溯次数 | 调试日志级别 | 批号比较模式 | 无保质期哨兵值 |
|---------|--------|--------------|--------------|--------------|----------------|
| SF3190000000054 | TC-54a | -1 | 关闭 | 不敏感 | 2099/01/01 |

---

## 3. 预期结果

1. 读取配置失败；
2. 错误信息包含：`最大回溯次数`、`必须是正整数`、`-1`；
3. 不进入后续数据加载与校验。

---

## 4. 补充说明

- 本用例与 TC-54b 成对使用，避免“一次多个非法值导致首错短路”影响断言稳定性。
