# TC-01/TC-30 策略一：单行精确匹配成功 & 调试日志关闭时不生成日志表

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000000 |
| 场景编号 | TC-01 / TC-30 |
| 场景名称 | 策略一：单行精确匹配成功；调试日志关闭时不生成日志表 |
| 测试目的 | ① 验证当质检库存五元组数量恰好等于退单需求量（qty = D）时，策略一命中；② 验证行状态 = 批量导入；③ 验证退单号状态 = 批量导入；④ 验证配置"调试日志级别=关闭"时，系统不生成调试日志表工作表（TC-30）；⑤ 验证 Full Run 后运行历史记录表追加 1 行（TC-44 嵌入覆盖） |
| 覆盖规则 | §4.2.2 可用QC筛选（最后一行条件）；§4.2.4 策略一；§4.4.1 行级状态判定；§4.4.2 退单号状态聚合；§5.1 分配状态汇总表；§5.2 成功分配明细表；§5.3 调试日志表（关闭场景） |
| 配置：最大回溯次数 | 200（默认） |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | 无 |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 质检库存五元组 ZP:LA01:2029/01/01 的数量 = 2，恰好等于退单需求 D = 2，触发策略一精确匹配 | §4.2.4 策略一 |
| 本组仅 1 行退货（最后一行），nextMinQty = ∅，可用QC筛选条件为 T = D | §4.2.2 最后一行筛选条件 |
| 配置"调试日志级别=关闭"，运行结束后不应存在调试日志表工作表 | §5.3 调试日志表生成规则；§6.7.6 配置区域 |

---

## 2. 输入数据

### 2.1 退单表（1 行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|-----------|-----|------|------|---------|
| TK00000001 | H000000001 | 00001 | 2 | SF3190000000000 |

### 2.2 质检库存表（1 行，已是唯一五元组）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000000 | H000000001 | ZP | LA01 | 2029/01/01 | 2 |

> **数量校验**：SKU H000000001 退单合计 = 2，库存合计 = 2 ✓
>
> **五元组汇总**：仅 1 行，无需合并。
>
> **分配前校验预判**：E01～E11 全部通过。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01～E05）数据完整性 | 关键字段非空；行号 `00001` 满足五位前导零格式；QC情况 `ZP` 合法；数量 `2` 为正整数；效期 `2029/01/01` 满足 YYYY/MM/DD 格式且为合法日期 | 通过 |
| 第2层（E06/E07）物流单号一致性 | 退单表与质检库存表均包含 SF3190000000000，两表物流单号集合完全一致 | 通过 |
| 第3层（E08）数量一致性 | SF3190000000000 + H000000001：退单合计 2 = 库存合计 2 | 通过 |
| 第4层（E11）QC碎片可分配性 | groupMinQty = 2（组内唯一行需求）；ZP 总库存 T = 2：0 < 2 < 2？否（T = groupMinQty，不满足严格小于条件）；QC/NG 总库存 T = 0，T = 0 不触发 E11 | 通过 |

> **结论**：四层全部通过，进入分配阶段。

---

## 4. 分配过程推导

### 4.1 SF3190000000000 — SKU H000000001

库存池（1 条五元组）：ZP:LA01:2029/01/01:2

#### 4.1.1 静态排序

> **说明**：本 SKU 组仅 1 行退单，nextMinQty 定义为"当前行分配后剩余未分配行的最小需求量"，此处无其他行，初始 nextMinQty = ∅（最后一行条件）。
>
> 筛选可用 QC 时适用最后一行条件：仅当 T = D 时该 QC 可用。ZP 总库存 T = 2，D = 2，T = D ✓，ZP 可用；QC/NG 总库存 T = 0，T ≠ D，不可用。

| 处理序 | 行号 | 所属退单号 | D | 初始 nextMinQty | 初始可用QC数 | 排序依据 |
|--------|------|-----------|---|----------------|------------|---------|
| 1 | 00001 | TK00000001 | 2 | ∅（最后一行） | 1（仅 ZP） | 唯一行，无需排序 |

#### 4.1.2 预检测

**预检测A**：行 00001 初始可用 QC 数 = 1 ≠ 0 → **不触发**。

**预检测B**：仅 1 行满足"初始可用QC数=1"，强制竞争行数 < 2 → **不触发**。

> 结论：预检测通过，进入主分配循环。

#### 4.1.3 分配过程

| 行号 | D | 候选QC | nextMinQty | 策略 | 选中（QC:批号:效期） | 分配量 | 库存变化 | 行状态 |
|------|---|-------|-----------|------|-------------------|-------|---------|--------|
| 00001 | 2 | {ZP} | ∅（最后一行） | 策略一 | ZP:LA01:2029/01/01 | 2 | ZP: 2 → 0 | 批量导入 |

**策略一命中推导**：
- 动态可用 QC 筛选（最后一行，T = D 条件）：ZP T = 2，T = D = 2 ✓ → ZP 进入候选池
- 策略一：在候选池中寻找 qty = D 的五元组 → ZP:LA01:2029/01/01 qty = 2 = D = 2 ✓ → 命中
- 扣减 2，ZP 剩余 0；该行使用 1 种批号/效期组合（LA01:2029/01/01）→ 行状态 = **批量导入**

**退单号状态聚合**：TK00000001 下所有行（仅 00001）均为批量导入，无失败行 → 退单号状态 = **批量导入**

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|-----------|-----------|------|
| SF3190000000000 | TK00000001 | 批量导入 | （空） |

### 5.2 成功分配明细表（1 行）

| 物流单号 | WMS退单号 | SKU | 行号 | 退单数量 | QC情况 | 批号 | 效期 | 分配数量 | 行状态 | 退单号状态 |
|---------|-----------|-----|------|----------|--------|------|------|----------|--------|-----------|
| SF3190000000000 | TK00000001 | H000000001 | 00001 | 2 | ZP | LA01 | 2029/01/01 | 2 | 批量导入 | 批量导入 |

### 5.3 数据异常明细表

不生成（校验全部通过，无 E01～E05/E07 错误）。

### 5.4 调试日志表（TC-30 专项验证）

**不生成**——配置"调试日志级别 = 关闭"，系统不创建调试日志表工作表。

> 注意：调试日志表工作表不存在（或内容为空）时视为正确。但运行历史记录表中的"总回溯次数"和"最大单组回溯次数"字段**仍须有正确值**（见 §6.5.4），不因关闭日志而置零或省略。

### 5.5 运行历史记录表（TC-44 嵌入覆盖）

> **TC-44 说明**：运行历史是**每次点击运行追加 1 行**，不是每个物流单号 1 行。本用例仅 1 个物流单号、运行 1 次，故期望运行历史表总行数 = 运行前 + 1。

| 字段 | 期望值 | 说明 |
|------|--------|------|
| 运行类型 | Full Run | 点击"开始分配" |
| 输入：退单表行数 | 1 | 与第 2.1 节一致 |
| 输入：质检库存表行数 | 1 | 与第 2.2 节一致 |
| 输入：物流单号数 | 1 | SF3190000000000 |
| 校验失败物流单号数 | 0 | 四层校验全部通过 |
| 分配成功物流单号数 | 1 | SF3190000000000 分配成功 |
| 分配失败物流单号数 | 0 | 无失败 |
| 错误码分布 | （空） | 无任何错误码命中 |
| 总回溯次数 | 0 | 无回溯，单行直接命中策略一 |
| 最大单组回溯次数 | 0 | 同上 |
| 追加验证 | 运行后总行数 = 运行前总行数 + 1 | 确认历史记录只增不减，不覆盖历史 |

---

## 6. 补充说明

- **TC-01 与 TC-30 合并原因**：两者使用完全相同的输入数据和配置（调试日志级别=关闭），在同一次运行中同时验证，无需重复构造数据。TC-01 的核心验证点是策略一命中及行/退单号状态；TC-30 的核心验证点是调试日志表工作表不生成。
- **TC-30 与其他成功类TC的区别**：TC-03、TC-04 等其他成功场景均可选择开启或关闭调试日志，**TC-30 专门选择"关闭"配置**，以验证可选输出逻辑的"关闭分支"——即系统不应因调试日志逻辑出现空表或报错，而应完全静默不生成。
- **最后一行筛选条件示例**：本用例仅含 1 行退单，恰好是最后一行条件（T = D）的最简验证，适合与 TC-02（非最后一行，T ≥ D + nextMinQty）对照理解。

## 7. M14 Excel写入联调核对清单（新增）

> 用于补足“业务逻辑已通过，但 Excel 落表行为仍需核对”的风险点。

1. 运行前在 `分配状态汇总表`、`成功分配明细表`、`数据异常明细表` 人工填入一行脏数据；运行后确认：**表头保留、数据区被清空后再写入新结果**。
2. 运行前在 `运行历史记录表` 末尾记录一条人工标记；运行后确认：**历史记录为追加，不覆盖旧行**。
3. 校对 `成功分配明细表` 的列顺序必须是：`物流单号, WMS退单号, SKU, 行号, 退单数量, QC情况, 批号, 效期, 分配数量, 行状态, 退单号状态`。
4. 校对 `分配状态汇总表` 的列顺序必须是：`物流单号, WMS退单号, 退单号状态, 原因`。
5. 本用例配置 `调试日志级别=关闭`，确认：`调试日志` 及其分表（如 `调试日志_2`）**不保留旧数据且不产生新日志数据**。
