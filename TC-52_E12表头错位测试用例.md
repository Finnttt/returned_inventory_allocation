# TC-52 E12 表头错位测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | —（结构级异常，发生在装载前） |
| 场景编号 | TC-52 |
| 场景名称 | 输入表头列名错误（错字/错位）触发 E12，整次运行立即中止 |
| 测试目的 | ① 验证输入表头与系统预设不一致时触发 E12 ② 验证错误信息包含“工作表+列号+期望表头+实际表头” ③ 验证运行在数据加载前中止，不进入后续校验与分配 |
| 覆盖规则 | §4.1 第0层 E12（表头校验）、§6.3.1.1 步骤1a |
| 配置：最大回溯次数 | 任意（本用例不进入分配） |
| 配置：调试日志级别 | 任意（本用例不进入分配） |
| 预期错误码 | E12 |
| 是否触发整单回滚 | 否（整次运行中止，不进入分配） |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 将 `输入_退单表` 第1列表头由 `物流单号` 改为 `物流单号X` | E12-① 表头列不符 |
| 运行时应抛出定位信息（例如“第 1 列应为 [物流单号]，实际为 [物流单号X]”） | E12 可读、可定位要求 |

---

## 2. 输入数据

### 2.1 输入_退单表（示例）

| 物流单号X | WMS退单号 | SKU | 行号 | 数量 |
|-----------|-----------|-----|------|------|
| SF3190000000052 | TK00000011 | H000000001 | 00001 | 2 |

### 2.2 输入_质检库存表（示例，内容无关）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000052 | H000000001 | ZP | LA01 | 2029/01/01 | 2 |

---

## 3. 分配前校验结论

| 校验层 | 结果 |
|--------|------|
| 第0层（E12）结构完整性 | **命中 E12：表头列不符** |
| 第1~4层（E01~E11） | **不执行**（已中止） |

> 结论：命中 E12 后立即中止整次运行，不进入数据加载、校验、分配流程。

---

## 4. 分配过程

不进入分配（E12 中止）。

---

## 5. 预期结果

1. VBA 报错信息可读且可定位，包含：
   - 工作表名（如 `输入_退单表`）
   - 列号（如第1列）
   - 期望表头（`物流单号`）
   - 实际表头（`物流单号X`）
2. 不生成分配结果相关输出。
3. 运行状态为“E12 中止”。

---

## 6. 补充说明

- 本用例属于“结构级异常”，优先级高于所有业务校验。
- 与字段合法性类用例（E01~E05）不同，E12 一旦命中，后续逻辑全部停止。
