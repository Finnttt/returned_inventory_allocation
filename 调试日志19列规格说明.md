# 调试日志 19 列规格说明（TC-29 / A0）

> **用途**：固定 `预期_调试日志` 与 VBA 输出 `调试日志` 工作表的列结构；代码实现（M13/M14/M09）须与本规格对齐。
>
> **验收标准**：当前 Excel DataSet 的 `预期_调试日志` 以 **简版 + 19 列** 为准。

---

## 1. 三档日志级别

| 级别 | 配置值 | 行为 |
|------|--------|------|
| **关闭** | `关闭` | 不写入任何日志数据行；主表仅保留表头；删除分表 |
| **简版** | `简版` | 每个退单行输出 **1 条最终结果** 记录（`IsFinalResult=True`） |
| **详细** | `详细` | 同 19 列结构；同一退单行可有多条记录（含回溯尝试、撤销中间步骤） |

> **说明**：简版与详细的列结构相同，区别在**行数**——详细档包含过程事件。

---

## 2. 表头（19 列，顺序固定）

| 列号 | 表头名称 | 含义 |
|------|---------|------|
| 1 | 物流单号 | 当前处理的物流单号 |
| 2 | SKU | 当前 SKU 组 |
| 3 | WMS退单号 | 退单号 |
| 4 | 行号 | 退单行号（五位文本） |
| 5 | D | 该行需求量 |
| 6 | 处理序 | 静态排序后的处理顺序（1-based）；未到达行填 `-` |
| 7 | 动态nextMinQty | 分配该行时的 nextMinQty；最后一行可为空或 `-` |
| 8 | 候选QC数 | 动态筛选后可用 QC 类型数 |
| 9 | 被排除QC列表 | 已尝试排除的 QC，逗号分隔；无则空 |
| 10 | 策略 | 策略一/策略二/策略三；失败未尝试填 `-` |
| 11 | 分配QC | 最终选用的 QC；未分配填 `-` |
| 12 | 分配前QC剩余 | 选用 QC 的五元组汇总池剩余量（**当前实现固定为占位符 `-`**，见 §8） |
| 13 | 分配后QC剩余 | 扣减后的剩余量（**当前实现固定为占位符 `-`**，见 §8） |
| 14 | 批号/效期组合数 | 该行成功分配涉及的批号+效期组合数 |
| 15 | 是否回溯重试 | 是/否 |
| 16 | 实际回溯次数 | 该 SKU 组累计回溯次数（简版取最终值） |
| 17 | 行状态 | 批量导入/手工操作/失败 等 |
| 18 | 错误码 | E09/E10 等；成功为空 |
| 19 | 分配失败子类型 | 见 §3 |

---

## 3. 分配失败子类型（第 19 列）枚举

| 取值 | 适用场景 |
|------|---------|
| （空） | 分配成功 |
| 预检测A（初始可用QC=0） | 静态预检测 A 命中 |
| 预检测B（强制竞争库存不足） | 静态预检测 B 命中 |
| 动态分配无可用QC | 分配循环内候选池为空 |
| 回溯路径穷尽 | E09 回溯后路径穷尽 |
| 连带回滚—同SKU未到达行 | 同 SKU 前序行先失败，本行未处理 |
| 连带回滚—跨SKU短路 | 同物流单号其他 SKU 短路 |

---

## 4. 简版输出规则（`BuildDebugLogRows` 过滤）

```
若 cfg.DebugLogLevel = 关闭 → 不写数据行
若 cfg.DebugLogLevel = 简版 → 仅输出 IsFinalResult = True 的事件
若 cfg.DebugLogLevel = 详细 → 输出全部 AllocationEvent
```

---

## 5. 与旧 11 列格式的关系

| 旧列（11列，已废弃） | 新列对应 |
|---------------------|---------|
| 事件类型 | 合并入 策略/错误码/子类型 |
| 使用QC | → 分配QC |
| 使用策略 | → 策略 |
| 回溯序号 | → 实际回溯次数 |
| 是否已撤销 | 详细档过程事件专用 |
| 是否最终结果 | 简版过滤条件 |
| 日志级别 | 不再作为输出行列（配置级属性） |

---

## 6. 嵌入覆盖（TC-29）

以下 DataSet 的规范 TC 文档中含 `预期_调试日志` 验证点：

| DataSet | 规范 TC | 典型场景 |
|---------|---------|---------|
| SF0003 | TC-06/39 | 预检测A，回溯次数=0 |
| SF0016 | TC-21 | 回溯成功（计步 4），多行日志 |
| SF0027 | TC-22 | 回溯计步 59（15 条路径穷尽），E09 |
| SF0028 | TC-24 | 回溯计步 11（> 上限 10），E10 |
| SF0049 | TC-49 | 成功分配简版日志 |

> **回溯计数口径（2026-07-19 统一）**：实现（M09 modBacktracking）按“单行撤销-重试”每回退一行计 1 次；
> 规范 TC 早期按“直达 target 的回溯事件数”叙述（如 SF0027 的 14），两种口径终态一致，数据以实际运行计步为准。

## 8. 第 12/13 列（分配前/后QC剩余）当前实现口径

M09 的事件构造器当前把第 12/13 列统一写为占位符 `-`（简版与详细一致），
2026-07-19 实际运行与 38 行 `预期_调试日志` 冻结数据均以此为准。
如需真实剩余量，须在 M09 事件中补充携带并在本规格删除本说明。

---

## 7. 代码模块职责

| 模块 | 职责 |
|------|------|
| M09 `modBacktracking` | 分配/回溯过程中生成 `AllocationEvent` |
| M15 `modRunner` | 从分配结果收集 events 数组 |
| M13 `modOutputBuilder` | `BuildDebugLogRows` 按级别过滤并映射 19 列 |
| M14 `modExcelOutput` | `WriteDebugLog` 写入 19 列表头与分表 |

**实现状态（截至 2026-07-18）**：上述四模块代码已对齐本规格；单元测试见 `modTestRunner` TC-BT03b、TC-OB07~OB09、TC-EO04/EO05。实际执行结果以 `规则-TC-模块-数据集映射表.md` 的执行记录为准。
