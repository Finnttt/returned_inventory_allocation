# 退货入库分配系统 — 测试用例规范模板

> 本文档分两部分：
> - **第一部分**：填写规范说明（解释每个节的用途与填写要求）
> - **第二部分**：空白模板（直接复制后填写）

---

# 第一部分：填写规范说明

## 整体结构说明

每个测试用例文件包含以下六节，其中第3、4节在校验失败类用例中可简化：

| 节编号 | 节名称 | 必填/可选 | 适用场景 |
|--------|--------|----------|---------|
| 1 | 测试描述 | 必填 | 所有TC |
| 2 | 输入数据 | 必填 | 所有TC |
| 3 | 分配前校验结论 | 必填 | 所有TC（失败类只填命中的层） |
| 4 | 分配过程推导 | 按需 | 校验通过后进入分配的TC；校验失败类直接写"校验未通过，不进入分配" |
| 5 | 预期结果 | 必填 | 所有TC |
| 6 | 补充说明 | 可选 | 有特殊设计说明或与相近TC需区分时填写 |

---

## 第1节：测试描述

### 1.1 描述表格（必填字段说明）

| 字段 | 填写要求 |
|------|---------|
| 物流单号 | 本用例使用的物流单号，如 SF3190000000026 |
| 场景编号 | 与测试场景清单对应，如 TC-10；多场景合并时填多个，如 TC-21/33/34 |
| 场景名称 | 一句话说明本TC的核心验证点，含关键规则关键词 |
| 测试目的 | 用动词开头逐条描述验证目标，如"验证回溯机制有效" |
| 覆盖规则 | 列出需求文档节号，如 §4.2.7 回溯机制、§4.4 整单回滚 |
| 配置：最大回溯次数 | 填实际值，默认200；验证E10时需调小，如10 |
| 配置：调试日志级别 | 关闭 / 简版 / 详细（决定第5.4节是否存在） |
| 预期错误码 | 无（全部成功）/ EXX（如E09、E10、E01+E04） |
| 是否触发整单回滚 | 是 / 否 |

### 1.2 关键触发点表格（必填）

说明本TC中哪些特殊数据构造触发了哪些规则，是推导过程的"索引"。
每行一个触发点，第二列注明对应需求文档节号。

---

## 第2节：输入数据

### 2.1 退单表（必填）

- 标题注明行数，如"退单表（9行）"
- 列顺序：WMS退单号 → SKU → 行号 → 数量 → 物流单号
- 行号须为五位前导零文本格式，如 00001

### 2.2 质检库存表（必填）

- 标题注明行数，如"质检库存表（3行，每行已是唯一五元组）"
- 若有同五元组多行，需在注释中说明汇总结果
- 列顺序：物流单号 → SKU → QC情况 → 批号 → 效期 → 数量

### 2.3 数量校验与预判注释（必填）

在两张表后面用引用块（`>`）写明：
1. **数量校验**：每个SKU的退单合计 vs 库存合计，后跟 ✓ 或 ✗
2. **五元组汇总**（如有同五元组多行）：说明哪几行合并为什么
3. **分配前校验预判**：预期E01~E11全部通过，或预期命中哪个错误码

---

## 第3节：分配前校验结论

用一张四行表格列出四层校验结果。

- 全部通过时，每行填"通过"，结论写"四层全部通过，进入分配阶段"
- 有命中时，命中行填"命中 EXX — 具体说明"，结论写"命中 EXX，该物流单号所有行标记失败，不进入分配阶段"
- **E11 特别说明**：需写明具体触发条件，如"ZP总库存T=3，groupMinQty=5，0<3<5，触发E11"

> **重要**：校验类TC（TC-13~TC-20、TC-36~TC-38）在此节完整填写命中情况后，
> 第4节写"校验未通过，不进入分配阶段"即可，无需展开分配推导。

---

## 第4节：分配过程推导

按**物流单号 → SKU组**分层展开，每个SKU组包含以下四个子节：

### 4.X.1 静态排序（必填）

列出该SKU下每行的排序参数：
- 初始nextMinQty = 同组其他所有行需求的最小值
- 初始可用QC数 = 按初始nextMinQty筛选后可用的QC种类数
- 排序结果 = 最终处理位置（第1个处理、第2个处理...）

### 4.X.2 预检测A/B（按需）

- 不触发预检测时，此子节省略
- 触发时，明确写明是预检测A（初始可用QC=0）还是预检测B（强制竞争库存不足）
- 写出触发条件的具体数值

### 4.X.3 分配过程（必填）

用表格逐行记录：
- 行号、需求量D、当前候选QC（动态筛选后）、nextMinQty、命中策略、选中的库存行、库存变化、行状态

> 表格列较多时可酌情拆分为"筛选列"和"选中列"两张小表

### 4.X.4 回溯过程（按需）

- 不触发回溯时，此子节省略
- 触发时，先描述初始路径失败原因，再描述每次回溯的目标行和改选QC
- 路径较多（如TC-22的15条路径）时，可用枚举表格

---

## 第5节：预期结果

### 5.1 汇总表（必填）

每个WMS退单号一行，无论成功失败都需列出。
- 成功时原因列为空
- 失败时原因列填错误码格式：`E09 - 分配路径穷尽`（直接原因）或 `整单回滚（触发原因：E09）`（连带回滚）

### 5.2 成功分配明细表（必填）

- 整单回滚时，写 **空** — 整单回滚后无成功分配记录，不展开表格
- 成功时，标题注明行数，手工操作时同一行号展开多行（每个批号/效期组合各一行）
- 列顺序：物流单号 → WMS退单号 → SKU → 行号 → 退单数量 → QC情况 → 批号 → 效期 → 分配数量 → 行状态 → 退单号状态

### 5.3 数据异常明细表（按需）

- 仅当校验阶段命中 E01/E02/E03/E04/E05/E07 时填写
- E06/E08/E11/E09/E10 不产生此表，省略此节
- 校验全部通过时，省略此节
- 列顺序：来源表 → 原始行号 → 物流单号 → WMS退单号 → SKU → 异常字段名 → 原始值 → 错误码 → 原因说明

### 5.4 调试日志表（按需）

- 配置"调试日志级别=关闭"时，省略此节
- 使用简版时：每行仅记录最终分配结果
- 回溯类TC需重点确认：是否回溯重试、实际回溯次数、被排除QC列表
- 跨SKU短路从未执行的行，处理序/候选QC数/策略/分配QC填"-"，分配失败子类型填"连带回滚—跨SKU短路"
- 同SKU内从未到达的行（前序行先失败），处理序/候选QC数填"-"，分配失败子类型填"连带回滚—同SKU未到达行"
- 固定使用 19 列，列顺序：物流单号 → SKU → WMS退单号 → 行号 → D → 处理序 → 动态nextMinQty → 候选QC数 → 被排除QC列表 → 策略 → 分配QC → 分配前QC剩余 → 分配后QC剩余 → 批号/效期组合数 → 是否回溯重试 → 实际回溯次数 → 行状态 → 错误码 → 分配失败子类型
- 字段含义和填值约定以 [`调试日志19列规格说明.md`](调试日志19列规格说明.md) 为准，避免不同 TC 各自定义表头

---

## 第6节：补充说明（可选）

建议在以下情况填写：
- 本TC与相近TC的区别（如TC-24与TC-22库存结构相同但max不同）
- 特殊构造数据的设计意图（如为什么选C(6,2)=15条路径）
- 测试执行时的注意事项（如需临时调小回溯次数）
- 验证陷阱提示（如"行00003在所有路径中均未被触达"）

---
---

# 第二部分：空白模板

> **使用方法**：
> 1. 复制"## 空白模板正文"以下的全部内容到新文件
> 2. 按需删除带"▷省略说明"注释的可选节
> 3. 将所有 `<占位符>` 替换为实际内容

---

## 空白模板正文

```
（以下为复制起点）
```

---

# TC-XX \<场景名称（简短，含核心关键词）\>

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | \<如 SF3190000000001\> |
| 场景编号 | TC-XX |
| 场景名称 | \<详细场景名称\> |
| 测试目的 | \<逐条列出，如：① 验证... ② 验证...\> |
| 覆盖规则 | \<如 §4.2.7 回溯机制、§4.4 整单回滚、§5.1 汇总表\> |
| 配置：最大回溯次数 | \<200（默认）/ 调整为 N\> |
| 配置：调试日志级别 | \<关闭 / 简版 / 详细\> |
| 预期错误码 | \<无 / E0X / E0X + E0Y\> |
| 是否触发整单回滚 | \<是 / 否\> |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| \<触发点1\> | \<§X.X\> |
| \<触发点2\> | \<§X.X\> |

---

## 2. 输入数据

### 2.1 退单表（\<N\>行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|-----------|-----|------|------|---------|
| \<TK...\> | \<H...\> | \<00001\> | \<N\> | \<SF...\> |

### 2.2 质检库存表（\<N\>行）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| \<SF...\> | \<H...\> | \<ZP/QC/NG\> | \<LA01\> | \<2029/01/01\> | \<N\> |

> **数量校验**：\<SKU-H1\> 退单 \<N\> = 库存 \<N\> ✓
> **五元组汇总**（如有）：\<说明哪几行合并，如 ZP:LA01:2029 = 8+12 = 20\>
> **分配前校验预判**：\<E01~E11 全部通过 / 预期命中 EXX\>

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01~E05）数据完整性 | 关键字段非空、行号格式、QC合法性、数量为正整数、效期格式 | \<通过 / 命中 EXX — 说明\> |
| 第2层（E06/E07）物流单号一致性 | 退单表与质检库存表物流单号集合比对 | \<通过 / 命中 EXX\> |
| 第3层（E08）数量一致性 | 同物流单号+SKU两表数量合计比对 | \<通过 / 命中 E08\> |
| 第4层（E11）QC碎片可分配性 | 是否存在 0 \< T \< groupMinQty 的QC类型 | \<通过 / 命中 E11 — T=N，groupMinQty=N\> |

> **结论**：\<四层全部通过，进入分配阶段 / 命中 EXX，该物流单号所有行标记失败，不进入分配阶段\>

---

## 4. 分配过程推导

> ▷ 校验失败类TC此节写"校验未通过，不进入分配阶段"后删除以下内容

### 4.1 \<物流单号\> — SKU \<H000000001\>

库存池（\<N\>条五元组）：\<简短描述，如 ZP:LA01:2029:20、QC:LA01:2029:22\>

#### 4.1.1 静态排序

| 处理序 | 行号 | 所属退单号 | D | 初始nextMinQty | 初始可用QC数 | 排序依据 |
|--------|------|-----------|---|---------------|------------|---------|
| 1 | \<00001\> | \<TK...\> | \<N\> | \<N\> | \<N\> | \<可用QC数升序→D降序→退单号→行号\> |

#### 4.1.2 预检测（如触发，否则删除此节）

> **预检测A / 预检测B**：\<描述触发条件及具体数值\>
> 结论：命中 E09（子类型：\<预检测A（初始可用QC=0）/ 预检测B（强制竞争库存不足）\>），该组所有行标记失败，短路跳过同物流单号其余SKU组。

#### 4.1.3 分配过程

| 行号 | D | 候选QC | nextMinQty | 策略 | 选中（QC:批号:效期）| 分配量 | 库存变化 | 行状态 |
|------|---|-------|-----------|------|-------------------|-------|---------|--------|
| \<00001\> | \<N\> | \<{ZP,QC}\> | \<N\> | \<策略一/二/三\> | \<ZP:LA01:2029\> | \<N\> | \<QC: 22→10\> | \<批量导入/手工操作\> |

#### 4.1.4 回溯过程（如触发，否则删除此节）

**初始路径（失败）**：

| 行号 | D | 候选QC | 策略 | 选中 | 库存变化 | 结果 |
|------|---|-------|------|------|---------|------|
| \<...\> | | | | | | \<✓ / ✗ 失败→回溯\> |

**回溯第1次** → 目标行 \<00001\>，撤销至此行，将 \<ZP\> 加入已尝试列表，改选 \<QC\>：

| 行号 | D | 候选QC数 | 策略 | 选中 | 库存变化 |
|------|---|---------|------|------|---------|
| \<...\> | | | | | |

> 实际回溯次数 = \<N\>（\< 上限 \<200\>）

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|-----------|-----------|------|
| \<SF...\> | \<TK...\> | \<批量导入/手工操作/无法分配\> | \<空 / EXX - 原因说明 / 整单回滚（触发原因：EXX）\> |

### 5.2 成功分配明细表（\<N\>行）

> ▷ 整单回滚时替换为：**空** — 整单回滚后无成功分配记录。（删除下方表格）

| 物流单号 | WMS退单号 | SKU | 行号 | 退单数量 | QC情况 | 批号 | 效期 | 分配数量 | 行状态 | 退单号状态 |
|---------|-----------|-----|------|----------|--------|------|------|----------|--------|-----------|
| \<SF...\> | \<TK...\> | \<H...\> | \<00001\> | \<N\> | \<ZP\> | \<LA01\> | \<2029/01/01\> | \<N\> | \<批量导入\> | \<批量导入\> |

### 5.3 数据异常明细表（仅 E01/E02/E03/E04/E05/E07 命中时填写，否则删除此节）

| 来源表 | 原始行号 | 物流单号 | WMS退单号 | SKU | 异常字段名 | 原始值 | 错误码 | 原因说明 |
|--------|---------|---------|-----------|-----|-----------|--------|--------|---------|
| \<退单表/质检库存表\> | \<Excel行号，如 3\> | \<SF...\> | \<TK... / [N/A]\> | \<H...\> | \<行号/效期/数量/QC情况\> | \<原始值\> | \<E0X\> | \<如：效期无法解析为合法日期（月份13不存在）\> |

### 5.4 调试日志表（调试日志级别=关闭时删除此节）

> **注**：调试日志级别=关闭时不生成调试日志表，但运行历史表中的回溯统计字段（总回溯次数、最大单组回溯次数）仍强制记录（见 §6.5.4）。

| SKU | WMS退单号 | 行号 | D | 处理序 | 候选QC数 | 被排除QC列表 | 策略 | 分配QC | 分配前QC剩余 | 分配后QC剩余 | 批号/效期组合数 | 是否回溯重试 | 实际回溯次数 | 行状态 | 错误码 | 分配失败子类型 |
|-----|-----------|------|---|-------|---------|------------|------|--------|------------|------------|--------------|------------|------------|--------|--------|--------------|
| \<H1\> | \<TK...\> | \<00001\> | \<N\> | \<1\> | \<N\> | \<ZP / 空\> | \<策略一\> | \<ZP\> | \<N\> | \<N\> | \<1\> | \<是/否\> | \<N\> | \<批量导入\> | \<空/EXX\> | \<空 / 预检测A（初始可用QC=0）/ 预检测B（强制竞争库存不足）/ 动态分配无可用QC / 回溯路径穷尽 / 连带回滚—同SKU未到达行 / 连带回滚—跨SKU短路\> |

> ▷ 短路从未执行的行（跨SKU短路）：处理序/候选QC数/策略/分配QC/分配前后QC剩余/批号效期组合数 均填"-"
> ▷ 同SKU内因前序失败而未被处理到的行：处理序填"-"，候选QC数填"-"

### 5.5 运行历史记录表（每次运行均需验证）

> **说明**：每次点击"开始分配"（Full Run）或"仅运行校验"（Dry Run）后，运行历史记录表须追加一条新记录。本节校验该记录的关键字段值，以及历史记录表的"只增不减"特性。

| 字段 | 期望值 | 说明 |
|------|--------|------|
| 运行类型 | \<Dry Run / Full Run\> | "仅运行校验"=Dry Run；"开始分配"=Full Run |
| 输入：退单表行数 | \<N\> | 与第2.1节退单表行数一致 |
| 输入：质检库存表行数 | \<N\> | 与第2.2节质检库存表行数一致 |
| 输入：物流单号数 | \<N\> | 两表去重合并后的物流单号总数 |
| 校验失败物流单号数 | \<N\> | 命中E01~E08/E11的物流单号数；无则填0 |
| 分配成功物流单号数 | \<N\> | 整单通过分配的物流单号数；Dry Run 时填0 |
| 分配失败物流单号数 | \<N\> | 进入分配后失败数（含E09/E10/E99）；Dry Run 时填0 |
| 错误码分布 | \<如 E09:1 / E01:2; E04:1 / 空\> | 按错误码升序，未命中不列；全部成功时为空 |
| 总回溯次数 | \<N\> | 所有SKU组回溯次数之和；Dry Run 时填0；无回溯时填0 |
| 最大单组回溯次数 | \<N\> | 单个SKU组最高回溯次数；Dry Run 时填0 |
| 追加验证 | 运行后总行数 = 运行前总行数 + 1 | 确认历史记录只增不减，不覆盖历史 |

> ▷ Dry Run TC（如TC-45）：分配成功/失败物流单号数、回溯次数均填0，运行类型=Dry Run。
> ▷ 成功无错误的TC：错误码分布填空，校验/分配失败数填0。
> ▷ 调试日志级别=关闭时：总回溯次数和最大单组回溯次数仍须有正确值（不为0且正确，见 §6.5.4）。

---

## 6. 补充说明（无特殊说明时删除此节）

- \<与相近TC的区别说明\>
- \<特殊数据构造的设计意图\>
- \<测试执行注意事项\>

---

```
（以上为复制终点）
```

---

# 附录：字段填写速查表

## A. 行状态取值

| 取值 | 含义 | 来源 |
|------|------|------|
| 批量导入 | 该行使用了1种批号/效期组合 | 策略一或策略二成功 |
| 手工操作 | 该行使用了≥2种批号/效期组合 | 策略三成功（必然多种组合） |
| 分配失败 | 该行无法完成分配 | E09/E10/E99触发，或整单回滚 |
| 未处理 | 初始状态，不应出现在输出结果中 | — |

## B. 退单号状态取值

| 取值 | 含义 | 触发条件 |
|------|------|---------|
| 批量导入 | 该退单号下所有行均为"批量导入" | §4.4 第二层 |
| 手工操作 | 该退单号下至少一行为"手工操作"，无失败行 | §4.4 第二层 |
| 无法分配 | 该退单号下存在分配失败行，或整单回滚 | §4.4 第二层 |

## C. 汇总表原因字段格式

| 场景 | 格式 | 示例 |
|------|------|------|
| 直接触发单个错误码 | `EXX - 原因说明` | `E09 - 分配路径穷尽` |
| 直接触发多个错误码 | `EXX - 说明; EYY - 说明`（按错误码升序） | `E01 - 关键字段为空或格式异常; E04 - 数量非法` |
| 连带回滚（未直接触发） | `整单回滚（触发原因：EXX）` | `整单回滚（触发原因：E10）` |
| 成功 | 空 | |

## D. 错误码速查

| 错误码 | 原因说明 | 触发阶段 | 产生数据异常明细表记录？ |
|--------|---------|---------|----------------------|
| E01 | 关键字段为空或格式异常 | 校验（第1层） | ✓ |
| E02 | 退单表行号重复 | 校验（第1层） | ✓（重复的两行均记录） |
| E03 | QC情况非法 | 校验（第1层） | ✓ |
| E04 | 数量非法 | 校验（第1层） | ✓ |
| E05 | 效期格式非法 | 校验（第1层） | ✓ |
| E06 | 物流单号仅存在于退单表 | 校验（第2层） | ✗ |
| E07 | 物流单号仅存在于质检库存表 | 校验（第2层） | ✓（孤立物流单号的唯一记录渠道） |
| E08 | 同物流单号+SKU数量不一致 | 校验（第3层） | ✗ |
| E09 | 分配路径穷尽 | 分配阶段 | ✗ |
| E10 | 回溯超限 | 分配阶段 | ✗ |
| E11 | QC库存碎片无法分配（0 \< T \< groupMinQty） | 校验（第4层） | ✗ |
| E99 | 未知运行时异常 | 任何阶段 | ✗ |

## E. 分配失败子类型（对应调试日志表"分配失败子类型"字段）

> 此字段仅在行分配失败时填写，成功行留空。字段名对应需求文档 §5.3 调试日志表字段定义。

| 子类型 | 适用错误码 | 触发时机 | 对应代码路径 |
|--------|----------|---------|------------|
| 预检测A（初始可用QC=0） | E09 | 静态预检测阶段，未进入分配循环 | §4.2.3 预检测A |
| 预检测B（强制竞争库存不足） | E09 | 静态预检测阶段，未进入分配循环 | §4.2.3 预检测B |
| 动态分配无可用QC | E09 | 分配循环中，当前行候选池为空且无可回溯点 | §4.2.2 + §4.2.7 |
| 回溯路径穷尽 | E09 | 所有选择点的所有QC路径均已尝试失败 | §4.2.7 步骤6 |
| 连带回滚—同SKU未到达行 | E09/E10 | 同SKU内，前序行先失败，本行从未被处理到（如TC-22中行00003） | §4.2.7 + §4.2 短路总原则 |
| 连带回滚—跨SKU短路 | E09/E10 | 同物流单号其他SKU触发E09/E10后短路，本SKU行从未被执行（如TC-24中H2行） | §4.2 短路总原则 |

## F. 调试日志表 — 特殊值填写规范

| 情形 | 处理序 | 候选QC数 | 策略/分配QC | 分配失败子类型 |
|------|--------|---------|-----------|--------------|
| 正常分配行 | 1, 2, 3... | 动态值 | 实际值 | 空 |
| 分配失败行（动态无可用QC） | 正常序号 | 0 | - | 动态分配无可用QC |
| 分配失败行（回溯穷尽） | 正常序号 | 有值 | 最后尝试值 | 回溯路径穷尽 |
| 同SKU内从未到达的行（前序行先失败） | - | - | - | 连带回滚—同SKU未到达行 |
| 跨SKU短路从未执行的行 | - | - | - | 连带回滚—跨SKU短路 |
