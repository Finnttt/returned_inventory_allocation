# TC-18 E06 物流单号仅存在于退单表测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000058 |
| 场景编号 | TC-18 |
| 场景名称 | 退单表有物流单号 SF0058，质检库存表中无对应记录，触发 E06 |
| 测试目的 | ① 验证物流单号仅存在于退单表、库存表中无对应行时触发 E06 ② 验证 E06 触发后，该物流单号下所有 WMS 退单号均标记为"无法分配" ③ 验证不存在于库存表的物流单号不进入分配阶段 |
| 覆盖规则 | §4.1 第三层 E06（R046）— 物流单号仅存在于退单表 |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | E06 |
| 是否触发整单回滚 | 否（E06 属于校验阶段失败，未进入分配，不触发整单回滚） |

> **DataSet 编号说明**：物流单号尾号选 58（SF3190000000058）而非 18，因 SF3190000000018 已被 TC-04/07/08 占用。

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 退单表物流单号 = SF3190000000058，质检库存表中无任何数据行，因此无 SF3190000000058 对应库存记录 | §4.1（R046） |

---

## 2. 输入数据

### 2.1 退单表（1行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|----------|-----|------|------|---------|
| TK10000580 | H000000058 | 00001 | 5 | SF3190000000058 |

### 2.2 质检库存表（空表，仅表头）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
|（无数据行）|  |  |  |  |  |

> **说明**：库存表留空是最简洁的 E06 场景：退单表中存在 SF3190000000058，但库存表没有任何物流单号，因此 SF3190000000058 仅存在于退单表。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01/E02）关键字段完整性 | 行号、WMS退单号、SKU 均合法 | 通过 |
| 第2层（E03~E05）字段值合法性 | 退单表数量合法；库存表无数据行，无 QC/效期字段需要校验 | 通过 |
| 第3层（E06）物流单号跨表比对 | 退单表物流单号集合 ∩ 库存表物流单号集合 | **命中 E06**（SF3190000000058 仅在退单表） |
| 第3层（E07/E08）继续执行 | E07 不命中（库存表无数据行）；E08 因无对应库存记录而不适用 | SF0058：E06 |
| 第4层（E11）QC碎片 | 不适用（E06 已标记失败） | 通过 |

> **结论**：第三层命中 E06（SF3190000000058），整个物流单号下所有行标记为"无法分配"，**不进入分配阶段**。

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|----------|-----------|------|
| SF3190000000058 | TK10000580 | 无法分配 | E06 - 物流单号仅存在于退单表（库存表无对应记录） |

### 5.2 成功分配明细表

**不生成**（E06 导致分配失败，无任何成功分配记录）

### 5.3 数据异常明细表

**不生成**（E06 属于跨表一致性错误，不产生字段级数据异常明细记录；错误仅出现在汇总表中）

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 1 |
| 输入：质检库存表行数 | 0 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 1 |
| 分配成功物流单号数 | 0 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | E06 × 1 |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |

---

## 6. 补充说明

### 6.1 E06 与 E07 的对称关系

| 错误码 | 触发场景 | TC |
|--------|---------|-----|
| **E06** | 物流单号仅在**退单表**（库存表缺失） | **TC-18**（本用例）|
| **E07** | 物流单号仅在**库存表**（退单表缺失） | TC-19 |

两者是对称的跨表一致性检查，R046/R047 对应两个方向。

### 6.2 库存表空行设计说明

本 TC 质检库存表留空（仅有表头），是最简洁的 E06 触发方式。在生产场景中，E06 通常发生于文员只填写了退单表、忘记填写库存表的情况。
