# TC-02 策略二：单行超量匹配成功

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000001 |
| 场景编号 | TC-02 |
| 场景名称 | 策略二：单行超量匹配成功（同一退单号双行，行一命中策略二，行二命中策略一） |
| 测试目的 | ① 验证当质检库存五元组数量大于退单需求量（qty > D）时，策略二命中，并从该五元组中仅扣减需求量，剩余库存保留；② 验证同一退单号多行场景下，处理完行一后剩余库存能正确供后续行使用；③ 验证所有行行状态 = 批量导入、退单号状态 = 批量导入；④ 验证静态排序按"可用QC数升序 → D降序"排序，D大的行优先处理 |
| 覆盖规则 | §4.2.1 静态排序；§4.2.2 可用QC筛选（T≥D+nextMinQty 条件）；§4.2.3 预检测B（T=S，通过）；§4.2.4 策略一、策略二；§4.4.1 行级状态判定；§4.4.2 退单号状态聚合；§5.1 分配状态汇总表；§5.2 成功分配明细表 |
| 配置：最大回溯次数 | 200（默认） |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | 无 |
| 是否触发整单回滚 | 否 |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 质检库存五元组 ZP:LA01:2029/01/01 数量 = 3，大于行 00001 的需求 D = 2（T > D），策略一失败（T ≠ D），策略二命中（T ≥ D） | §4.2.4 策略一、策略二 |
| 行 00001 处理后 ZP 剩余 = 1，恰好等于行 00002 需求 D = 1（最后一行 T = D 条件），策略一再次命中 | §4.2.2 最后一行筛选条件；§4.2.4 策略一 |
| 两行均属同一退单号 TK00000011，初始可用 QC 数均为 1（仅 ZP），排序时以 D 降序为次排序键，D = 2 的行 00001 先处理 | §4.2.1 静态排序：可用QC数升序 → D降序 |
| 预检测B：两行均为ZP强制行，S = 3 = T，满足 T = S 条件，预检测B不触发 | §4.2.3 预检测B |

---

## 2. 输入数据

### 2.1 退单表（2 行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|-----------|-----|------|------|---------|
| TK00000011 | H000000001 | 00001 | 2 | SF3190000000001 |
| TK00000011 | H000000001 | 00002 | 1 | SF3190000000001 |

### 2.2 质检库存表（1 行，已是唯一五元组）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000001 | H000000001 | ZP | LA01 | 2029/01/01 | 3 |

> **数量校验**：SKU H000000001 退单合计 = 2 + 1 = 3，库存合计 = 3 ✓
>
> **五元组汇总**：仅 1 行，无需合并。
>
> **分配前校验预判**：E01～E11 全部通过。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01～E05）数据完整性 | 关键字段非空；行号 `00001`/`00002` 均满足五位前导零格式；QC情况 `ZP` 合法；数量 `2`/`1`/`3` 均为正整数；效期 `2029/01/01` 满足 YYYY/MM/DD 格式且为合法日期 | 通过 |
| 第2层（E06/E07）物流单号一致性 | 退单表与质检库存表均包含 SF3190000000001，两表物流单号集合完全一致 | 通过 |
| 第3层（E08）数量一致性 | SF3190000000001 + H000000001：退单合计 2 + 1 = 3 = 库存合计 3 | 通过 |
| 第4层（E11）QC碎片可分配性 | groupMinQty = min(2, 1) = 1；ZP 总库存 T = 3：0 < 3 < 1？否（T > groupMinQty，不触发 E11）；QC/NG 总库存 T = 0，不触发 E11 | 通过 |

> **结论**：四层全部通过，进入分配阶段。

---

## 4. 分配过程推导

### 4.1 SF3190000000001 — SKU H000000001

库存池（1 条五元组）：ZP:LA01:2029/01/01:3

#### 4.1.1 静态排序

**各行初始 nextMinQty 与可用QC数计算**（静态状态，所有行均未分配）：

| 行号 | D | 初始 nextMinQty | ZP（T=3）筛选 | 初始可用QC数 |
|------|---|----------------|--------------|------------|
| 00001 | 2 | min(其他行需求) = min(1) = 1 | T=D? 3=2? 否；T≥D+nextMinQty? 3≥2+1=3? **是 ✓** | 1（仅 ZP） |
| 00002 | 1 | min(其他行需求) = min(2) = 2 | T=D? 3=1? 否；T≥D+nextMinQty? 3≥1+2=3? **是 ✓** | 1（仅 ZP） |

**排序决策**：两行初始可用 QC 数均 = 1（并列），按 D 降序次排序：D=2（行00001）> D=1（行00002）。

| 处理序 | 行号 | 所属退单号 | D | 初始 nextMinQty | 初始可用QC数 | 排序依据 |
|--------|------|-----------|---|----------------|------------|---------|
| 1（先处理） | 00001 | TK00000011 | 2 | 1 | 1（仅 ZP） | 可用QC数=1（并列）→ D降序：D=2 优先 |
| 2（后处理） | 00002 | TK00000011 | 1 | 2 | 1（仅 ZP） | 可用QC数=1（并列）→ D降序：D=1 靠后 |

#### 4.1.2 预检测

**预检测A**：行 00001 和行 00002 初始可用 QC 数均 = 1 ≠ 0 → **不触发**。

**预检测B**：两行均满足"初始可用QC数=1，QC类型=ZP"，构成强制竞争组。
- 强制行需求集合：{D=2, D=1}，S = 2 + 1 = **3**
- ZP 总库存：T = **3**
- 非强制行（可用QC数>1且可用ZP的行）：无，minQtyOther = 不适用
- 检查：**T = S？3 = 3？是 ✓** → 条件满足，预检测B **不触发**

> 结论：两项预检测均通过，进入主分配循环。

#### 4.1.3 分配过程

| 行号 | D | 候选QC | nextMinQty（动态） | 策略 | 选中（QC:批号:效期） | 分配量 | 库存变化 | 行状态 |
|------|---|-------|-----------------|------|-------------------|-------|---------|--------|
| 00001 | 2 | {ZP} | 1（剩余行00002，D=1） | 策略二 | ZP:LA01:2029/01/01 | 2 | ZP: 3 → 1 | 批量导入 |
| 00002 | 1 | {ZP} | ∅（最后一行） | 策略一 | ZP:LA01:2029/01/01 | 1 | ZP: 1 → 0 | 批量导入 |

**行 00001（D=2）分配推导**：
- 动态 nextMinQty = min(剩余未分配行需求) = min(行00002的D=1) = **1**
- 动态可用 QC 筛选：ZP 当前 T = 3；T = D? 3 = 2? 否；T ≥ D + nextMinQty? 3 ≥ 2 + 1 = 3? **是 ✓** → ZP 进入候选池
- 策略一：在候选池中寻找 qty = D = 2 的五元组 → ZP:LA01:2029/01/01 qty = 3 ≠ 2 → **策略一失败**
- 策略二：在候选池中寻找 qty ≥ D = 2 的五元组 → ZP:LA01:2029/01/01 qty = 3 ≥ 2 ✓ → **策略二命中**
- 扣减 2，ZP 剩余 3 - 2 = **1**；该行使用 1 种批号/效期组合（LA01:2029/01/01）→ 行状态 = **批量导入**

**行 00002（D=1）分配推导**：
- 动态 nextMinQty = ∅（行 00002 为最后一行，其后无未分配行）
- 动态可用 QC 筛选（最后一行条件，T = D）：ZP 当前 T = 1；T = D? 1 = 1? **是 ✓** → ZP 进入候选池
- 策略一：在候选池中寻找 qty = D = 1 的五元组 → ZP:LA01:2029/01/01 qty = 1 = 1 ✓ → **策略一命中**
- 扣减 1，ZP 剩余 1 - 1 = **0**；该行使用 1 种批号/效期组合（LA01:2029/01/01）→ 行状态 = **批量导入**

**退单号状态聚合**：TK00000011 下所有行（00001、00002）均为批量导入，无失败行 → 退单号状态 = **批量导入**

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|-----------|-----------|------|
| SF3190000000001 | TK00000011 | 批量导入 | （空） |

### 5.2 成功分配明细表（2 行）

| 物流单号 | WMS退单号 | SKU | 行号 | 退单数量 | QC情况 | 批号 | 效期 | 分配数量 | 行状态 | 退单号状态 |
|---------|-----------|-----|------|----------|--------|------|------|----------|--------|-----------|
| SF3190000000001 | TK00000011 | H000000001 | 00001 | 2 | ZP | LA01 | 2029/01/01 | 2 | 批量导入 | 批量导入 |
| SF3190000000001 | TK00000011 | H000000001 | 00002 | 1 | ZP | LA01 | 2029/01/01 | 1 | 批量导入 | 批量导入 |

### 5.3 数据异常明细表

不生成（校验全部通过，无 E01～E05/E07 错误）。

### 5.4 调试日志表

不生成（配置"调试日志级别 = 关闭"）。

> 注意：运行历史记录表中的"总回溯次数"和"最大单组回溯次数"字段仍须有正确值（见 §6.5.4），不因关闭日志而省略。

### 5.5 运行历史记录表（每次运行均需验证）

| 字段 | 期望值 | 说明 |
|------|--------|------|
| 运行类型 | Full Run | 点击"开始分配" |
| 输入：退单表行数 | 2 | 与第 2.1 节一致 |
| 输入：质检库存表行数 | 1 | 与第 2.2 节一致 |
| 输入：物流单号数 | 1 | SF3190000000001 |
| 校验失败物流单号数 | 0 | 四层校验全部通过 |
| 分配成功物流单号数 | 1 | SF3190000000001 分配成功 |
| 分配失败物流单号数 | 0 | 无失败 |
| 错误码分布 | （空） | 无任何错误码命中 |
| 总回溯次数 | 0 | 无回溯，两行均直接命中策略（策略二/策略一） |
| 最大单组回溯次数 | 0 | 同上 |
| 追加验证 | 运行后总行数 = 运行前总行数 + 1 | 确认历史记录只增不减，不覆盖历史 |

---

## 6. 补充说明

- **TC-01 与 TC-02 对照**：TC-01 仅有 1 行退单，使用最后一行筛选条件（T = D），策略一命中。TC-02 共 2 行退单，行 00001 使用非最后行筛选条件（T ≥ D + nextMinQty），且 T ≠ D，需要走到策略二；行 00002 为最后一行（T = D），策略一命中。两个用例联合覆盖了 §4.2.2 两种筛选分支。
- **预检测B 边界值说明**：本用例的 ZP 库存 T = 3 = S（强制行需求之和），恰好满足 T = S 这个边界条件，属于预检测B"勉强通过"的典型场景。若要构造预检测B触发的用例，需在保持 E08 通过（退单合计 = 库存合计）的前提下，使 T ≠ S 且 T < S + minQtyOther——例如增加一个非强制行并适当调整各QC库存量，让强制QC的剩余碎片无法被任何非强制行消化。直接缩减库存到 T = 2 会先触发 E08（退单合计 3 ≠ 库存合计 2），校验阶段即失败，不会进入预检测。
- **策略二的关键验证点**：行 00001 处理完后，库存应从 3 扣减为 1（不是 0），验证"策略二仅扣减需求量，剩余库存保留"。若实现错误地清零了整行库存，则行 00002 将找不到可用库存，导致分配失败——此为实现层常见 bug，本用例数据专门设计为可捕捉该错误。
