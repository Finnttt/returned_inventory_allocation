# TC-56 库存账本测试用例（M06 modInventoryLedger）

## 概述

| 项目 | 内容 |
|------|------|
| 模块 | M06 库存账本 modInventoryLedger |
| 测试集 | UT-Ledger |
| 测试入口 | `RunLedgerTests()` |
| 覆盖函数 | BuildLedger / QueryQCTotal / GetFiveTupleRows / Deduct / Undo / TakeSnapshot |

---

## 测试用例清单

### TC-L01：相同五元组多行应合并汇总

**目的**：同一批货可能分多行录入，账本必须累加合并，不能重复计算。

| 字段 | 行1 | 行2 |
|------|-----|-----|
| ShipmentNo | SF001 | SF001 |
| SKU | H001 | H001 |
| QC | ZP | ZP |
| LotNo | BA01 | BA01 |
| Expiry | 2029/01/01 | 2029/01/01 |
| Qty | 3 | 5 |

**期望结果**：
- `QueryQCTotal(ledger, "SF001", "H001", "ZP")` = **8**
- `GetFiveTupleRows(...)` 返回 **1行**，`CurrentQty = 8`，`OriginalQty = 8`

---

### TC-L02：不同批号不合并，独立存储

**目的**：批号不同则五元组不同，应各自独立存储，以便分批分配。

| 字段 | 行1 | 行2 |
|------|-----|-----|
| LotNo | BA01 | BA02 |
| Qty | 3 | 4 |

**期望结果**：
- `QueryQCTotal(...)` = **7**
- `GetFiveTupleRows(...)` 返回 **2行**

---

### TC-L03：非法行（QtyValid/QCValid 为 False）不计入账本

**目的**：M05 已报过错的非法行不应占用库存，避免脏数据进入分配池。

| 行 | 标记 | 期望 |
|----|------|------|
| 行1 | 全部 Valid=True，qty=5 | 计入 |
| 行2 | QtyValid=False，qty=3 | 跳过 |
| 行3 | QCValid=False，qty=4 | 跳过 |

**期望结果**：
- `QueryQCTotal(...)` = **5**
- `GetFiveTupleRows(...)` 返回 **1行**

---

### TC-L04：正常扣减成功

**目的**：可用数量充足时，Deduct 应成功，CurrentQty 减少，OriginalQty 不变。

- 初始 qty = 5，扣减 3
- **期望**：返回 `True`，`CurrentQty = 2`，`OriginalQty = 5`

---

### TC-L05：扣减量超过余量，拒绝扣减，账本不变

**目的**：防止分配超发，这是库存守恒的第一道防线。

- 初始 qty = 3，尝试扣减 5
- **期望**：返回 `False`，`CurrentQty` 仍为 3，`undoLog.Count = 0`

---

### TC-L06：精确扣尽（边界值）

**目的**：当需求量恰好等于余量时，应允许扣减至 0。

- 初始 qty = 5，扣减 5
- **期望**：返回 `True`，`CurrentQty = 0`

---

### TC-L07：Key 不存在时 Deduct 返回 False

**目的**：防止对不存在的五元组误操作，避免账本出现意外数据。

- 账本中有 H007，用 H007_NONEXIST 作为 key 扣减
- **期望**：返回 `False`，`undoLog.Count = 0`

---

### TC-L08：Undo 后账本完全恢复至扣减前状态

**目的**：回溯分配的核心机制——撤销所有扣减，使账本回到分配前状态。

- 两个五元组各扣减一次（BA01 扣 4，BA02 扣 3）
- 调用 Undo
- **期望**：BA01.CurrentQty = 10，BA02.CurrentQty = 8（全部恢复）

---

### TC-L09：Undo 后 undoLog 被清空

**目的**：防止撤销日志被重复使用，避免多次 Undo 造成数量异常增加。

- 扣减 → 调用 Undo
- **期望**：`undoLog.Count = 0`

---

### TC-L10：快照独立性——后续扣减不影响快照

**目的**：M10 守卫需要对比"分配前"和"分配后"的状态，快照必须是独立副本。

- 取快照时 `CurrentQty = 10`
- 扣减 6 件后 `CurrentQty = 4`
- **期望**：`GetSnapshotCurrentQty(snap, key) = 10`（快照不变）

---

### TC-L11：QueryQCTotal 跨批号/效期汇总同一 QC

**目的**：查询某 QC 的总可用量，必须把所有批号/效期加总，才能正确判断是否满足退单需求。

- ZP 下有 BA01(5件) 和 BA02(3件)，QC 下有 BA03(4件)
- **期望**：`QueryQCTotal(..., "ZP") = 8`，`QueryQCTotal(..., "QC") = 4`

---

### TC-L12：GetFiveTupleRows 只返回指定 QC 的行

**目的**：不同 QC 的库存行不能混入同一候选池，否则分配策略会使用错误的库存。

- ZP 有 2行，NG 有 1行，QC 有 0行
- **期望**：
  - `GetFiveTupleRows(..., "ZP")` = 2行
  - `GetFiveTupleRows(..., "NG")` = 1行
  - `GetFiveTupleRows(..., "QC")` = 0行（空数组）

---

## 覆盖矩阵

| 函数 | 覆盖的用例 |
|------|-----------|
| `BuildLedger` | L01, L02, L03 |
| `QueryQCTotal` | L01, L02, L03, L11 |
| `GetFiveTupleRows` | L01, L02, L03, L12 |
| `Deduct` | L04, L05, L06, L07, L08, L09, L10 |
| `Undo` | L08, L09 |
| `TakeSnapshot` | L10 |

---

## 测试运行方式

在 VBA 编辑器中，打开即时窗口（Ctrl+G），输入：

```
RunLedgerTests
```

全部通过时显示 `测试全部通过：12 个`。
