# TC-20 E08 同物流单号+SKU 数量不一致测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000060 |
| 场景编号 | TC-20 |
| 场景名称 | 退单表合计数量（8）≠ 质检库存表合计数量（5），触发 E08 |
| 测试目的 | ① 验证同一物流单号+SKU 组合下，退单表数量合计 ≠ 库存表数量合计时触发 E08 ② 验证 E08 触发后，该物流单号所有 WMS 退单号标记为"无法分配" ③ 验证 E08 不产生数据异常明细表记录（错误为跨表汇总级，无法定位到单行）|
| 覆盖规则 | §4.1 第三层 E08（R048）— 同物流单号+SKU数量不一致 |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | E08 |
| 是否触发整单回滚 | 否（E08 属于校验阶段失败，未进入分配，不触发整单回滚） |

> **DataSet 编号说明**：物流单号尾号选 60（SF3190000000060）而非 20，因 SF3190000000020 已被 TC-12 占用。

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 退单表 SF3190000000060 + H000000060：合计 = 5+3 = 8 | §4.1（R048）|
| 库存表 SF3190000000060 + H000000060：合计 = 5 | §4.1（R048）|
| 退单合计（8） ≠ 库存合计（5） → 触发 E08 | §4.1（R048）|

---

## 2. 输入数据

### 2.1 退单表（2行，数量合计 = 8）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|----------|-----|------|------|---------|
| TK10000600 | H000000060 | 00001 | 5 | SF3190000000060 |
| TK10000601 | H000000060 | 00001 | 3 | SF3190000000060 |

> **说明**：两行退单 SKU 相同，合计数量 = 5+3 = 8。

### 2.2 质检库存表（1行，数量合计 = 5）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000060 | H000000060 | ZP | LA01 | 2029/06/15 | 5 |

> **说明**：库存合计 5 ≠ 退单合计 8，触发 E08。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01/E02）关键字段完整性 | 行号、WMS退单号、SKU 均合法；两退单号各1行，行号均为 `00001`（不同退单号各自独立），连续性通过 | 通过 |
| 第2层（E03~E05）字段值合法性 | QC、数量（均为正整数）、效期均合法 | 通过 |
| 第3层（E06/E07）物流单号集合比对 | 两表均有 SF3190000000060，一致 | 通过 |
| 第3层（E08）数量合计比对 | 退单合计 8 ≠ 库存合计 5 | **命中 E08** |
| 第4层（E11）QC碎片 | 不适用（E08 已标记失败） | 不执行 |

> **结论**：第三层命中 E08，整个物流单号 SF3190000000060 下所有行标记为"无法分配"，**不进入分配阶段**。

---

## 4. 分配过程

**不进行分配**（E08 在校验阶段已标记失败，未进入分配循环）。

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|----------|-----------|------|
| SF3190000000060 | TK10000600 | 无法分配 | E08 - 同物流单号+SKU数量不一致（退单合计=8，库存合计=5） |
| SF3190000000060 | TK10000601 | 无法分配 | E08 - 同物流单号+SKU数量不一致（退单合计=8，库存合计=5） |

### 5.2 成功分配明细表

**不生成**（E08 导致分配失败，无任何成功分配记录）

### 5.3 数据异常明细表

**不生成**（E08 属于跨表汇总级错误，无法定位到单一源数据行，不产生数据异常明细记录；错误仅出现在汇总表中）

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 2 |
| 输入：质检库存表行数 | 1 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 1 |
| 分配成功物流单号数 | 0 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | E08 × 1（物流单号级别） |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |

---

## 6. 补充说明

### 6.1 E08 与退单号关系

E08 是**物流单号+SKU** 级别的错误（比对的是同物流单号下同一 SKU 的合计数量），但汇总表以 WMS 退单号为粒度记录，因此该物流单号下所有退单号均显示 E08 原因。

### 6.2 数量不一致的典型场景

- 文员录入退单数量时笔误（本 TC 模拟此情形：退单填 8，库存实际只有 5）
- 库存数据未及时更新（部分货物已被他单分走）
- 两张表来自不同时间点的导出

所有场景均须人工核实后重新导入，系统不自动修正。
