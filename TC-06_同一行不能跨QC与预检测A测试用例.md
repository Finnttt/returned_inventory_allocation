# TC-06 / TC-39 / TC-29 综合测试用例

## 物流单号

SF3190000000003

## 场景编号

| TC-06 | TC-39 | TC-29 |
|-------|-------|-------|
| 同一行只能从单一QC分配，不能跨QC（§3.2 R032）| 预检测A：排序后、进入分配循环前，初始可用QC数=0 → E09（§4.2.3 R081）| 调试日志开启时生成日志表，情形一：回溯次数=0（§6.7 R124，嵌入覆盖）|

## 测试目的

1. 验证系统不会将多种QC类型的库存合并分配给同一退货行（R032核心约束：同一行只能从单一QC情况分配）
2. 验证预检测A机制：在静态排序后、进入分配循环**前**，能识别"某行初始可用QC数=0"并直接触发E09，不进入分配循环、不触发回溯
3. 验证调试日志开启时正确生成日志表，且在预检测A命中场景下，是否触发回溯=否，实际回溯次数=0

## 覆盖规则

§3.2 同一退货行QC唯一性约束（R032）、§4.2.3 预检测A—初始可用QC数为零（R081）、§6.7 调试日志输出（R124，TC-29 嵌入覆盖）

---

## 一、测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000003 |
| 场景编号 | TC-06 / TC-39 / TC-29 |
| 场景名称 | 同一行不能跨QC；预检测A初始可用QC=0触发E09；调试日志情形一（回溯次数=0） |
| 测试目的 | 见上节 |
| 覆盖规则 | §3.2（R032）、§4.2.3（R081）、§6.7（R124） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志 | 开启 |
| 预期错误码 | E09 |
| 是否触发整单回滚 | **否**（物流单号下仅有一个退单号 TK00000031，且该退单号整体失败，无"已成功被撤回"的记录，不满足整单回滚触发条件） |

### 关键触发点

| 触发点 | 需求节号 | 说明 |
|--------|---------|------|
| 退单2行（D=10/D=1），库存 ZP=5/QC=6 | 数据设计 | groupMinQty=min(10,1)=1；各QC T≥1，E11不触发；ZP/QC单独均无法满足D=10 |
| 行00001：D=10，nextMinQty=1，D+nextMinQty=11；ZP(5)<11且5≠10→不可用；QC(6)<11且6≠10→不可用→初始可用QC数=0 | §4.2.3 | 预检测A命中条件：行00001所有QC均不满足可用条件 |
| 行00002：D=1，nextMinQty=10，D+nextMinQty=11；ZP(5)<11且5≠1→不可用；QC(6)<11且6≠1→不可用→初始可用QC数=0 | §4.2.3 | 预检测A覆盖同组全部行 |
| ZP(5)+QC(6)=11≥D=10，但R032禁止跨QC拼凑 | §3.2 | TC-06核心约束：同一行只能从单一QC情况分配，不允许混合使用 |
| 调试日志中：是否回溯重试=否，实际回溯次数=0 | §6.7 | 预检测A阻断分配循环，回溯机制从未被触发 |

---

## 二、输入数据

### 退单表（2行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|----------|-----|------|------|---------|
| TK00000031 | H000000001 | 00001 | 10 | SF3190000000003 |
| TK00000031 | H000000001 | 00002 | 1 | SF3190000000003 |

### 质检库存表（2行，每行已是唯一五元组）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000003 | H000000001 | ZP | LA01 | 2029/01/01 | 5 |
| SF3190000000003 | H000000001 | QC | LA01 | 2029/01/01 | 6 |

> **数量校验**：退单 H000000001 合计 = 10+1 = 11；库存 H000000001 合计 = 5+6 = 11 ✓
> **五元组汇总**：两行的 QC情况 不同（ZP ≠ QC），五元组唯一，无需合并
> **E11 预检验**：groupMinQty = min(10, 1) = 1；ZP(T=5): 0<5<1? 否；QC(T=6): 0<6<1? 否 → **E11 不触发** ✓
> **分配前校验预判**：E01~E11 全部通过（退单量合法、库存行完整、物流单号在两表一致）

---

## 三、分配前校验结论

| 校验层 | 结果 |
|--------|------|
| 第一层：关键字段完整性（E01~E02） | 通过 |
| 第二层：字段值合法性（E03~E05） | 通过 |
| 第三层：跨表一致性（E06~E08） | 通过 |
| 第四层：库存碎片预检（E11） | 通过（groupMinQty=min(10,1)=1；ZP(5)≥1，QC(6)≥1，均不满足 0<T<groupMinQty） |

**结论**：四层全部通过，进入分配阶段。

---

## 四、分配过程推导

### 初始库存状态

| QC情况 | 批号 | 效期 | 数量（T） |
|--------|------|------|----------|
| ZP | LA01 | 2029/01/01 | 5 |
| QC | LA01 | 2029/01/01 | 6 |

### SF3190000000003 — SKU H000000001

本 SKU 组包含退单 TK00000031 下的 2 行（00001/D=10，00002/D=1）。

#### 4.1 静态排序与 nextMinQty

各行的初始 `nextMinQty` = 组内其他所有行的需求量最小值：

| 行号 | D | 初始 nextMinQty | D + nextMinQty | ZP（T=5）可用？ | QC（T=6）可用？ | 初始可用QC数 |
|------|---|----------------|---------------|----------------|----------------|------------|
| 00001 | 10 | min(D₀₀₀₀₂)=1 | 11 | 5≥11? 否；5=10? 否 → **不可用** | 6≥11? 否；6=10? 否 → **不可用** | **0** |
| 00002 | 1 | min(D₀₀₀₀₁)=10 | 11 | 5≥11? 否；5=1? 否 → **不可用** | 6≥11? 否；6=1? 否 → **不可用** | **0** |

排序规则：初始可用QC数升序（均=0）→ D降序 → 行号升序

| 处理序 | 行号 | D | 初始可用QC数 |
|--------|------|---|------------|
| 1 | 00001 | 10 | 0 |
| 2 | 00002 | 1 | 0 |

#### 4.2 可用QC筛选（静态，预检测A阶段）

对处理序=1 的行（00001，D=10）执行静态可用QC判断：

**可用QC判定条件**：`T ≥ D + nextMinQty` 或 `T = D`

| QC情况 | T | D | nextMinQty | D+nextMinQty | T≥D+nextMinQty? | T=D? | 可用? |
|--------|---|---|-----------|-------------|-----------------|------|-------|
| ZP | 5 | 10 | 1 | 11 | 5≥11？否 | 5=10？否 | **不可用** |
| QC | 6 | 10 | 1 | 11 | 6≥11？否 | 6=10？否 | **不可用** |

**初始可用QC数 = 0 → 预检测A命中（R081）**

#### 4.3 TC-06 验证点说明

即便将 ZP(5) 与 QC(6) 的库存合并，总量 5+6=11 ≥ D=10，理论上"数量够用"。然而根据 **R032**：同一退货行只能从单一QC情况分配，两种QC不能混合使用，不存在"跨QC拼凑"的操作路径。这正是 TC-06 的核心验证点。

#### 4.4 预检测A命中后的处理

预检测A在进入分配主循环**之前**触发，系统直接将该（物流单号+SKU）下所有行标记为 E09，**不进入分配循环，不触发回溯机制**。

| 行 | 结果 | 原因 |
|----|------|------|
| TK00000031-00001 | 无法分配 | E09（预检测A：初始可用QC数=0） |
| TK00000031-00002 | 无法分配 | E09（预检测A：同组连带，初始可用QC数=0） |

**退单号状态聚合**：TK00000031 全部行"无法分配" → 退单号状态 = **无法分配**

---

## 五、预期结果

### 5.1 分配汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|----------|-----------|------|
| SF3190000000003 | TK00000031 | 无法分配 | E09（预检测A：初始可用QC数=0） |

### 5.2 分配明细表

**空**（无成功分配记录）

### 5.3 数据异常明细表

**不生成**（E09 不产生数据异常明细表记录，错误仅出现在汇总表中）

### 5.4 调试日志表（TC-29 嵌入覆盖）

| 物流单号 | SKU | WMS退单号 | 行号 | D | 处理序 | 动态nextMinQty | 候选QC数 | 被排除QC列表 | 策略 | 分配QC | 分配前QC剩余 | 分配后QC剩余 | 批号/效期组合数 | 是否回溯重试 | 实际回溯次数 | 行状态 | 错误码 | 分配失败子类型 |
|---------|-----|----------|------|---|-------|----------------|---------|------------|------|--------|------------|------------|--------------|------------|------------|--------|--------|--------------|
| SF3190000000003 | H000000001 | TK00000031 | 00001 | 10 | 1 | 1 | 0 | ZP、QC（均不满足可用条件）| — | — | — | — | — | **否** | **0** | 无法分配 | E09 | 预检测A（初始可用QC=0） |
| SF3190000000003 | H000000001 | TK00000031 | 00002 | 1 | 2 | 10 | 0 | ZP、QC（均不满足可用条件）| — | — | — | — | — | **否** | **0** | 无法分配 | E09 | 预检测A（同组连带，初始可用QC=0） |

> **TC-29 验证说明**：调试日志已生成，且记录"是否回溯重试=否，实际回溯次数=0"，符合 TC-29 情形一定义（调试日志中回溯次数=0）。预检测A阻断了分配循环，回溯机制从未被触发。

---

## 六、补充说明

### 6.1 TC-06 与 TC-07 的区别

| 对比项 | TC-06（本用例） | TC-07 |
|--------|---------------|-------|
| 规则 | R032：同一行只能使用单一QC，不能跨QC | R031：同物流单号+同退单号+同行号只能匹配一次 |
| 验证点 | 不同QC不能合并分配给同一行 | 同一行号不能被重复分配 |
| 数据特征 | 两种QC均不足，无法独立满足需求 | 同一行号出现在多次分配结果中 |

### 6.2 TC-39 与 TC-23 的区别

| 对比项 | TC-39（本用例，SF0003） | TC-23（SF0027） |
|--------|----------------------|----------------|
| 触发时机 | 静态排序后、进入分配循环**前**（预检测阶段）| 进入分配循环**后**动态筛选阶段 |
| 代码路径 | 预检测A静态拦截 | 动态QC筛选，所有回溯路径均已穷尽（14次回溯）|
| 是否触发回溯 | **否** | **是**（回溯次数=14） |
| E09失败子类型 | 预检测A（初始可用QC=0）| 动态路径穷尽 |

> SF0003 同时作为 TC-23 "从未有可回溯点" 子情形的补充参考，但 TC-23 的主DataSet为SF0027。

### 6.3 参考资料中两处修正说明

**修正1 — 是否触发整单回滚**：参考资料标注"是"有误。整单回滚须满足"同物流单号下部分退单号已成功、部分失败"，本场景仅TK00000031一个退单号且完全失败，无已成功记录，**整单回滚=否**。

**修正2 — 是否经过回溯**：参考资料标注"是（回溯机制被触发了）"有误。本场景归属TC-39（预检测A），分配循环未进入，回溯机制从未被触发，**是否回溯=否**，回溯次数=0。
