# TC-45 R128 Dry Run（干跑）测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 场景编号 | TC-45 |
| 场景名称 | Dry Run：仅执行分配前校验，不进入分配阶段 |
| 测试目的 | ① 验证 `RunValidationOnly` 不写入成功分配明细 ② 校验通过时汇总表为空（干跑不输出“通过但未分配”的单） ③ 校验失败时仍输出汇总表失败行与数据异常明细 ④ 每次干跑运行历史追加 1 行，运行类型 = `Dry Run` |
| 覆盖规则 | §6.7.5 Dry Run（R128） |
| 入口函数 | `RunValidationOnly(wb)` |
| 是否触发整单回滚 | 否（未进入分配） |

> **DataSet 说明**：本 TC **不新建独立 Excel 工作簿**，复用已有 DataSet 分别执行干跑。

---

## 2. 子场景与复用 DataSet

| 子场景 | 复用 DataSet | 校验结果 | 核心验证点 |
|--------|-------------|---------|-----------|
| **TC-45a** | SF0000（TC-01/TC-30） | 四层校验全部通过 | 汇总/明细/异常均为空；运行历史 1 行 `Dry Run` |
| **TC-45b-1** | SF0013（TC-13） | E01 × 2 | 汇总有失败行；**数据异常明细有字段级记录** |
| **TC-45b-2** | SF0036（TC-36） | E11 | 汇总有 E11；**数据异常明细为空**（E11 只进汇总） |

---

## 3. TC-45a：校验通过（SF0000）

### 3.1 操作步骤

1. 打开 `SF0000` 工作簿（或汇总工作簿中仅含 SF0000 数据的子集）。
2. 配置：调试日志=关闭，最大回溯次数=200。
3. 点击 **干跑**（`RunValidationOnly`），**不要**点击 Full Run。

### 3.2 预期结果

| 输出表 | 预期 |
|--------|------|
| 分配状态汇总表 | **空**（仅表头；通过校验的单据不进干跑汇总） |
| 成功分配明细表 | **空** |
| 数据异常明细表 | **空** |
| 调试日志表 | 不生成数据行（配置=关闭） |
| 运行历史记录表 | 追加 **1 行**，运行类型 = `Dry Run` |

### 3.3 运行历史期望字段（最后一行）

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Dry Run |
| 输入：退单表行数 | 1 |
| 输入：质检库存表行数 | 1 |
| 校验失败物流单号数 | 0 |
| 分配成功物流单号数 | 0 |
| 分配失败物流单号数 | 0 |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |

---

## 4. TC-45b-1：校验失败 + 字段级异常（SF0013）

### 4.1 操作步骤

1. 打开 SF0013 工作簿。
2. 执行 **干跑**。

### 4.2 预期结果

| 输出表 | 预期 |
|--------|------|
| 分配状态汇总表 | 有失败行：`SF3190000000013` / `TK10000130` 与 `[N/A]`，原因含 `E01` |
| 成功分配明细表 | **空** |
| 数据异常明细表 | **有记录**（行号格式异常、WMS退单号为空各 1 条） |
| 运行历史记录表 | 追加 1 行 `Dry Run`，校验失败物流单号数 = 1 |

> **要点**：干跑的主要用途是“先查数据有没有错”。E01 类错误必须同时出现在**汇总表**（业务结论）和**异常明细**（定位 Excel 行）。

---

## 5. TC-45b-2：校验失败 + 仅汇总级错误（SF0036）

### 5.1 操作步骤

1. 打开 SF0036 工作簿。
2. 执行 **干跑**。

### 5.2 预期结果

| 输出表 | 预期 |
|--------|------|
| 分配状态汇总表 | 1 行：`无法分配`，原因 = `E11 - QC库存碎片无法分配（0 < T < groupMinQty）` |
| 成功分配明细表 | **空** |
| 数据异常明细表 | **空**（E11 不产生字段级异常明细，符合 R125） |
| 运行历史记录表 | 追加 1 行 `Dry Run` |

---

## 6. 补充说明

- **Dry Run 与 Full Run 的区别**：Dry Run 调用 `BuildSummaryRows(..., dryRunMode=True)`，只保留 `无法分配` 状态的汇总行；Full Run 会输出成功单的汇总与明细。
- **运行历史**：无论 Dry Run 还是 Full Run，**每点击运行 1 次**追加 1 行历史，不是每个物流单号 1 行。
- **TC-44 关系**：TC-44（R126）验证 Full Run 历史追加，嵌入 TC-01/TC-30；本 TC 验证 Dry Run 分支。
