# 退货入库分配系统 - 需求与技术方案（完整版）

## 一、业务背景

2C电商退货流程中，包裹退回仓库后需要：

1. 仓库开箱检查，登记到**质检库存表**（Excel）
2. 客服开单，生成**退单表**（Excel）
3. 将质检库存匹配到退单表，便于文员批量操作，将库存收入仓库系统

---

## 二、数据表结构

### 2.1 退单表（输入）

**字段**：物流单号、WMS退单号、SKU、行号、数量

**特点**：

- 同一物流单号可能包含多个WMS退单号
- 同一WMS退单号可能包含多个SKU
- 同一物流单号下不同退单号可能包含相同SKU
- 行号在同一WMS退单号中不重复
- 同一WMS退单号下同一SKU可能有多行（行号仍不重复）
- **WMS退单号是文员操作的最小单位**
- **WMS退单号全局唯一**：一个WMS退单号只属于一个物流单号，不会在不同物流单号下重复出现；汇总表可直接按WMS退单号聚合，无需组合物流单号作为联合主键
- **行号格式**：行号为五位前导零纯数字字符串，如 `00001`、`00002`；比较时按字符串比较，同一WMS退单号内行号不重复且必须连续（E02校验）；**行号连续性要求**：同一WMS退单号内，行号必须从 `00001` 起、依次递增（`00001`、`00002`、`00003`…），不允许跳号（如 `00001`、`00003` 缺 `00002`）且不允许从非 `00001` 起（如 `00002`、`00003`），否则触发 E02。**读取规则（严格校验，不自动补零）**：VBA读取后取 `CStr(cell.Value)`（不执行自动补零），对读取结果执行以下校验：①恰好为5位；②全部为数字字符（0-9）；两者均满足则视为合法并以此文本参与后续处理，任一不满足则触发 E01（关键字段格式异常）。数据应以文本型 `"00001"` 直接录入；数值型单元格（无论是否配合 `00000` 自定义格式）经 `CStr()` 后结果不满足上述条件，同样触发 E01。

### 2.2 质检库存表（输入）

**字段**：物流单号、SKU、QC情况、批号、效期、数量

**QC情况类型**：

- ZP：良品
- QC：瑕疵
- NG：不可售卖

**特点**：

- QC情况、批号、效期不影响匹配优先级（不需要先进先出）
- 同一物流单号+SKU可能有多行（不同QC/批号/效期）
- 同一物流单号+SKU+相同QC+相同批号+相同效期也可能有多行（分配时按该组合汇总数量作为可分配池）

**效期字段输入规范**：

- **允许格式**：`YYYY/MM/DD` 或 `YYYY-MM-DD`（如 `2029/01/01`、`2029-01-01`），校验阶段强制检查
- **不允许为空**：所有商品（含无保质期商品）在库存系统中均须填写效期值，效期为空视为数据异常，校验不通过
- **无保质期商品的哨兵值**：所有无保质期商品（如刷子、化妆包等品类，**整类商品恒无保质期**）统一使用配置项 `无保质期哨兵值`（默认 `2099/01/01`，可配置；可选值如 `9999/12/31`）。该哨兵值**仅作为系统占位，不代表真实到期日**。
- **业务前提与一致性校验**：哨兵值仅适用于"整品类无保质期"的商品。若某 SKU 同时存在真实效期（如 `2029/12/31`）和哨兵值（如 `2099/01/01`）两种记录，属于源数据录入错误，**本系统不在算法层做此类语义校验**——其前提是需要业务方维护一份可靠的 `SKU-批号-效期对照表` 作为标准答案。如业务方建立了对照表，可在分配前校验阶段（§4.1）引入「效期匹配对照表」的扩展校验，将异常反馈给业务方修正后再次运行分配（与 §6.7 的"仅运行校验"干跑模式天然契合）。当前版本暂不实现此扩展校验。
- **非法日期**：无法解析为合法日期的值（如 `2029/13/01`、`abc`）视为格式非法，校验不通过

**效期的 VBA 读取注意事项**：

- Excel 会将符合日期格式的文本自动识别为日期类型，单元格内部存储为数值序列号（Date Serial），而非字符串。VBA 使用 `CStr()` 读取此类单元格时，得到的是本机默认日期格式的字符串（如 `2029/1/1`，月日无前导零），而非原始录入字符串。
- **推荐读取方式**：对效期单元格，使用 `VarType(cell.Value)` 判断单元格内部存储类型（**不使用 `IsDate()`**：`IsDate("2029/01/01")` 对文本字符串也返回 True，会让文本效期被误判为日期型，跳过字符串级格式校验，重新引入区域设置风险；`VarType` 返回的是单元格实际存储类型，能精确区分 Excel 日期序列号与手工录入文本），按以下三条分支处理：
  - **`VarType = vbDate（7）`**（Excel 已将单元格值存储为日期序列号）：使用 `CDate(cell.Value)` 转为 VBA Date 变量，再用 `Format(d, "yyyy/mm/dd")` 统一转为 `YYYY/MM/DD` 字符串；无需字符串级校验
  - **`VarType = vbString（8）`**（文本型单元格）：直接读取字符串，**先按 `YYYY/MM/DD` 和 `YYYY-MM-DD` 两种格式做字符串级校验**（按分隔符拆分后逐段检查年月日范围），校验通过后再调用 `CDate()` 构建 Date 变量；不直接依赖 `CDate()` 解析原始文本（防止本机区域设置导致静默解析错误）；校验不通过则触发 E05
  - **`VarType = vbEmpty（0）` 或其他类型**：视为空值或非法类型，触发 E01
  - **字符串级校验范围细则**：年须为正整数（建议 1900～2999 范围）；月须为 01～12；日须满足对应月份的最大天数——1/3/5/7/8/10/12 月最多 31 天，4/6/9/11 月最多 30 天，2 月须先判断闰年：年份能被 4 整除且不能被 100 整除，或能被 400 整除，则为闰年（最多 29 天），否则最多 28 天；不满足上述任一规则则触发 E05
- **输出时统一为** `YYYY/MM/DD` 格式（含前导零，如 `2029/01/01`），避免月日随本机设置变化
- **日期比较时使用 Date 类型**，不使用字符串比较（避免因格式差异导致比较错误）

### 2.3 数据规模

- 每次处理：约500个物流单号
- 退单表：约1,500行（平均每物流单号3行，最多5行）
- 质检库存表：约2,500行（平均每物流单号5行）

---

## 三、核心约束规则

### 3.1 QC一致性约束（最重要）

**规则**：同物流单号 + 同WMS退单号 + 同一行号 → 只能匹配**一种**QC情况

**允许的情况**：

- 同物流单号下不同行可以有不同QC情况
- 同物流单号同退单号下不同行可以有不同QC情况

**不允许的情况**：

- 同一行号匹配到多种QC情况

### 3.2 分配约束

- 同一行只能从**单一QC情况**中分配（不能跨QC情况）
- 分配过程中库存动态扣减必须准确

### 3.3 批量导入 vs 手工操作

**批量导入**（成功）：

- 同一行号不能匹配多个批号或效期
- 文员可直接批量导入系统

**手工操作**（成功）：

- 同一行号可以匹配多个批号或效期
- 文员需要手动分行操作，分别录入不同批号/效期

**无法分配**（失败）：

- 分配失败的情况，需要记录原因

> **注**：完整的状态判定机制（含行级状态、整单回滚、退单号状态聚合、连带回滚原因格式）已挪到 §4.4，作为分配流程的最终输出环节统一描述。

---

## 四、分配流程

> **全流程概览**（各步骤的详细规则见后续小节）：
>
> 1. **加载数据** → 读取退单表和质检库存表到内存，按五元组汇总库存
> 2. **分配前校验**（4.1 节）→ 检查数据完整性和一致性，不通过的物流单号直接标记失败
> 3. **静态排序**（§4.2.1）→ 按分配难度对退货行排序（难的先处理）
> 4. **逐行分配**（§4.2.4）→ 每行依次尝试策略一（精确匹配）→ 策略二（最接近匹配）→ 策略三（多行拼凑）
> 5. **回溯补救**（§4.2.7）→ 当前行无法分配时，回退到前序行换一条 QC 路径重试
> 6. **整单回滚**（§4.4 第二层）→ 某物流单号下存在任何失败行时，该物流单号的全部分配结果撤回
> 7. **状态聚合**（§4.4）→ 根据行状态聚合退单号状态
> 8. **分配后校验**（4.3 节）→ 验证输出数据与输入数据一致
> 9. **输出结果**（第五节）→ 生成汇总表、明细表、调试日志表（可选）

### 4.0 数据标准化（读取后、校验前统一执行）

> 本节将散落在各章节的数据读取与规范化规则集中汇总，**所有字段在进入校验逻辑（§4.1）之前必须完成标准化**，确保后续校验、分配、比较、输出使用一致的内部表示。

| 字段 | 标准化规则 | 详细说明来源 |
|------|-----------|-------------|
| **所有字符串字段** | 读取后执行 `Trim`，去除首尾空白字符 | 普遍适用，防止隐藏空格导致匹配失败 |
| **行号** | 取 `CStr(cell.Value)`（不自动补零）；若结果不满足"恰好5位且全为数字字符"则触发 E01（数值型单元格经 `CStr()` 后结果同样受此校验约束；唯一合法输入为文本型五位前导零字符串，如 `"00001"`） | §2.1 行号读取规则 |
| **批号** | 按文本读取，保留前导零；执行 `Trim`；**统一转为大写**（`UCase`）后参与比较与输出（默认不区分大小写，可由配置项 `批号比较模式` 切换为敏感模式，详见 §6.7） | §4.2.5 批号读取规范 |
| **效期** | 用 `VarType(cell.Value)` 判断（vbDate=7 为 Excel 日期序列号；vbString=8 为文本型；vbEmpty=0 或其他为空/非法）；vbDate 时直接 `Format(CDate(cell.Value), "yyyy/mm/dd")`；vbString 时**先按 `YYYY/MM/DD` 和 `YYYY-MM-DD` 两种格式做字符串级校验**（拆分后逐段检查年月日范围，含闰年及各月天数上限，规则见 §2.2），校验通过后再调用 `CDate()` 构建 Date 变量；不直接用 `CDate()` 解析原始文本（防止本机区域设置导致静默解析错误）；vbEmpty 或其他触发 E01；字符串级校验不通过时输出校验标记，由 §4.1 触发 E05；统一内部格式为 `YYYY/MM/DD`（含前导零） | §2.2 效期读取注意事项 |
| **QC情况** | 读取后依次执行：① `Trim` 去除首尾空格（与"所有字符串字段"规则合并）；② `UCase()` 统一转为大写（容错手工录入的小写，如 `zp` → `ZP`）；③ 校验是否为 `ZP`/`QC`/`NG` 之一（E03）。**三步顺序不可颠倒**——若先校验再 Trim，含前导空格的值（如 ` zp`）会误触发 E03；若先 UCase 再 Trim，结果相同但不直观，建议始终保持 Trim → UCase → 校验的顺序 | §4.1 E03 |
| **数量** | 读取后转为整数；非正整数（含负数、零、小数、非数字）触发 E04；空值触发 E01 | §4.1 E04 |
| **物流单号 / WMS退单号 / SKU** | 读取后 Trim；为空触发 E01 | §4.1 E01 |

> **标准化与校验的分工**：标准化层负责"格式归一"（如补零、Trim、格式转换）并输出规范化值及校验所需的中间标记（如效期字符串级校验是否通过）；最终合法性判断（E01~E05 等错误码落地）统一在 §4.1 校验层完成。两层职责分开，实现更清晰——表格中标注的"触发 E××"均指"产生相应校验标记"，错误码实际由 §4.1 统一输出，实现时不应将错误码记录逻辑散落在标准化阶段。

### 4.1 分配前校验

> 校验按以下顺序执行，同一物流单号可能命中多个错误码，记录**所有**命中的错误码（便于文员一次性修正所有问题）。校验未通过的物流单号下所有行标记为"无法分配"。

**错误码定义**：

| 错误码 | 原因说明 | 校验内容 |
|--------|---------|---------|
| E12 | 结构异常（装载前体检） | 两种情况：①**表头列不符**：输入工作表第一行列名与系统预设不一致（含列顺序错位、列名拼写错误、列缺失），意味着所有字段读取均将错位，继续校验无意义；②**WMS退单号跨物流单号重复**：同一WMS退单号出现在两个不同物流单号下（违反 §2.1 声明的全局唯一约束，将导致汇总表按退单号聚合时静默错乱）。命中任一情形，**立即中止整次运行**，不继续后续校验和分配 |
| E01 | 关键字段为空或格式异常 | 退单表的物流单号、WMS退单号、SKU、行号、数量，质检库存表的物流单号、SKU、QC情况、批号、效期、数量，任一为空即触发；退单表行号读取后归一化为字符串，若不满足"恰好5位且全部为数字字符"则触发 E01（详见 §2.1 行号读取规则） |
| E02 | 退单表行号重复或不连续 | 同一WMS退单号下：①存在重复行号；②行号不从 `00001` 起（如首行为 `00002`）；③行号有跳号（如 `00001`、`00003`，缺 `00002`）。校验方式：对该退单号内所有格式合法的行号排序后，检查是否构成从 `00001` 起的完整连续整数序列；格式非法行（已标记E01）不参与此连续性检查 |
| E03 | QC情况非法 | 质检库存表中QC情况不是 ZP、QC、NG 之一 |
| E04 | 数量非法 | 退单表或质检库存表中数量不是正整数（包括非数字值、负数、零、小数等） |
| E05 | 效期格式非法 | 质检库存表中效期无法解析为合法日期（允许格式见 2.2 节） |
| E06 | 物流单号仅存在于退单表 | 退单表中的物流单号在质检库存表中找不到 |
| E07 | 物流单号仅存在于质检库存表 | 质检库存表中的物流单号在退单表中找不到 |
| E08 | 同物流单号+SKU数量不一致 | 同物流单号+同SKU的数量合计在两张表中不相等 |
| E11 | QC库存碎片无法分配 | 同物流单号+SKU下，某QC类型总库存 T 满足 **0 < T < groupMinQty**（有正库存但小于该组最小行需求；`groupMinQty` = 该(物流单号+SKU)组所有退货行需求量的最小值），该QC库存碎片必然无法被任何单行完整消化（每行需求 D ≥ groupMinQty > T；策略三也在单一QC内执行，无法跨QC凑量）；T=0 的QC类型不触发E11 |

**校验顺序**（第0层命中则立即中止；第1~4层均会执行，不因前层发现错误而跳过后层）：

0. **结构完整性校验**（E12）：在数据加载**之前**执行（对应 §6.3.1.1 步骤 1a），属于"装载前体检"。任一命中则**立即中止整次运行**，不进入后续任何校验或分配逻辑——表头错位会导致所有字段读取均对应错误列，在错误数据上继续运行无任何意义；WMS退单号跨物流单号重复则汇总逻辑的根基不成立，同样不可继续。
   - **表头校验**（E12-①）：读取两个输入工作表的第一行，检查列名与系统预设是否完全一致（包括列顺序与列名拼写）
   - **WMS退单号全局唯一校验**（E12-②）：在退单表逐行加载过程中同步检查，若同一WMS退单号出现在两个不同物流单号下，立即中止（将 §2.1 中的业务声明落地为可执行校验）
   - **E12 命中后的输出行为**：由于结构异常发生在数据加载阶段，系统无法构建有效的物流单号/WMS退单号列表，**不生成分配状态汇总表、不生成任何分配结果表**；仅执行以下两项：① 弹出 VBA 错误提示框（说明具体原因，如"退单表第3列列名应为SKU，实际为sku"）；② 向运行历史记录表追加一条记录（运行类型="Full Run"或"Dry Run"，校验失败物流单号数填 `[E12-中止]`，其余字段填 0 或 `[N/A]`）
1. **数据完整性校验**（E01~E05）：逐行检查两张表的字段合法性
2. **物流单号一致性校验**（E06、E07）：比对两张表的物流单号集合
3. **数量一致性校验**（E08）：按物流单号+SKU汇总比对数量
4. **QC库存可分配性校验**（E11）：同物流单号+SKU下，若某QC类型总库存 T 满足 **0 < T < groupMinQty**（有正库存但小于该组最小行需求），则标记E11；T=0 的QC类型不参与此校验

> **E11 的检测范围说明**：E11 是"必然失败"的**充分条件检测，而非完备检测**——命中 E11 必然无法分配，但未命中 E11 不代表一定能分配成功。部分必然失败场景（如多行之间因 nextMinQty 约束产生的库存竞争碎片：某QC总库存 T ≥ groupMinQty（不触发E11），但各行均因 T < D + nextMinQty 且 T ≠ D 而无法使用）无法在静态校验阶段识别，由分配阶段的预检测或 E09 兜底。
>
> **设计意图**：E11 不追求完备覆盖所有不可分配场景。其目的是以 O(n) 的低成本在校验阶段提前拦截**最显而易见的一类必败场景**（碎片库存），减少这类数据进入分配循环后产生大量无效回溯。完备性由「E11（碎片拦截）+ 预检测A/B（强制竞争拦截）+ 动态 QC 筛选 + 三级策略 + 回溯 + E09 兜底」整体闭环保证，每一层各司其职，不在 E11 单层追求完备。

> **注意**：第 3 层依赖第 1 层的数据合法性（例如数量字段必须是正整数才能汇总比对）。若某物流单号在第 1 层已命中 E04（数量非法），则该物流单号的第 3 层 E08 校验跳过（因无法可靠汇总），但第 2 层仍正常执行。第 4 层同时依赖第 1 层（数量必须合法，才能计算 groupMinQty）和第 3 层（总量必须一致，才能确认库存无法被消化是矛盾的），若某物流单号已命中 E04 或 E08，则该物流单号的第 4 层 E11 校验跳过。

> **校验输出说明**：校验阶段发现的错误分两处记录：（1）**分配状态汇总表**（§5.1）：以物流单号/WMS退单号为单位记录错误码和原因说明，供文员总览；（2）**数据异常明细表**（§5.4）：针对可定位到源数据行或字段的错误（E01~E07，以及 E02 的重复/不连续相关退单行），记录 Excel 原始行号、字段名和原始值，供文员直接定位并修正源数据。E08、E11 不产生数据异常明细表记录，仅出现在汇总表中。

### 4.2 分配中逻辑

> **短路总原则**：同一物流单号内，一旦任一 (物流单号+SKU) 组触发 E09/E10/E99 或静态预检测命中，立即停止该物流单号下**所有剩余 SKU 组**的分配，未处理的 SKU 组均按"连带回滚"输出（格式见 §4.4 和 §5.1）。后文 §4.2.3 和 §4.2.7 中出现的短路规则均为此总原则的落地，不再重复说明。

#### 4.2.1 退货行处理顺序（静态排序，分配前一次性计算）

同物流单号+同SKU下的退货行，按**预计分配难度降序**排列（越难越先处理）：

1. **主排序：可用QC情况数量 升序**（越少越难，越优先）
2. **次排序：Qty 降序**（同等难度下，大数量优先）
3. **末排序：WMS退单号 升序 → 行号 升序**（确定性兜底，两级排序：先按退单号字符串比较升序，退单号相同时再按行号字符串升序；五位前导零文本的字符串升序与数值升序结果等价，此处统一用字符串描述，实现时不得做数值转换）

其中"可用QC情况数量"的**静态计算方法**：复用下文"（二）可用QC情况筛选"的判定规则，但 nextMinQty 取**静态初始值**——即"同(物流单号+SKU)组内除当前行外其他所有行的需求最小值"（此时所有行均未分配，所有库存均处于初始状态）。计算结果记为各行的"初始可用QC数量"。排序在分配开始前一次性完成，分配过程中不重新排序（与动态筛选互补：静态值用于排序与预检测，动态值用于实际分配时的可用 QC 判定）。

> **设计取舍说明**：
> 本系统采用"静态排序 + 动态筛选"的组合设计，而非每次分配后动态重排，原因如下：
> 1. **稳定性**：静态排序保证相同输入产生相同处理顺序，便于测试复现和问题排查
> 2. **可解释性**：每行的处理顺序在分配前即可确定，便于人工核查和调试
> 3. **实现简单性**：动态重排需要在每次分配后重新计算所有未分配行的优先级，实现复杂度和性能开销显著增加
> 4. **不追求全局最优**：全局最优分配是 NP-hard 问题，本系统通过"贪心策略 + 回溯补救"追求足够好的结果，而非理论最优
> 5. **动态 nextMinQty 已提供足够的自适应能力**：虽然排序是静态的，但每行分配前的 QC 筛选是动态的（见下文），能根据当前库存状态实时过滤不可行的路径
>
> **关于"行顺序是否影响分配成败"的澄清**：静态排序的核心价值是**减少首次贪心的失误次数、提升回溯效率**，而非"保证某种顺序才能成功"。在 E08（数量守恒）成立、回溯次数充足（200 次为经验默认值，可配置；极端情况下单 SKU 组路径数可能超过 200，超出上限触发 E10 代表预算耗尽，不代表数据必然无解）、算法实现正确这三个前提下，**理论上行顺序不影响分配的最终成败**——可行解的存在性由数据本身决定，回溯机制可在足够预算内修正任何顺序导致的局部错误。换言之，静态排序追求的是"以更少的回溯次数到达正确结果"。

#### 4.2.2 可用QC情况筛选（动态，每行分配前重新计算）

对于每个待分配行（需求量 D），在分配前动态筛选该行的可用QC类型：

1. **动态获取 nextMinQty**：nextMinQty = 该(物流单号+SKU)下，**当前行分配后**剩余的未分配行中数量的最小值（不含当前行本身；若当前行为最后一行则无剩余行，nextMinQty 记作 ∅）
2. **逐QC类型判断**：获取该QC类型的当前总剩余库存 T，根据是否为最后一行分两种情况：
   - **若当前行为最后一行**（nextMinQty = ∅）：仅当 **T = D** 时可用
     > 由 E08 守恒性（同物流单号+SKU总量两表相等）与前序行精确扣减（每行恰好扣减 D）共同保证：处理到最后一行时，被选中QC的剩余库存 T 必然等于 D，此条件在正常流程中必然成立。
     >
     > **注意**：上述保证成立的前提是"前序各行均已在同一 QC 路径下精确扣减 D"。若最后一行动态筛选后无任何 QC 满足 T=D（例如回溯改变了中间行的 QC 选择，导致 QC 路径不一致），则该行无可用 QC，需触发回溯或标记分配失败——**不可假设最后一行总有可用 QC**。
   - **若当前行之后仍有未分配行**（nextMinQty 存在）：满足以下任一条件即为"可用"：
     - **T = D**（精确匹配，分配后剩余=0，无碎片）
     - **T >= D + nextMinQty**（分配后剩余 ≥ nextMinQty，碎片能被后续至少一行消化）
3. **构建候选池**：筛选后的可用QC类型的所有库存行（按五元组汇总后），共同构成**统一候选池**
4. 策略一、二、三均在此候选池中操作，不先按QC类型分组

#### 4.2.3 静态排序后快速失败预检测（静态，分配前一次性执行）

完成静态排序和初始可用QC数量计算后，在进入主分配循环前，对每个(物流单号+SKU)组执行以下两项预检测。任一命中则直接标记该组所有行为"分配失败"（错误码 E09），**同时立即停止该物流单号下所有剩余 SKU 组的分配**（同物流单号其余 SKU 组按"连带回滚"处理，格式见 §4.4 和 §5.1），不进入分配循环，不触发回溯。

**预检测A：某行初始可用QC数量 = 0**

若静态排序后，某行的初始可用QC数量 = 0，则该(物流单号+SKU)组必然无法分配。

> **原因**：静态排序按可用QC数量升序排列，可用QC=0的行必定排在最前面（最先处理）。第一个被处理时，库存处于完整初始状态，可用QC仍为0，分配立即失败；且该行是第一个被处理的行，前面没有可回溯的前序行，整组必然失败，无法通过回溯解决。

> **与 E11 的关系**：E11 在校验阶段已拦截部分"初始可用QC=0"的成因（当某QC的总库存 T 满足 0 < T < groupMinQty 时，该QC对组内所有行均不满足筛选条件，导致该行可用QC数减少）。预检测A 处理 E11 **未覆盖**的情形：例如，某QC的总库存 T ≥ groupMinQty（不触发E11），但对需求量较大的特定行，因 nextMinQty 约束导致 T < D + nextMinQty 且 T ≠ D，该QC对该行仍不可用，最终该行初始可用QC=0。两者互补，共同在进入分配循环前拦截必然失败的组。

**预检测B：多行强制竞争同一QC，库存不足**

若静态排序后，存在多个行的初始可用QC数量 = 1 且 QC类型相同（即这些行"只能"使用同一种QC，形成强制竞争），则检查该QC的总库存 T 是否能覆盖这些行的总需求：

- 设强制竞争该QC的行的需求集合为 {D1, D2, …}，S = sum(D1, D2, …)
- 设"也可以使用该QC、但可用QC数 > 1"的其他行（非强制行）中的最小需求为 minQtyOther（若不存在非强制行，则该项不适用）

满足以下任一条件，可继续进入分配循环：
- **T = S**（精确匹配，无剩余库存碎片）
- **T ≥ S + minQtyOther**（剩余碎片 T − S 可被非强制行消化）

若上述条件均不满足（含"该QC下无非强制行且 T ≠ S"的情况），则该(物流单号+SKU)组必然无法分配，错误码 E09。

> **预检测B 的保守性**：预检测B 基于静态初始状态判断，属于保守检测——命中则必然无法分配，但**不会误杀实际可分配的场景**。原因：强制行无论如何必须使用该QC，其总需求 S 是刚性约束；若分配后剩余 T-S 小于 minQtyOther，该碎片在动态分配过程中同样无法被消化（非强制行选用其他QC时此碎片无人消化，若非强制行改用该QC则其原本的QC又留下新碎片，总量无法守恒）。因此预检测B 的判断结论在动态分配中仍然成立，不存在静态判定失败而动态实际可行的情形。

> **预检测A 与 预检测B 的关系**：两项检测独立执行，命中任意一项即标记失败。预检测A 针对单行初始可用QC为零的情形（可能由多种 QC 类型均不满足 nextMinQty 筛选条件共同造成，不限于某种 QC 的 T=0 情形）；预检测B 针对多行强制竞争同一 QC、总库存不足的情形；两者检测逻辑不同，不存在包含关系。

**示例（新增3场景）**：同物流单号+SKU，退单表需求：1，2，5，6；质检库存：ZP-7，QC-4，NG-3。

| 行需求 | nextMinQty | ZP（T=7） | QC（T=4） | NG（T=3） | 初始可用QC数 |
|--------|-----------|-----------|-----------|-----------|------------|
| D=6 | min(1,2,5)=1 | 7≥6+1=7 ✓ | 4<6+1 ✗ | 3<6+1 ✗ | 1（仅ZP） |
| D=5 | min(1,2,6)=1 | 7≥5+1=6 ✓ | 4<5+1 ✗ | 3<5+1 ✗ | 1（仅ZP） |
| D=2 | min(1,5,6)=1 | 7≥2+1=3 ✓ | 4≥2+1=3 ✓ | 3≥2+1=3 ✓ | 3 |
| D=1 | min(2,5,6)=2 | 7≥1+2=3 ✓ | 4≥1+2=3 ✓ | 3≥1+2=3 ✓ | 3 |

> 注：静态排序阶段，各行初始 nextMinQty = 全组其他行的需求最小值（所有行均未分配）。以 D=1 为例：nextMinQty = min(2,5,6) = 2，筛选条件为 T=D 或 T≥D+nextMinQty=1+2=3；以 D=6 为例：nextMinQty = min(1,2,5) = 1，筛选条件为 T=D 或 T≥D+nextMinQty=6+1=7。本例各行结果与旧定义一致（因最小行 D=1 被排除后全组最小值不变），仅最小行本身的 nextMinQty 与旧定义不同（旧：1，新：2）。

预检测B触发：D=5 和 D=6 均仅可用ZP（可用QC=1，QC类型相同）。S = 5+6 = 11，ZP-T = 7。
- 非强制行（D=1，D=2也可用ZP），minQtyOther = min(1, 2) = 1
- T=S? 7=11? 否。T≥S+minQtyOther? 7≥11+1=12? 否
- → 预检测B命中，整组无法分配，错误码 E09。

#### 4.2.4 分配策略（三级策略，依次尝试）

对于每个待分配行，在统一候选池中依次尝试以下三种策略。任一策略成功即停止，不再尝试后续策略。

**策略一：精确匹配（单行 qty = 需求量）**

在候选池中寻找一个五元汇总行（物流单号+SKU+QC+批号+效期），其汇总数量恰好等于当前行的需求数量。

- 匹配成功 → 直接分配，行状态 = "批量导入"
- 多个匹配时的优先级：**ZP > QC > NG** → **效期更晚优先** → **批号更小优先**
- 无匹配 → 进入策略二

**策略二：最接近匹配（单行 qty >= 需求量，取最接近的）**

在候选池中寻找 qty >= 需求量 的五元汇总行，选择数量与需求量差值最小的。

- 匹配成功 → 从该行扣减需求数量，行状态 = "批量导入"
- 多个同样接近时的优先级：**ZP > QC > NG** → **效期更晚优先** → **批号更小优先**
- 不需要额外的 minQty 约束（QC可用性已在前置筛选中保证）
- 无满足条件的行 → 进入策略三

> （注：进入策略二时，策略一已处理所有 qty = D 的情况；因此策略二实际处理的均为 qty > D 的行，即单行库存数量超出需求，仅扣减所需数量，该行剩余库存保留在候选池中。）

**策略三：多行连续扣减（单一QC内，逐步最接近剩余需求量）**

> **重要说明（阅读步骤前请先确认以下三点）**：
> - **进入条件**：策略一、二均失败，即候选池中不存在任何单行 qty ≥ D，所有行 qty < D。
> - **允许部分扣减**：策略三允许从某条库存行中进行部分扣减——当选中行的剩余数量大于当前剩余需求时，仅扣减当前剩余需求量，不要求整行全部参与分配。
> - **结果必然为手工操作**：由于质检库存在进入分配前已按"物流单号 + SKU + QC情况 + 批号 + 效期"五元组完成汇总，候选池中每行均代表唯一的批号/效期组合。策略三进入时所有行 qty < D，第一步必然无法单行完成，至少需要两行；策略三在单一QC内执行，多行之间差异只能来自批号或效期，因此策略三成功时必然跨批号/效期，行状态固定为"手工操作"。

当没有单行能满足需求时，在锁定的QC内连续扣减直到需求被满足：

1. **第一步（确定QC类型）**：
   - 在候选池（所有可用QC类型）中，找与需求量差值 |行qty - 需求qty| 最小的行
   - 由于策略二已排除所有 qty ≥ 需求量的行，此时所有行 qty < 需求量，因此"最接近"等同于"数量最大"
   - **第一步平局处理**：若多行差值相同（即数量相同），优先 **ZP > QC > NG** → **效期更晚优先** → **批号更小优先**
   - 取选中行的QC类型，后续步骤锁定在此QC类型内

2. **后续步骤（在锁定QC内迭代）**：
   - 每步从锁定QC的剩余行中，找与当前剩余需求量差值 |行qty - 剩余qty| 最小的行
   - **后续步骤平局处理**：若多行差值相同，优先选 **qty ≥ 剩余需求量** 的行（一步即可完成）；若都 ≥ 或都 <，则 **效期更晚优先** → **批号更小优先**
   - 若选中行 qty ≥ 当前剩余需求量，则仅扣减剩余需求量（部分扣减），该行剩余库存保留在候选池中；若 qty < 当前剩余需求量，则整行全部扣减
   - 扣减后更新剩余需求量，继续迭代
   - 直到剩余需求量 = 0

3. **行状态判定**：分配完成后，统计该行实际使用的"批号+效期"组合种类数：
   - 种类数 = 1 → 行状态 = "批量导入"
   - 种类数 ≥ 2 → 行状态 = "手工操作"
   - 在当前五元汇总模型下，策略三必然使用 ≥ 2 个不同五元组（推导见上方重要说明），因此行状态必然为"手工操作"。**状态由实际结果决定，策略名称仅作推论依据，不直接决定状态。**

> **策略三的理论必然成功性**：在以下两个前提同时满足时，策略三理论上**必然能凑够需求量 D**：
> 1. 被选中QC类型的总剩余库存 T ≥ D（由 nextMinQty 筛选条件已保证此点）
> 2. 允许最后一步部分扣减（已明确支持）
>
> 推导：策略三每步从锁定QC内取最接近当前剩余需求的行，整行扣减直到最后一步；最后一步的剩余需求 ≤ 当前选中行的数量时执行部分扣减，剩余需求归零。由于总库存 T ≥ D，每轮扣减后剩余需求单调递减且不为负，过程必然在有限步内终止于0。
>
> 因此，策略三失败只会发生在以下情形：（a）候选池为空（无可用QC）、（b）实现逻辑有误、（c）可用QC筛选逻辑出错导致 T < D 的QC进入候选池。**开发和测试阶段如果策略三报告失败，应优先检查 nextMinQty 筛选是否正确，而非策略三本身的迭代逻辑。**

**示例**：需求量 = 12，可用库存如下：

| QC | 批号 | 效期 | 汇总数量 |
|----|------|------|----------|
| ZP | L | 2029/01/01 | 9 |
| ZP | Q | 2029/01/01 | 7 |
| ZP | W | 2029/01/01 | 3 |
| ZP | W | 2029/02/01 | 3 |
| QC | L | 2029/01/01 | 9 |
| QC | Q | 2029/01/01 | 4 |
| QC | W | 2029/01/01 | 2 |

分配过程：
1. 策略一：无 qty=12 的行 → 失败
2. 策略二：无 qty>=12 的行 → 失败
3. 策略三：
   - 第一步：最接近12（所有行<12，取最大）= 9，ZP和QC各有一个9，按QC优先选ZP → 选ZP:L-2029/01/01-9，锁定ZP，剩余=3
   - 第二步：ZP内最接近3 → W-2029/01/01-3 和 W-2029/02/01-3 都精确匹配，按效期更晚选 W-2029/02/01-3，剩余=0
4. 结果：ZP:L-2029/01/01-9 + ZP:W-2029/02/01-3，行状态="手工操作"

#### 4.2.5 效期与批号优先级定义

**效期更晚 = 保质期更长 = 更优先**。当 QC 相同时，优先选择效期日期更晚的库存行。**此规则不影响分配成败、也不影响"批量导入/手工操作"的行状态判定**（两者由总量守恒与策略命中情况决定，与平局时选哪个具体批次无关）；其唯一作用是**确定性兜底**——保证相同输入始终产生相同输出，便于测试复现和跨次比对。如业务后续有明确偏好（如改为临期先出），可直接调换为"效期更早优先"，不影响算法正确性。

**批号更小 = 字符串比较值更小 = 确定性兜底**。当 QC 和效期均相同时，优先选择批号字符串比较值更小的库存行（即按字典序升序，取最小值）。此规则的核心目的是保证排序结果完全确定，业务上可理解为同效期时优先使用较早批次的库存。

**批号读取与比较规范**（VBA实现须遵守）：

- **前导零保留**：批号须按文本类型读取，Excel 单元格格式为文本时保留原始值（如 `00123` 不得自动转为 `123`）；若单元格为数值格式导致前导零丢失，视为数据录入问题，需提醒文员将该列格式设为文本后重新录入
- **首尾空格裁剪**：读取后对批号执行 `Trim`，去除首尾空白字符，再参与比较和索引
- **大小写处理**（默认：不区分大小写）：批号读取并 `Trim` 后，**统一通过 `UCase()` 转为大写**，再参与五元组聚合、匹配、排序和输出。即 `a01`、`A01`、`a01 `（含空格）经标准化后均视为相同批号 `A01`。此策略可降低文员录入大小写不一致导致库存"分裂"的风险。
- **可配置切换**：通过配置项 `批号比较模式`（默认 `不敏感`，可切换为 `敏感`）控制；切换为 `敏感` 时使用 VBA 二进制比较（Binary Compare），保留批号原始大小写。详见 §6.7。
- **比较字段**：匹配时按标准化后（Trim + 视配置 UCase）的批号比较；**输出时输出标准化后的批号**（默认大写）。

#### 4.2.6 后续最小需求量（nextMinQty）定义与用途

- **定义**：nextMinQty(物流单号, SKU, 当前行) = 该物流单号+该SKU下，**当前行分配后**剩余未分配行的需求数量最小值（不含当前行）。若当前行为最后一行，则 nextMinQty = ∅（不存在）。
- **动态性**：随分配进度变化——每当一行完成分配，剩余行集合缩小，nextMinQty 可能随之增大
- **与旧定义的区别**：旧定义将当前行本身也纳入计算（"所有未分配行的最小值"），当当前行恰好是需求最小的行时，旧定义 = 当前行需求 D，导致可用QC筛选条件变为 T ≥ 2D，可能错误拒绝合理的QC路径；新定义排除当前行，确保判断的是"分配后剩余库存是否能被后续行消化"
- **用途**：
  - 用于 §4.2.2 中动态筛选可用QC情况（非最后行：T = D 或 T ≥ D + nextMinQty；最后行：T = D）
  - 静态排序阶段（§4.2.1）计算每行初始可用QC数量时，每行的 nextMinQty = 同组其他所有行的需求最小值（此时所有行均未分配）

#### 4.2.7 回溯机制

**选择点定义**：每一行开始分配时，通过 nextMinQty 筛选出的可用QC情况集合即为该行的"分配选择点"。若可用QC情况 >= 2 种，该行具有回溯价值（有其他QC方案可尝试）。

**触发条件**：当前行经 nextMinQty 筛选后无可用QC（候选池为空），或所有可用QC均已依次尝试三级策略后仍无法完成时

**回溯流程**：

1. 当前行分配失败 → 在**选择栈**中找到最近一个"仍有未尝试QC"的历史分配行（记为 target）
2. **撤销 target 行及其之后所有已分配行的结果**，恢复对应占用的库存（不仅仅撤销 target 一行，target 到当前失败行之间的中间行也一并撤销）
3. 将 target 行上次使用的QC类型在其**自身的已尝试列表**中标记为已用
4. **清除 target 行之后所有行的已尝试QC记录**（`attemptedQCsByRow` 中行下标 > target 行下标的所有条目）。原因：target 行换了QC路径，前序库存状态已改变；后续行在旧状态下记录的"已失败QC"在新状态下未必仍然失败，若不清除则会把旧路径的失败历史带入新状态，错误地排除仍可行的QC路径，导致漏解——即算法报告 E09（路径穷尽）但实际存在可行分配方案。
5. 将分配指针重置回 target 行，**从 target 行重新开始**，依次向后逐行重新分配（策略一 → 策略二 → 策略三）
6. 若 target 行所有可用QC都已尝试仍失败 → 继续往前找更早的 target 行
7. 最多回溯 N 次（每个SKU组独立计算），N 为可配置参数，默认值 200，可在配置表中修改（见 §6.7）；超过则标记该 (物流单号+SKU) 组所有行为"分配失败"，错误码 E10，**同时立即停止该物流单号下所有剩余 SKU 组的分配**（不再进入后续 SKU 的分配循环），其余未尝试 SKU 组按"连带回滚"处理（格式见 §4.4 和 §5.1）。此规则同样适用于分配循环中动态产生的 E09（所有 QC 路径穷尽）和 E99（运行时异常）——三者均属算法层或运行时失败，文员无法通过修改源数据解决，继续处理同物流单号其他 SKU 组无额外诊断价值。
8. 每个(物流单号+SKU)组的实际回溯次数须记录到调试日志表（见 5.3 节），用于评估默认值是否合理

> **关键区别**：回溯目标是让 target 行换一条 QC 路径，然后**重新依次处理 target 之后的所有行**（包括之前失败的那行）。不能只撤销 target 一行而继续在原失败行重试，否则中间行的库存占用依然存在，导致库存状态不一致。

> **说明**：回溯机制的主要作用不是修复单行内的组合失败（策略三在库存总量足够的前提下极少失败），而是修复"前序行选择了某QC后，导致后续行无可用QC"的全局路径问题。触发回溯意味着当前路径在全局上走不通，需要退回到最近一个有其他QC可选的行，换一条路重试。
>
> **默认值依据**：回溯机制的唯一分支变量是"每行选哪种 QC"（QC 锁定后，批号/效期/策略均确定性决定）。对含 N 行、每行最多 3 种可用 QC 的 SKU 组，完整搜索空间上界为 3^N 条路径，完整遍历所需的最大回溯步数约为 3^(N+1)/2：
>
> | N（组内行数） | 路径上界 3^N | 完整遍历最大回溯步数 | 默认值 200 是否完备 |
> |---|---|---|---|
> | 1 | 3 | 0（单行无前序可回溯） | 完备 |
> | 2 | 9 | ≤ 12 | 完备 |
> | 3 | 27 | ≤ 39 | 完备 |
> | 4 | 81 | ≤ 120 | 完备 |
> | 5 | 243 | ≤ 363 | **不完备**（预算上限，非完备保证） |
>
> 本项目真实数据以 toC 单件退货为主，同(物流单号+SKU)组内行数几乎均为 1~2，N ≥ 5 的组占比 < 1%。因此：（1）**N ≤ 4 时，默认值 200 可保证完整搜索，有解必能找到，E10 不会误杀有解的单**；（2）**N ≥ 5 时，200 是预算上限而非完备保证——触发 E10 不代表数据必然无解，但此场景在实际数据中极罕见**。可根据调试日志表中的实际回溯次数分布调整此参数。

### 4.3 分配后校验

**必须检查项**：

1. **数量一致性**：输出数据与输入数据数量是否一致
2. **QC一致性**：检查是否满足QC一致性约束
3. **数据完整性**：所有字段数据是否一致
4. **退单号状态一致性**：退单号状态是否与其下所有行的行状态聚合结果一致（规则见 §4.4 第二层）

**失败处理**：任一检查项不通过，**立即抛出 E99，停止本次运行，不输出成功分配明细表**，并在 E99 描述中记录失败的检查项、物流单号、SKU、期望值与实际值，供开发人员定位 bug。

> **设计意图**：分配后校验是正确性的最后防线。走到这一步仍失败，意味着系统内部一致性被破坏——数据层面的问题已在 §4.1 拦截，此处失败只能是代码 bug，文员修改源数据也无法解决。因此动作是"中止+报警"，而非"标记某单失败后继续输出"。
>
> **与 §6.5 工程守卫的关系**：§6.5 是分配**过程中**的实时守卫，在每次回溯撤销后、每个 SKU 组结束时逐步校验局部一致性；§4.3 是分配**全部完成后**的总账复核，对所有输出结果做整体验算。两者同属 E99 体系、互补不替代：§6.5 在过程中捕捉局部不一致，§4.3 在写入输出前做最终兜底。

### 4.4 分配状态判定机制（开发依据）

> 本节是状态判定的完整规则，供开发直接参考。状态由实际分配结果决定，与走了哪条策略无直接关系。本节内容原位于第三章核心约束规则末尾，因属于"分配流程的最终输出环节"，挪到第四章末尾统一描述。

#### 4.4.1 第一层：行级状态判定

某一退货行分配完成后，检查该行实际使用的"批号+效期"组合种类数：

| 实际使用的批号/效期组合种类数 | 行状态 | 典型来源 |
|---|---|---|
| 恰好 1 种 | 批量导入 | 策略一或策略二成功（单行库存覆盖全部需求） |
| 2 种及以上 | 手工操作 | 策略三成功（多行库存拼凑覆盖需求） |

**在当前五元汇总数据模型下，策略与行状态的对应关系是可证明的推论（不是定义）：**
- 策略一/二：必然只用 1 行库存 → 1 个五元组 → 1 种批号/效期 → 批量导入
- 策略三：必然用 ≥ 2 行库存（所有行 qty < D，单行无法完成）→ ≥ 2 个五元组（每行代表唯一五元组）→ QC 已锁定，差异只来自批号/效期 → ≥ 2 种批号/效期 → 手工操作

#### 4.4.2 第二层：物流单号级整单回滚与退单号状态聚合

分配按物流单号逐个处理。某物流单号下所有 SKU 组分配完毕后，执行以下判定：

```
该物流单号下是否存在任何分配失败的行？
        │
        ├── 是 ──→ 【整单回滚】
        │          1. 该物流单号下所有已成功的分配结果一并撤回（不输出到成功分配明细表）
        │          2. 该物流单号下所有 WMS 退单号 → 退单号状态 = "无法分配"
        │          3. 原因字段按以下规则赋值（适用于 E08、E09、E10、E11、E99）：
        │             • 直接原因：该 WMS退单号下存在至少一个 SKU 直接触发了错误码
        │               → 原因格式：`{错误码} - {原因说明}`（如 `E10 - 回溯超限`）
        │             • 连带回滚：该 WMS退单号下所有 SKU 均未直接触发错误码，
        │               仅因同物流单号其他 SKU 失败而被整单回滚
        │               → 原因格式：`整单回滚（触发原因：{错误码}）`
        │               （如 `整单回滚（触发原因：E10）`）
        │             • 同一 WMS退单号若自身同时命中多个错误码，按升序（E01→E99）排列、同码去重后用分号合并
        │               （如 `E09 - 分配路径穷尽; E10 - 回溯超限`）
        │
        └── 否 ──→ 逐个检查每个 WMS 退单号下所有行的行状态：
                    │
                    ├── 任意一行为"手工操作" ──→ 退单号状态 = "手工操作"
                    │
                    └── 全部行均为"批量导入" ──→ 退单号状态 = "批量导入"
```

> **设计说明**：采用整单回滚（而非保留部分成功结果），是因为同一物流单号下的不同退单号可能共享同一 SKU 的库存池。保留部分成功结果可能导致文员误以为可以直接操作，实际上库存关系已不完整。统一回滚后由文员手工处理整个物流单号，更安全可控。

> **业务前提声明与设计取舍**：
>
> - **前提**：同物流单号下，不同 WMS 退单号可能共享同一 SKU 的库存池（库存按"物流单号+SKU"维度独立，跨退单号），且业务上不接受"半成功"状态进入 WMS 系统。
> - **设计选择**：本系统采用「**物流单号粒度整单回滚 + 短路总原则**」作为唯一回滚策略，不提供配置开关。
> - **量化评估**：当前业务数据规模下（单物流单号平均 1~3 行，绝大多数仅 1 个 WMS 退单号，极个别 ≥ 3 个），"连累成功退单号"的概率极低，边际代价可接受。
> - **未实现"按 WMS 退单号粒度回滚"的考虑**：理论上可通过「取消短路 + 事后退单号聚合回滚」实现更细粒度，但当前数据规模下边际收益接近于零，且会显著增加状态管理与回归测试成本，故采用 YAGNI（You Ain't Gonna Need It）原则不实现。如未来数据规模变化（如单物流单号常规出现 5+ 个退单号），可重新评估。

---

## 五、输出结果

### 5.1 分配状态汇总表

**字段**：物流单号、WMS退单号、退单号状态、原因

（文员操作的最小单位是WMS退单号，因此汇总表按退单号列出，便于文员按单操作与核对。）

**退单号状态**：

- 批量导入（成功）
- 手工操作（成功）
- 无法分配（失败）

**原因说明**（针对无法分配，使用错误码标识）：

| 错误码 | 原因说明 | 触发阶段 |
|--------|---------|---------|
| E01 | 关键字段为空或格式异常 | 校验阶段 |
| E02 | 退单表行号重复或不连续 | 校验阶段 |
| E03 | QC情况非法 | 校验阶段 |
| E04 | 数量非法 | 校验阶段 |
| E05 | 效期格式非法 | 校验阶段 |
| E06 | 物流单号仅存在于退单表 | 校验阶段 |
| E07 | 物流单号仅存在于质检库存表 | 校验阶段 |
| E08 | 同物流单号+SKU数量不一致 | 校验阶段 |
| E09 | 分配路径穷尽（所有可用QC组合均已尝试仍无法完成分配，或静态预检测发现路径必然不通） | 分配阶段 |
| E10 | 回溯超限（超过配置的上限次数） | 分配阶段 |
| E11 | QC库存碎片无法分配（某QC类型总库存 T 满足 0 < T < groupMinQty，存在正库存但小于该组最小行需求（groupMinQty）的碎片，必然无法被任何单行完整消化；为充分条件检测，多行竞争导致的碎片问题由 E09 兜底） | 校验阶段 |
| E12 | 结构异常（表头列不符或WMS退单号跨物流单号重复）；命中立即中止整次运行 | 装载前体检（§4.1 第0层） |
| E99 | 未知异常（运行时错误，含分配后校验失败） | 任何阶段 |

> 错误码的详细校验逻辑见 4.1 节。汇总表中"原因"字段格式分两种情况：
> - **直接原因**（该 WMS退单号下存在 SKU 直接触发错误码）：格式为 `错误码 - 原因说明`，多个错误码**按升序排列（E01 → E99），同码去重**，用分号分隔（如 `E01 - 关键字段为空或格式异常; E04 - 数量非法`）；固定升序保证相同问题的两次运行输出一致，便于自动化比对与统计
> - **连带回滚**（该 WMS退单号下所有 SKU 均未触发错误码，仅因同物流单号其他 SKU 失败整单回滚）：格式为 `整单回滚（触发原因：{错误码}）`（如 `整单回滚（触发原因：E10）`）
>
> 此区分适用于 E08、E09、E10、E11、E99——这些错误码以 (物流单号+SKU) 为粒度触发，但会波及同物流单号下与失败 SKU 无关的其他 WMS退单号。E01～E07 均为行级或物流单号级错误，可直接归因于命中的 WMS退单号，无需区分。
>
> **E12 的特殊说明**：E12 是全局结构错误，在数据加载阶段即中止整次运行，系统无法构建物流单号/WMS退单号列表，因此**不生成分配状态汇总表，也不适用直接原因/连带回滚归因框架**。E12 命中后的唯一输出是 VBA 提示框 + 运行历史记录表一条记录，详见 §4.1 第0层。

**E07 的特殊处理**：E07（物流单号仅存在于质检库存表）属于孤立物流单号，退单表中没有对应行，因此系统无法获取 WMS退单号。针对此类物流单号，汇总表中增加一条专用记录：

| 字段 | 填写内容 |
|------|---------|
| 物流单号 | 该孤立物流单号的实际值 |
| WMS退单号 | `[N/A]` |
| 退单号状态 | 无法分配 |
| 原因 | `E07 - 物流单号仅存在于质检库存表` |

文员收到此记录后，可通知仓库核查该物流单号是否为多录或漏建退单。

> **占位符约定**：本系统所有输出表统一使用标准英文占位符 `[N/A]`（Not Applicable）表示"该字段本不适用或源数据缺失"。使用英文占位符而非中文（如 `（无对应退单）`）的原因有三：①避免 VBA 写入 Excel 时因编码/字体导致的显示异常风险；②`[N/A]` 是国际通用占位符，文员一眼可识别"此处非真实数据"；③单一规则便于自动化处理（如二次筛选、外部脚本读取）。各输出表的表头注释需说明此约定。

**用途**：

- 文员按WMS退单号快速查看退单号状态
- 反向验证所有物流单号/退单号都已处理
- 无法分配时，错误码便于文员自行判断原因，或反馈给客服/仓库定位问题

### 5.2 成功分配明细表

> **整单回滚规则**：仅当某物流单号下**所有 SKU 均分配成功**时，该物流单号的分配结果才会出现在本表中。若某物流单号下存在任一行分配失败，则该物流单号的所有分配结果均不输出（详见 §4.4 第二层整单回滚规则）。

**字段**：

| 字段 | 来源 | 说明 |
|------|------|------|
| 物流单号 | 退单表 | |
| WMS退单号 | 退单表 | |
| SKU | 退单表 | |
| 行号 | 退单表 | |
| 退单数量 | 退单表 | 该退货行的原始需求总量，手工操作时每条明细行该值相同 |
| QC情况 | 质检库存 | |
| 批号 | 质检库存 | |
| 效期 | 质检库存 | |
| **分配数量** | 分配结果 | **该批号/效期组合实际分配给本行的数量**；批量导入时等于退单数量；手工操作时多条明细行的分配数量之和等于退单数量 |
| 行状态 | 分配结果 | 批量导入 / 手工操作 |
| **退单号状态** | 分配结果 | **该行所属 WMS退单号的整体状态**：批量导入 / 手工操作（由该退单号下所有行的行状态聚合而来，规则见 §4.4 第二层；同一退单号的所有明细行该字段值相同）；文员可直接在本表按此字段筛选操作范围，无需对照汇总表 |

> **手工操作时的多行展示规则**：当某退货行经策略三拆分到多个批号/效期组合时，成功分配明细表中该行将展开为**多条记录**（每条对应一个批号/效期组合），每条记录的"退单数量"相同、"分配数量"各不相同，所有分配数量之和等于退单数量。文员需按每条记录单独录入 WMS 系统。

**列去重说明**：输出列中不重复定义同义字段（例如物流单号、SKU 只保留一列）；**不得对分配明细行进行去重**——每一条实际分配记录均必须保留，即使多条记录的物流单号、WMS退单号、SKU、行号相同。

> **手工操作行无唯一键说明**：手工操作场景下，同一退货行展开为多条记录（对应不同批号/效期），物流单号 + WMS退单号 + SKU + 行号的**四字段组合在本表中不唯一**。若文员需对本表执行 VLOOKUP 或数据透视等操作，应使用**物流单号 + WMS退单号 + SKU + 行号 + 批号 + 效期**的六字段组合作为唯一标识；批量导入行（每行仅对应一条记录）使用四字段或六字段均可。

**用途**：

- 文员直接按"退单号状态"字段筛选，快速分出批量导入和手工操作两类工作，无需在汇总表和明细表间来回核对
- 文员核查 QC 一致性是否满足
- 核查退单号状态聚合是否正确（规则见 §4.4 第二层）
- 验证输出数据与输入数据是否完全一致
- 手工操作场景下，文员依据分配数量逐条录入 WMS 系统

### 5.3 调试日志表（可选输出）

> 本表用于开发调试和运行调优，**不面向文员日常使用**。建议在界面上提供"输出调试信息"选项，勾选后才生成此表。

**记录规则**（由 §6.7 的"调试日志级别"配置项控制）：

- **关闭（默认）**：不生成调试日志表；需要排查问题时，可在配置表中临时改为简版或详细。
- **简版**：每个退货行仅记录**最终分配结果一条**，被回溯撤销的中间尝试不单独记录（回溯过程通过"被排除QC列表"和"是否为回溯重试"两个字段追溯）。所有行的字段完全一致，无回溯的行相关字段填空或填 0 / 否。
- **详细**：与简版使用相同的固定 19 列结构，但额外输出回溯尝试、撤销等过程事件；简版只保留每行最终结果。详细模式下行数量大幅增加，仅用于复杂回溯问题的专项排查，不建议日常使用。字段名称、顺序和枚举值以 `调试日志19列规格说明.md` 为准，不再增加“尝试序号、路径ID、是否被撤销、是否为最终结果”等额外输出列。

**详细模式行数估算与自动分表**：

- **关于"回溯次数"的口径澄清**：一次"回溯"= 一次"撤销 target 行及其之后所有行 + 重新尝试"事件。**一次回溯可能涉及多行的撤销和重新分配，因此"每行的尝试次数"通常 ≠ "回溯次数"**——同一行可能因多次回溯而被反复重新尝试，其尝试次数累计后可能多于回溯次数。
- **行数估算**（按本项目数据规模，以回溯预算上限为基准的**极端保守估算**）：触发回溯的组按 10% 估算（约 75 组），每组按回溯预算上限 200 次保守估算（依据 §4.2.7 的 3^N 分析，N≤4 时完整搜索实际最多约 120 步，远低于 200；N=1~2 时几乎不回溯；200 次在真实数据中几乎不会被打满），每次回溯涉及约 3 行 × 每行最多尝试 3 种 QC ≈ 1,800 条/组；其余 90% 无回溯组每组约 5 条。总量约 75 × 1,800 + 675 × 5 ≈ **14 万条**（实际因回溯次数远低于 200 而显著少于此上界）。远低于 Excel 单工作表上限（104 万行），但 VBA 批量写入 Excel 性能瓶颈明显，单表行数过大时写入时间显著增加。
- **自动分表机制**：详细模式下，调试日志按配置项 `详细日志单表上限`（默认 100,000 行）自动分表，超出阈值时新建工作表 `调试日志_2`、`调试日志_3` 顺延编号；每张表均保留完整表头，便于独立筛选。配置项可在 §6.7 中调整。

**字段**（按阅读逻辑分五组排列）：

| 组 | 字段 | 说明 |
|----|------|------|
| **① 身份识别** | 物流单号 | 当前处理的物流单号 |
| | SKU | 当前处理的 SKU |
| | WMS退单号 | 当前退货行所属退单号 |
| | 行号 | 当前退货行的行号 |
| **② 处理上下文** | SKU组内处理顺序 | 该行在静态排序后的处理序号（从1开始）；用于验证排序算法是否按预期执行（难的是否先处理） |
| | 需求数量 | 当前行的需求量 D |
| | 候选QC种类数 | 当前行分配时经 nextMinQty 筛选后可用的 QC 类型数量（动态，每行分配前重新计算） |
| | 动态 nextMinQty | 当前行分配前动态计算的 nextMinQty 值（即当前行之后所有未分配行的最小需求数量 D）；最后一行无剩余未分配行时记为 ∅（或 0）；用于验证系统是否使用了动态值而非固定 groupMinQty |
| | 被排除QC列表 | 该行在当前路径中已被回溯机制排除（尝试失败）的 QC 类型，用逗号分隔；首次分配或无回溯时为空 |
| **③ 分配决策** | 使用策略 | 实际命中的策略（策略一/策略二/策略三/失败） |
| | 分配QC | 实际分配使用的 QC 类型；失败时为空 |
| | 分配前该QC剩余总量 | 本次分配执行前，所选 QC 类型在该(物流单号+SKU)下的库存剩余总量；用于验证动态筛选是否正确 |
| | 分配后该QC剩余总量 | 本次分配执行后，同一 QC 类型的库存剩余总量；与上一字段对比可逐步核查扣减准确性；失败时为空 |
| | 批号/效期组合数 | 该行实际使用的(批号+效期)组合种类数；= 1 为批量导入，≥ 2 为手工操作 |
| **④ 分配结果** | 行状态 | 批量导入 / 手工操作 / 分配失败 |
| | 错误码 | 失败时的错误码（成功时为空） |
| | 分配失败子类型 | 仅在行分配失败时填写，成功行留空。按场景分两类：（一）**错误码=E09**：取值 `预检测A（初始可用QC=0）` / `预检测B（强制竞争库存不足）` / `动态分配无可用QC` / `回溯路径穷尽`；（二）**行因短路从未进入分配循环**（错误码继承自触发短路的 SKU，可为 E09 或 E10）：取值 `连带回滚—同SKU未到达行`（同 SKU 内因前序行先失败、本行从未被处理到的行）或 `连带回滚—跨SKU短路`（同物流单号其他 SKU 触发 E09/E10 后短路，本 SKU 行从未被执行）；其余情况留空 |
| **⑤ 回溯信息** | 是否为回溯重试 | 是 / 否；该行最终分配是否发生在回溯之后（即该行曾被撤销并重新分配）；结合"被排除QC列表"可还原完整回溯过程 |
| | 实际回溯次数 | 该(物流单号+SKU)组的累计回溯次数（组内所有行显示相同值）；无回溯时为 0；用于评估回溯上限是否合理 |

**用途**：

- 验证静态排序：通过"SKU组内处理顺序"确认难的行是否先处理
- 验证动态筛选：通过"动态 nextMinQty"确认系统是否动态重新计算而非复用固定 groupMinQty；再通过"候选QC种类数"核查筛选结果是否正确；最后通过"分配前该QC剩余总量"验证实际可用库存
- 核查库存扣减：相邻行的"分配后该QC剩余总量"与下一行"分配前该QC剩余总量"应连续一致，不一致即为扣减 bug
- 还原回溯路径：通过"被排除QC列表"+"是否为回溯重试"+"实际回溯次数"判断某行为何选了非优先QC
- 定位 E09 原因：通过"E09失败子类型"区分是预检测失败、动态无可用QC还是路径穷尽
- 评估回溯上限：统计实际回溯次数分布，判断默认值 200 是否合理

### 5.4 数据异常明细表

> 本表在校验阶段发现字段级异常时自动生成，**面向文员和仓库人员**，用于快速定位源数据中需要修正的具体单元格。与分配状态汇总表（§5.1）的关系：汇总表提供物流单号级的错误概览，本表提供字段级的精确定位。

**生成规则**：仅当校验阶段发现下列错误码时才输出本表；若校验全部通过，本表为空（或不生成）。

**适用错误码**：

| 错误码 | 来源表 | 触发条件 | 进入本表的原因 |
|--------|--------|----------|----------------|
| E01 | 退单表 / 质检库存 | 关键字段为空，或行号格式不符（非五位前导零文本） | 可精确定位到行号+字段名+原始值 |
| E02 | 退单表 | 同一WMS退单号下存在重复行号，或行号不从 `00001` 起，或行号有跳号 | 可定位到对应 WMS 退单号下的相关退单行：重复行号记录重复的多行；不连续/不从 `00001` 起记录该 WMS 下参与连续性判断的所有格式合法行号行，原因说明中写明缺失行号或首行错误 |
| E03 | 质检库存 | QC情况不是 ZP / QC / NG 之一 | 可精确定位到行号+字段名+原始值 |
| E04 | 退单表 / 质检库存 | 数量不是正整数（含小数、负数、零、非数字；空值→E01，不触发E04） | 可精确定位到行号+字段名+原始值 |
| E05 | 质检库存 | 效期无法解析为合法日期 | 可精确定位到行号+字段名+原始值 |
| E06 | 退单表 | 物流单号仅存在于退单表，质检库存表中无对应库存 | 按退单行逐行记录（与 E07 对称），可精确定位到行号+字段名+原始值 |
| E07 | 质检库存 | 物流单号仅存在于质检库存表，退单表中无对应退单 | 汇总表无法为此类物流单号关联WMS退单号，本表是记录孤立库存行的唯一渠道 |

**不进入本表的错误码**：E08（跨行汇总比较，无具体异常字段）、E11（计算结果，无具体异常字段）、E09/E10/E99（算法或运行时结果，非数据录入错误）。

> **E06 路由变更（2026-07-20）**：E06 此前仅进汇总表（理由为"汇总表已足够定位"），应业务要求改为与 E07 对称——按退单行逐行进入本表，便于在大批量数据中直接定位需补录库存或删除的退单行。

> **校验跳过规则与本表的关系**：若同一 WMS 退单号内存在部分行行号格式非法（E01），E02 仅在该退单号内格式合法的行号子集中检查重复与连续性；格式非法的行仍各自独立记录 E01，不因此跳过对合法行的 E02 检查（即：跳过的是"该非法行本身参与比较"，而非"整个退单号组跳过 E02"）。若某物流单号为空（E01），该行不参与 E06/E07 跨表比对——跳过检查的部分不产生本表记录。E02 的数据异常明细输出按子情形处理：重复行号记录重复的具体行；跳号/不从 `00001` 起记录该 WMS 退单号下参与连续性判断的所有格式合法行号行，便于文员定位并重导或修正该退单号数据。

**字段**：

| 字段 | 说明 |
|------|------|
| 来源表 | `退单表` 或 `质检库存表` |
| 原始行号 | 该条记录在 Excel 中的行号（含表头则从第2行起算），便于文员直接定位到具体单元格 |
| 物流单号 | 原始值；该字段本身为空时填 `[N/A]` |
| WMS退单号 | 原始值（仅退单表行有此字段）；为空时填 `[N/A]`；质检库存行此字段不适用时填 `[N/A]`；E07孤立物流单号行填 `[N/A]` |
| SKU | 原始值；为空时填 `[N/A]` |
| 异常字段名 | 出错的具体字段名称，如 `行号`、`效期`、`数量`、`QC情况` |
| 原始值 | 该字段在 Excel 中的原始内容（含格式有问题的值，如 `2029/13/01`、`QM`、`12.9`）；字段为空时填 `[N/A]` |
| 错误码 | E01 / E02 / E03 / E04 / E05 / E07 |
| 原因说明 | 可读说明，如 `字段为空`、`行号格式不符（三位数字，须为五位前导零文本）`、`QC情况非法（仅允许ZP/QC/NG）`、`效期无法解析为合法日期（月份13不存在）`、`物流单号仅存在于质检库存表` |

**用途**：

- 文员收到汇总表中"E01 - 关键字段为空"等错误后，直接查阅本表，按原始行号在 Excel 中定位并修正具体单元格
- 仓库人员核查 E03/E04/E05 等录入错误时，无需逐行翻找，直接按本表行号跳转
- E07 孤立物流单号可通过本表快速找到质检库存中对应的具体行，核查是否为多录或漏建退单

### 5.5 失败后人工处理路径

> 当汇总表中某物流单号的退单号状态为"无法分配"时，按以下流程处理。

**处理流程**：

| 步骤 | 操作人 | 操作内容 |
|------|--------|---------|
| 1. 查看原因 | 文员 | 在汇总表中查看该物流单号的错误码和原因说明 |
| 2. 判断处理方式 | 文员/客服 | 按错误码分类处理（见下表） |
| 3. 修正数据 | 仓库/客服 | 修正退单表或质检库存表中的源数据 |
| 4. 复核确认 | 文员 | 确认修正后的数据正确 |
| 5. 重新分配 | 文员 | 使用修正后的数据重新运行分配（点击"开始分配"，之前的结果会被覆盖） |

**按错误码分类的处理方式**：

| 错误码范围 | 问题类型 | 处理方式 |
|-----------|---------|---------|
| E12 | 结构异常（表头/退单号唯一性） | 检查粘贴数据的列是否对齐、表头是否完整；若WMS退单号重复，联系客服核查是否为系统错误 |
| E01~E05 | 数据录入错误 | 仓库或客服修正源数据后重跑 |
| E06~E07 | 退单表与质检库存表不匹配 | 确认是否漏录或多录，补全后重跑 |
| E08 | 数量不一致 | 仓库复核实物数量，修正后重跑 |
| E09~E10 | 算法分配失败 | 文员在 WMS 系统中手工逐单操作（无需修正数据） |
| E99 | 运行时异常（含分配后校验失败） | 反馈给开发人员排查；E99 不因修改源数据而消除 |

### 5.6 运行历史记录表

> 本表是**追加式日志**，每次"仅运行校验"或"开始分配"运行结束时**自动追加一条记录**。本表不被"重跑覆盖"清空（见 §6.7.4），用于趋势分析、问题回溯与系统健康度评估。

**字段**：

| 字段 | 说明 |
|------|------|
| 运行编号 | 自增序号（从 1 开始），便于交叉引用 |
| 运行时间 | 运行开始的时间戳，格式 `YYYY/MM/DD HH:MM:SS` |
| 运行类型 | `Dry Run`（仅运行校验）/ `Full Run`（开始分配） |
| 输入：退单表行数 | 本次运行读取的退单表数据行数（不含表头） |
| 输入：质检库存表行数 | 本次运行读取的质检库存表数据行数（不含表头） |
| 输入：物流单号数 | 两表去重合并后的物流单号总数 |
| 校验耗时（秒） | §4.1 校验阶段耗时 |
| 分配耗时（秒） | §4.2 分配阶段耗时；Dry Run 时填 `0` |
| 总耗时（秒） | 从点击按钮到输出结束的总耗时 |
| 校验失败物流单号数 | §4.1 阶段标记为"无法分配"的物流单号数（含 E01~E08、E11） |
| 分配成功物流单号数 | 整单通过分配的物流单号数；Dry Run 时填 `0` |
| 分配失败物流单号数 | 进入分配阶段后失败的物流单号数（含 E09、E10、E99）；Dry Run 时填 `0` |
| 错误码分布 | 各错误码命中的物流单号数，格式：`E01:3; E04:2; E09:1`（按错误码升序，未命中的错误码不列出） |
| 总回溯次数 | 所有(物流单号+SKU)组的实际回溯次数之和；Dry Run 时填 `0`；用于评估配置项 `最大回溯次数` 是否合理 |
| 最大单组回溯次数 | 单个(物流单号+SKU)组的最高回溯次数；Dry Run 时填 `0` |
| 调试日志级别 | 本次运行的调试日志级别（关闭/简版/详细），便于回查行为差异 |
| 备注 | 留空，供文员手动备注，例如"试运行 V1 数据"、"修正 E04 后重跑" |
| 最大回溯次数 | 【实现补充·配置快照】本次运行生效的最大回溯次数 |
| 批号比较模式 | 【实现补充·配置快照】本次运行生效的批号比较模式（敏感/不敏感） |
| 无保质期哨兵值 | 【实现补充·配置快照】本次运行生效的哨兵效期 |

> **实现口径（2026-07-19 拍板）**：需求 17 字段全部实现，并在末尾追加 3 个配置快照列，共 **20 列**。
> 运行编号由写入端（M14）按表内行数自增生成；运行时间格式 `YYYY/MM/DD HH:MM:SS`；
> 错误码分布按"错误码+物流单号"去重后按错误码升序计数（如 `E01:3; E09:1`），无错误时留空。

**用途**：

- **效率趋势分析**：观察"总耗时"随时间的变化，发现性能衰退；如果"分配耗时"突然显著上升，结合"总回溯次数"可定位是数据特征变化还是回溯异常
- **配置调优依据**：通过"最大单组回溯次数"的历史分布，评估配置项 `最大回溯次数` 默认 200 是否过高/过低
- **错误码模式识别**：连续多次出现相同错误码分布，提示业务侧某类录入错误反复发生，需要在源端改进
- **跨次结果对照**：与"运行编号"对应的输出表归档后，可还原任意一次历史运行的完整上下文
- **简历项目展示**：作为"系统自带可观测性"的典型实现，体现运维思维

**保留策略**：

- 默认保留所有历史记录，不做自动清理（数据量极小：单条记录约 200 字节，1000 次运行约 200 KB）
- 用户可手动删除早期行，但**不建议**：删除后无法回溯历史趋势
- 如运行次数极多（如 10000+），可考虑按"运行类型"或"运行时间"分区到独立工作表 `运行历史_归档`

---

## 六、技术方案

### 6.1 技术选型

- **开发语言**：VBA（Visual Basic for Applications）
- **运行环境**：Excel
- **数据结构**：使用VBA数组和字典（Dictionary）存储数据

> **技术边界说明**：
> VBA + Excel 方案适用于当前业务规模（约 500 物流单号/次，数据量约 4,000 行）。以下情况建议评估迁移到其他技术方案（如 Python、Web 应用等）：
> - 单次处理数据量超过 10,000 行（Excel 性能瓶颈）
> - 分配规则持续复杂化（VBA 缺乏单元测试框架，回溯等复杂逻辑的调试成本较高）
> - 需要多人协同或审批流程（Excel 不支持并发访问和权限控制）
> - 需要与 WMS 系统直接 API 对接（VBA 维护 HTTP 请求成本高）
>
> 当前方案优先保证"能用、好用、正确"，代码结构保持清晰的模块分层，降低未来迁移的改造成本。

### 6.2 数据结构设计

#### 6.2.1 退单表数据结构

```
退单记录 {
    物流单号: String
    WMS退单号: String
    SKU: String
    行号: String  ' 五位前导零文本，如 "00001"；保留前导零，按字符串比较
    数量: Long
    行状态: String (未处理/批量导入/手工操作/分配失败)
}
```

#### 6.2.2 质检库存表数据结构

```
质检库存记录 {
    物流单号: String
    SKU: String
    QC情况: String (ZP/QC/NG)
    批号: String
    效期: String
    数量: Long
    剩余数量: Long       ' 动态更新，初始值=数量
}
```

**说明**：同一(物流单号, SKU, QC, 批号, 效期)可能有多行，加载时需按该五元组汇总数量，分配与扣减针对汇总后的可分配池进行。

#### 6.2.3 分配结果数据结构

```
分配结果 {
    物流单号: String
    WMS退单号: String
    SKU: String
    行号: String  ' 同退单表，保留原始前导零文本
    退单数量: Long
    QC情况: String
    批号: String
    效期: String
    分配数量: Long
    行状态: String (批量导入/手工操作)
}
```

> **数量字段类型说明**：所有数量字段（数量、剩余数量、退单数量、分配数量）均使用 `Long` 而非 `Integer`。VBA 的 `Integer` 为 16 位有符号整数，范围 -32,768 ~ 32,767，当单个 SKU 的库存汇总数量较大时有溢出风险；`Long` 为 32 位有符号整数（范围约 ±21 亿），可安全覆盖所有业务场景。

### 6.3 核心算法设计

#### 6.3.1 分配算法（三级策略 + 回溯）

> 本节伪代码描述算法整体结构与控制流。为便于阅读与维护，按"主流程 → 预检测子流程 → 主分配循环（含回溯）子流程"三段拆分。各步骤的详细业务规则已在第四章定义，此处仅引用，不重复叙述。

**6.3.1.1 主流程**

```
1. 初始化
   1a. 表头校验（E12-①，详见 §4.1 第0层）
       - 读取两个输入工作表的第一行
       - 校验列名与系统预设是否完全一致（含列顺序和列名拼写）
       - 若不通过 → 弹出错误提示框，追加运行历史记录（运行类型=Full Run，错误码=E12），立即终止，不进入步骤1b
   1b. 数据加载（仅在1a通过后执行）
       - 逐行加载退单表；加载过程中同步维护 WMS退单号→物流单号 映射字典，若同一WMS退单号出现在两个不同物流单号下（E12-②），弹出提示框，追加运行历史记录，立即终止，不等全部加载完毕
       - 逐行加载质检库存表到内存
       - 按(物流单号, SKU, QC, 批号, 效期)五元组汇总质检库存数量
       - 建立索引：按(物流单号+SKU)索引所有汇总后的库存行
       - 初始化分配结果数组，所有退货行的行状态设为"未处理"

2. 分配前校验（详见 §4.1）
   - 第1层：数据完整性校验（E01~E05）
   - 第2层：物流单号一致性校验（E06、E07）
   - 第3层：数量一致性校验（E08）
   - 第4层：QC库存可分配性校验（E11）
     FOR EACH (物流单号+SKU) 组 [仅限E04、E08均未命中的组]:
         groupMinQty = 该组所有退货行的需求最小值   ' 仅用于 E11 校验
         FOR EACH QC类型 in (ZP, QC, NG):
             T = 该QC类型总库存
             IF T > 0 AND T < groupMinQty:
                 标记 E11，该物流单号所有行 → "分配失败"
   - 未通过校验的物流单号 → 该物流单号下所有行标记为"分配失败"

3. 静态排序（详见 §4.2.1）
   FOR EACH (物流单号+SKU) 组:
       计算每行的初始 nextMinQty 和初始可用QC数（详见 §4.2.6）
       排序：可用QC数 升序 → Qty 降序 → WMS退单号 升序 → 行号 升序（字符串比较）

3.1 静态排序后快速失败预检测   ─→  调用 6.3.1.2「预检测子流程」

4. 主分配循环   ─→  调用 6.3.1.3「主分配循环子流程」

5. 物流单号级整单回滚检查（详见 §4.4 第二层）
   FOR EACH 物流单号:
     IF 该物流单号下存在任何"分配失败"的行:
       丢弃该物流单号下所有已成功的分配结果
       该物流单号下所有行标记为"分配失败"，原因=原始错误码
       ' ── 实现说明（三点）──
       ' 1. 输出严禁：该物流单号的所有分配明细禁止写入成功分配明细表（§5.2 整单回滚规则）
       '             无论分配成功与否均不输出到成功分配明细表
       ' 2. 内部库存：该物流单号已消耗的库存可恢复或废弃；因每个物流单号有独立库存池，
       '             不与其他物流单号共享；库存状态不影响后续物流单号的分配，
       '             但不得参与任何后续校验或输出
       ' 3. 调试日志：若保留回滚前已成功行的日志记录，
       '             其「行状态」字段须更新为「分配失败」以反映整单回滚后的最终状态

6. 退单号状态聚合（详见 §4.4 第二层）
   FOR EACH WMS退单号:
       根据该退单号下所有行的行状态及整单回滚结果，聚合为退单号级状态

7. 分配后校验（详见 §4.3）

8. 输出结果
   - 生成分配状态汇总表（字段：物流单号、WMS退单号、退单号状态、原因）
   - 生成成功分配明细表（仅含整单回滚检查通过的物流单号）
   - 生成数据异常明细表（如校验阶段命中 E01~E05、E07，详见 §5.4）
   - 视配置生成调试日志表（详见 §5.3）
   - 追加运行历史记录表一条记录（详见 §5.6）
```

**6.3.1.2 预检测子流程**

> 对应 §4.2.3。完成静态排序后、进入主分配循环前一次性执行。任一命中则该 (物流单号+SKU) 组标记为 E09，并按 §4.2 的"短路总原则"立即停止该物流单号下所有剩余 SKU 组的分配。

```
FOR EACH 物流单号:
  FOR EACH (物流单号+SKU) 组（该物流单号下）:

    -- 预检测A：某行初始可用QC=0 --
    IF 任意行的初始可用QC数 = 0:
        该组所有行标记为"分配失败"，错误码 = E09
        跳出当前物流单号的 SKU 循环，进入下一物流单号
        ' 同物流单号其余 SKU 组未被尝试，输出时按"连带回滚"处理（见 §4.4）

    -- 预检测B：多行强制竞争同一QC，库存不足 --
    FOR EACH QC类型:
        强制行 = 该组中初始可用QC数=1 且 唯一可用QC=该类型 的所有行
        IF 强制行数量 >= 2:
            S = sum(强制行的需求数量)
            T = 该QC类型总库存
            非强制行 = 该组中可用该QC、但可用QC数>1 的行
            minQtyOther = 非强制行中的需求最小值（若无非强制行则视为 +∞）
            IF T ≠ S AND T < S + minQtyOther:
                该组所有行标记为"分配失败"，错误码 = E09
                跳出当前物流单号的 SKU 循环，进入下一物流单号
```

**6.3.1.3 主分配循环子流程（指针推进 + 选择栈回溯模型）**

> 对应 §4.2.4 三级策略与 §4.2.7 回溯机制。逐物流单号、逐 SKU 组、逐行处理；遇分配失败时通过选择栈回溯到最近一个有未尝试 QC 的历史行重试。

```
FOR EACH 物流单号:
  ' 校验失败的物流单号直接跳过，不进入分配阶段
  ' 确保 E09/E10 等分配错误码不会叠加到已有校验错误码（E01~E08/E11）上
  IF 该物流单号下存在任何行已被校验阶段标记为"分配失败" THEN
      CONTINUE   ' 跳过该物流单号，进入下一个物流单号
  END IF

  FOR EACH SKU (该物流单号下):
    rows = 该(物流单号+SKU)下按主流程步骤3排序后的退货行数组（共 N 行）

    ' 选择栈：每个条目记录一行的分配状态
    '   格式：{ rowIndex, usedQC, 分配明细列表, 库存撤销日志 }
    choiceStack = []

    ' 已尝试QC字典：独立于 choiceStack，记录每行曾经尝试过（含被回溯撤销的）的 QC 集合
    '   写入时机：回溯时将 target 行的 usedQC 写入，target 之后各行的记录同时清除
    '   生命周期：每个 (物流单号+SKU) 组独立初始化
    attemptedQCsByRow = {}
    backtrackCount = 0
    i = 1   ' 当前处理行的下标（1-based）

    -- ★ 守卫 1：库存初始快照（供 §6.5 撤销快照校验与守恒断言使用）★
    initialSnapshot = 该(物流单号+SKU)下所有库存五元组的"剩余数量"副本

    WHILE i <= N:
      currentRow = rows[i]
      D = currentRow.需求量

      -- 动态筛选可用QC，构建候选池（详见 §4.2.2） --
      triedQCs = attemptedQCsByRow.GetOrEmpty(i)
      remainingRows = rows[i+1 .. N]
      nextMinQty = remainingRows 为空 ? ∅ : min(remainingRows 中各行需求量)
      candidatePool = FilterAvailablePool(物流单号, SKU, D, nextMinQty) 排除 triedQCs

      -- 依次尝试三级策略（详见 §4.2.4） --
      result = TryAllocate(candidatePool, D)   ' 内部依次尝试策略一→二→三

      IF result.success:
          choiceStack.push({ rowIndex: i, usedQC: result.usedQC,
                             分配明细: result.details,
                             库存撤销日志: result.undoLog })
          i = i + 1

      ELSE:
          -- 三级策略均失败 → 触发回溯（详见 §4.2.7） --

          ' 从选择栈末尾往前找最近一个"当前可选QC数 > 已尝试QC数"的历史行
          target = choiceStack 中最后一个满足如下条件的条目:
                   FilterAvailablePool(rowIndex行, 不排除任何QC) 的QC种类数
                   > 该条目已尝试QC数

          IF target 不存在:
              该(物流单号+SKU)组所有行标记为"分配失败"，错误码 = E09
              跳出 WHILE 循环，并跳出当前物流单号的 FOR EACH SKU 循环
          END IF

          ' 撤销 target 行及其之后所有已入栈行的分配，恢复对应库存
          FOR EACH 条目 in choiceStack（从栈顶到 target 条目，含 target）:
              按该条目的库存撤销日志，逐项恢复被扣减的库存量
              清除该条目的分配明细
          choiceStack.popUntilBefore(target.rowIndex)

          -- ★ 守卫 2：撤销快照一致性校验（详见 §6.5.2） ★
          ASSERT 当前库存状态 = initialSnapshot - sum(choiceStack 剩余条目的分配明细)
          IF 不一致 → 抛 E99，停止本次运行

          ' 将 target 行上次使用的 QC 记入独立字典，供下轮重新分配时排除该路径
          attemptedQCsByRow(target.rowIndex).Add(target.usedQC)

          ' ★ 清除 target 之后所有行的已尝试QC记录 ★
          ' 原因详见 §4.2.7 步骤 4
          FOR EACH key IN attemptedQCsByRow WHERE key > target.rowIndex:
              DELETE attemptedQCsByRow[key]

          i = target.rowIndex
          backtrackCount = backtrackCount + 1

          IF backtrackCount > 配置.最大回溯次数:
              该(物流单号+SKU)组所有行标记为"分配失败"，错误码 = E10
              跳出 WHILE 循环，并跳出当前物流单号的 FOR EACH SKU 循环
          END IF

    END WHILE

    -- ★ 守卫 3：组结束库存守恒断言（详见 §6.5.1） ★
    ASSERT 当前库存状态 + sum(本组所有分配明细) = initialSnapshot
    IF 不一致 → 抛 E99，停止本次运行
```

#### 6.3.2 QC优先级与效期比较规则

```
QC优先级（数值越小越优先）：
  ZP = 1（良品）
  QC = 2（瑕疵）
  NG = 3（不可售卖）

' 注意：QC 优先级顺序无业务含义，不代表"优先消耗良品"或任何库存策略偏好。
' 其唯一作用是作为平局时的确定性兜底排序，保证相同输入始终产生相同分配结果，
' 便于测试复现和问题排查。如业务后续有明确偏好（如优先消耗 NG），应更新此处数值。
'
' 当前实现：硬编码（ZP=1, QC=2, NG=3），仅在平局时作为确定性兜底，无业务含义。
' §6.7 配置表当前不开放此项（详见 §6.7.6 未列入配置项说明）。
' 未来扩展：若业务有明确的优先级偏好（如先消耗 NG），
'           可将此三个数值提升为配置项，无需改动分配逻辑代码，届时再添加。

效期比较：
  效期日期更晚（保质期更长）= 更优先
  比较时将效期字符串转为日期类型进行比较

综合排序规则（在各策略中统一使用）：
  1. QC优先级升序（ZP优先，确定性兜底，无业务含义）
  2. 效期降序（更晚优先）
  3. 批号升序（更小优先，字符串比较，确定性兜底）
```

#### 6.3.3 状态判定算法

> 行状态在每行分配完成时即已确定，退单号状态通过聚合行状态得出。详细规则见 §4.4。

```
' 行状态：在每行分配成功后，根据实际使用的批号/效期组合数判定
IF 该行使用的(批号+效期)组合种类数 = 1 THEN
    行状态 = "批量导入"
ELSE
    行状态 = "手工操作"
END IF

' 物流单号级整单回滚：任一行失败则整单撤回
FOR EACH 物流单号:
    IF 该物流单号下存在任何分配失败的行 THEN
        丢弃该物流单号下所有分配结果
        该物流单号下所有行标记为"分配失败"
    END IF

' 退单号状态：聚合该退单号下所有行的行状态
FOR EACH WMS退单号:
    IF 所属物流单号已整单回滚 THEN
        退单号状态 = "无法分配"
    ELSEIF 任意行的行状态 = "手工操作" THEN
        退单号状态 = "手工操作"
    ELSE
        退单号状态 = "批量导入"
    END IF
```

### 6.4 性能优化考虑

1. **数据结构优化**：
   - 使用Dictionary建立索引，快速查找
   - 预计算 groupMinQty（E11校验用：组内全部退货行需求的最小值）与各行初始 nextMinQty（QC筛选用：当前行之后剩余行需求的最小值）及可用QC列表
   - 按五元组汇总库存，减少匹配时的遍历量
2. **算法优化**：
   - 三级策略依次尝试，策略一二为O(n)匹配，性能最优
   - 策略三的贪心逐步匹配避免全排列搜索
   - 回溯仅在"具有多个可用QC"的分配行触发（只有一种可用QC的行不产生分支，不参与回溯），回溯状态空间有限
   - 回溯时记录已尝试的QC组合，避免重复尝试
3. **内存管理**：
   - 数据量约1,500-2,500行，内存占用可控
   - 及时释放不需要的临时变量

### 6.5 硬性工程守卫（防 VBA 回溯事故）

> 本节针对 §4.2.7 回溯机制在 VBA 实现中的高复杂度场景，定义一组**运行时强制守卫**。守卫**与业务逻辑无关**，是工程层面对算法正确性的兜底检查。任一守卫触发则立刻抛出 E99 并停止本次运行，避免错误结果被静默写入输出。

#### 6.5.1 库存守恒断言

**校验对象**：每个 (物流单号+SKU) 组的库存账目

**校验时机**：
1. **每次回溯撤销后**：被恢复的库存数量之和应严格等于本次撤销中所有分配明细的分配数量之和
2. **该 (物流单号+SKU) 组分配完成后**：
   - 该组库存的"已分配总量" + "剩余总量" 应严格等于"初始库存总量"
   - 该组所有退货行的"分配总量"（按行号聚合）应严格等于"退货行需求总量"
3. **整批运行结束后**：所有成功分配的物流单号，输出明细表中按"物流单号+SKU"聚合的分配总量应等于退单表对应汇总

**实现要点**：
- 用 `Long` 类型累加（避免浮点误差）
- 守恒断言失败时记录失败的物流单号、SKU、期望值、实际值到 E99 描述中，便于定位

#### 6.5.2 撤销快照一致性校验

**校验对象**：每个 (物流单号+SKU) 组的回溯前后状态

**实现方式**：

1. **快照点**：每个 (物流单号+SKU) 组开始处理前，对该组的所有库存五元组的"剩余数量"取**初始快照**（一份 Dictionary 副本）
2. **撤销点检验**：每次回溯将 choiceStack 中 target 之后的所有条目撤销后，应满足：
   - **若 choiceStack 已空**：当前库存状态 = 初始快照（精确相等）
   - **若 choiceStack 非空**：当前库存状态 = 初始快照 - choiceStack 中所有未撤销条目的分配明细之和
3. **不一致则抛 E99**：含义是"撤销日志与库存状态发生背离"，意味着 undoLog 写错或部分扣减恢复漏算
4. **组结束点检验**：组分配完成后，当前库存状态 + 该组所有分配明细 = 初始快照（守恒的反向验证）

#### 6.5.3 强制最小测试集（≥ 10 个场景）

> 测试用例必须随代码一同交付，作为验收的硬性条件。每个用例必须包含「输入数据 + 期望输出（汇总表 + 明细表关键字段）」，运行后**逐字段比对**。

**核心覆盖维度**（用例数 ≥ 10）：

| # | 场景 | 期望验证点 |
|---|------|----------|
| T1 | 单物流单号 + 单退单号 + 单 SKU + 策略一精确匹配 | 策略一命中、行状态=批量导入 |
| T2 | 单物流单号 + 单 SKU + 多行 + 策略二（最接近匹配） | 策略二命中、单行扣减后剩余库存保留 |
| T3 | 单物流单号 + 单 SKU + 策略三跨批号/效期 | 策略三命中、行状态=手工操作、多条明细记录 |
| T4 | 多 QC 竞争 + 静态排序生效（高难度行先处理） | 处理顺序符合预期、QC 优先级正确 |
| T5 | 回溯触发并成功 | 选择栈正确撤销、终态正确、回溯次数计数正确 |
| T6 | 回溯触发并耗尽（E10） | E10 触发、整单回滚 |
| T7 | E11 命中（碎片库存） | E11 触发、整单标记失败 |
| T8 | E08 命中（数量不一致） | E08 触发、汇总表正确 |
| T9 | 整单回滚（部分 SKU 成功部分失败） | 全部回滚、连带回滚原因格式正确 |
| T10 | E07 孤立物流单号 | 占位符 `[N/A]` 出现在 WMS退单号字段 |
| T11+ | 边界场景：单行库存恰好=需求；批号大小写不同；效期哨兵值；批号前导零；空行；超长字段 | 各对应规则的覆盖 |

**自动化入口分层**：

- `RunAllTests()`：运行 M16 的内存单元测试、模块集成测试和内存端到端测试，不读取外部 DataSet。
- `RunSingleTest 17`：运行依赖独立 `.xlsm/.xlsx` DataSet 的文件集成测试。
- `RunSingleTest 18`：运行批量测试器的内存冒烟测试。
- `RunBatchTestPlan`：按汇总工作簿中的“批量测试计划”执行 Excel 批量回归。

四层入口的通过范围彼此不同，`RunAllTests()` 通过不得等同于文件集成或批量 Excel 回归通过。

#### 6.5.4 调试日志表与回溯统计的强制下限

**调试日志表（工作表）**：受配置项"调试日志级别"控制，可设为"关闭"（不生成调试日志表工作表）、"简版"或"详细"。关闭后不影响系统运行正确性，仅牺牲逐行可追溯性。

**回溯统计（运行历史记录表字段）**：无论"调试日志级别"设置为何，每次运行结束时必须将"总回溯次数"和"最大单组回溯次数"写入运行历史记录表（§5.6）。这是工程上"事后可追溯"的最低保证，不允许省略或置零。

> **设计分工**：调试日志表提供每行的分配过程细节，运行历史记录表提供聚合统计。前者可按需开关，后者强制记录。两者分工明确，互不替代。

### 6.6 错误处理

> 所有错误均通过错误码体系（E01~E99）标识，详见 4.1 节和 5.1 节。

1. **数据校验错误**（E01~E08）：校验阶段发现的数据问题，记录错误码和说明到汇总表，该物流单号下所有行标记为"无法分配"
2. **分配阶段失败**（E09~E10）：
   - E09（候选QC全部不可用）：所有可用QC均尝试失败且无法回溯
   - E10（回溯超限）：超过配置的最大回溯次数（默认200）
3. **运行时异常**（E99）：使用 VBA `On Error` 捕获未预期的运行时错误，记录错误码 E99 和 VBA 原始错误信息

### 6.7 用户界面设计

#### 6.7.1 输入区域

退单表工作表、质检库存表工作表（由用户在固定名称的工作表中粘贴数据）。

#### 6.7.2 输出区域

| 工作表 | 说明 | 生成时机 |
|--------|------|----------|
| 分配状态汇总表 | 所有物流单号的退单号级状态和错误码 | 每次"开始分配"运行后必然生成 |
| 成功分配明细表 | 整单通过的物流单号的行级分配结果 | 每次"开始分配"运行后必然生成（无成功结果时为空） |
| 数据异常明细表 | 校验阶段发现的字段级异常（E01~E05、E07），精确到 Excel 行号和字段名 | 校验发现上述错误时生成；校验全部通过时不生成（见 §5.4） |
| 调试日志表 | 每行的分配过程明细，含回溯追踪和库存变化 | 仅当"调试日志级别"配置非"关闭"时生成（见 §5.3） |
| 运行历史记录表 | 历次运行的元数据汇总（运行时间、行数、错误码分布、回溯次数等） | 每次运行（含"仅运行校验"与"开始分配"）追加一条；详见 §5.6 |

#### 6.7.3 操作按钮

| 按钮 | 行为 | 说明 |
|------|------|------|
| **仅运行校验**（Dry Run） | 仅执行 §4.1 分配前校验，输出"数据异常明细表"与"分配状态汇总表"（汇总表仅含校验阶段错误码 E01~E08、E11），**不执行任何分配逻辑**，不生成"成功分配明细表"与"调试日志表" | 用于在正式分配前先发现并修正源数据问题，避免反复跑完整流程；详见 §6.7.5 |
| **开始分配** | 执行完整流程：分配前校验 → 分配 → 分配后校验 → 输出所有结果表 | 业务方修正数据后正式分配；按钮点击后**自动清空所有输出表**（见 §6.7.4），避免脏数据 |
| **清空结果** | 手动清空所有输出表内容（仅清空，不删除工作表本体） | 调试或归档场景下使用 |
| **导出结果**（可选） | 将所有输出表打包导出为独立 Excel 文件 | 便于归档与多版本对比 |

#### 6.7.4 重跑覆盖机制

为避免"半新半旧"的脏数据干扰，**点击"开始分配"或"仅运行校验"时，系统会按以下规则自动清空输出**：

1. **清空范围**：分配状态汇总表、成功分配明细表、数据异常明细表、调试日志表（含分表 `调试日志_2`、`调试日志_3` 等）的**数据区域**（保留表头）。
2. **不清空**：运行历史记录表（追加式，保留全部历史）、配置工作表（保留用户配置）、输入工作表（退单表/质检库存表）。
3. **执行时机**：在校验/分配开始**之前**统一清空一次，避免运行中途产生的输出与上次残留混合。
4. **跨工作表清空**：若上次"详细"模式产生了 `调试日志_2`、`调试日志_3` 等分表，本次清空时一并删除这些动态创建的工作表，仅保留主工作表 `调试日志`。
5. **失败兜底**：如清空过程发生异常（如工作表被保护），中止本次运行并提示用户手动检查，**不在脏数据上继续运行**。

#### 6.7.5 干跑（仅运行校验）模式

为支持"先校验、修数据、再分配"的实际工作流，提供独立的"仅运行校验"入口：

- **执行内容**：完成 §4.0 数据标准化 + §4.1 全部四层校验（E01~E08、E11）。
- **输出内容**：
  - **数据异常明细表**：定位到字段级的所有异常（E01~E05、E07）
  - **分配状态汇总表**：仅包含校验阶段错误码（E01~E08、E11）的物流单号/退单号；通过校验的物流单号**不出现在本次汇总表中**（避免误导业务方以为"已分配"）
  - **运行历史记录表**：追加一条记录，运行类型标记为 `Dry Run`
- **不输出**：成功分配明细表、调试日志表（这两张表只在真正执行分配时才有意义）
- **不做的事情**：不调用三级策略、不调用回溯、不修改任何库存状态、不做分配预测（避免产生用户误以为是"试运行结果"的中间数据）
- **典型工作流**：业务方点击「仅运行校验」→ 查看数据异常明细表 → 在源数据中修正 → 再次点击「仅运行校验」直到校验全部通过 → 点击「开始分配」执行正式分配

#### 6.7.6 配置区域

配置项存放于独立的「配置」工作表，每行一个键值对（参数名 / 值 / 说明），用户可直接编辑值列。

| 参数名 | 默认值 | 说明 |
|--------|--------|------|
| 最大回溯次数 | 200 | 每个(物流单号+SKU)组的回溯上限，可根据调试日志中的实际回溯次数分布调整；超出则触发 E10 |
| 调试日志级别 | 关闭 | 三档可选：`关闭`（不生成调试日志表）/ `简版`（每行一条最终结果，含回溯标记）/ `详细`（含所有被撤销的中间尝试，行数大幅增加，仅用于专项排查）；详见 §5.3 |
| 详细日志单表上限 | 100000 | 仅在"详细"模式下生效；单张工作表数据行数超过此值时自动创建新工作表 `调试日志_2`、`调试日志_3` 顺延编号；阈值过低会增加分表数量，过高会拖慢 Excel 渲染 |
| 批号比较模式 | 不敏感 | 二选一：`不敏感`（默认，读取后 `Trim+UCase` 统一大写，避免大小写不一致导致库存"分裂"）/ `敏感`（保留批号原始大小写，使用 VBA 二进制比较）；详见 §4.2.5 |
| 无保质期哨兵值 | 2099/01/01 | 无保质期商品在质检库存中的统一填值，仅作系统占位不代表真实到期日；可改为 `9999/12/31` 等其他哨兵值；详见 §2.2 |

> **未列入配置项的设计取舍**：
> - **回滚粒度**：固定为"物流单号粒度整单回滚"，不提供开关（详见 §4.4 业务前提声明）。
> - **QC 优先级**：固定为 `ZP=1, QC=2, NG=3`，仅在平局兜底时使用，不开放配置（避免业务侧误改优先级导致分配语义变化；详见 §6.3.2）。
> - **是否启用 SKU-批号-效期对照表校验**：当前版本不实现该扩展校验（详见 §2.2 业务前提与一致性校验）。

---

## 七、实施步骤

> **说明**：本章为技术方案文档，仅列出主要实施阶段。详细子任务拆分、工时估算与验收标准另行制定实施计划。

1. 需求确认：与用户确认所有细节
2. 数据结构设计：设计VBA数据结构
3. 核心算法实现：实现分配算法和回溯机制
4. 校验逻辑实现：实现分配前后校验
5. 输出功能实现：实现结果输出
6. 测试验证：使用实际数据测试
7. 优化调整：根据测试结果优化性能

---

## 八、注意事项

1. **QC一致性**：这是最核心的约束，必须严格保证
2. **库存扣减**：分配过程中库存扣减必须准确，避免重复分配
3. **回溯效率**：回溯上限为可配置参数（默认200），可根据调试日志表中实际回溯次数分布进行调优
4. **数据准确性**：输出数据必须与输入数据完全一致
5. **用户体验**：输出结果要便于文员检查和操作
 
