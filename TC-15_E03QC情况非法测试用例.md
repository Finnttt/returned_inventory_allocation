# TC-15 E03 QC情况非法测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000015 |
| 场景编号 | TC-15 |
| 场景名称 | 质检库存表 QC情况 = `XP`（非 ZP/QC/NG），触发 E03 |
| 测试目的 | ① 验证库存表 QC情况 不在合法值（ZP/QC/NG）集合内时触发 E03 ② 验证 E03 触发后，对应物流单号所有行标记为"无法分配" ③ 验证非法 QC 行进入数据异常明细表 |
| 覆盖规则 | §4.0（R022 QC标准化）、§4.1 第二层 E03（R043） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | E03 |
| 是否触发整单回滚 | 否（E03 属于校验阶段失败，未进入分配，不触发整单回滚） |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| 质检库存表第2行：QC情况 = `XP`，经 Trim + UCase 后仍不在 {ZP, QC, NG} 集合内 | §4.0（R022）、§4.1（R043） |

---

## 2. 输入数据

### 2.1 退单表（1行）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|----------|-----|------|------|---------|
| TK10000150 | H000000015 | 00001 | 5 | SF3190000000015 |

### 2.2 质检库存表（1行，QC情况为非法值）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000015 | H000000015 | XP | LA01 | 2029/06/15 | 5 |

> **说明**：QC情况 `XP` 不在合法集合 {ZP, QC, NG} 内，经 M04 标准化（Trim + UCase）后仍非法，M05 在第二层校验中触发 E03。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | 结果 |
|--------|---------|------|
| 第1层（E01/E02）关键字段完整性 | 行号、WMS退单号、SKU 均合法 | 通过 |
| 第2层（E03）QC情况合法性 | 须为 ZP、QC 或 NG | **命中 E03**（库存表 QC = `XP`，非法） |
| 第2层（E04/E05）数量/效期合法性 | 继续执行（四层均执行），但此处不命中 | 通过 |
| 第3层（E06~E08）跨表一致性 | 继续执行，但此处不命中 | 通过 |
| 第4层（E11）QC碎片 | 继续执行，但此处不命中 | 通过 |

> **结论**：第二层命中 E03，整个物流单号 SF3190000000015 下所有行标记为"无法分配"，**不进入分配阶段**。

---

## 4. 分配过程

**不进行分配**（E03 在校验阶段已标记失败，未进入分配循环）。

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|----------|-----------|------|
| SF3190000000015 | TK10000150 | 无法分配 | E03 - QC情况非法（须为 ZP/QC/NG） |

### 5.2 成功分配明细表

**不生成**（E03 导致分配失败，无任何成功分配记录）

### 5.3 数据异常明细表

| 来源表 | 原始行号 | 物流单号 | WMS退单号 | SKU | 异常字段名 | 原始值 | 错误码 | 原因说明 |
|--------|----------|---------|-----------|-----|------------|--------|--------|----------|
| 质检库存表 | 2 | SF3190000000015 | [N/A] | H000000015 | QC情况 | XP | E03 | QC情况非法：`XP` 不在合法值集合 {ZP, QC, NG} 内 |

> **说明**：库存表行无 WMS退单号字段，故该列填 `[N/A]`。

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 1 |
| 输入：质检库存表行数 | 1 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 1 |
| 分配成功物流单号数 | 0 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | E03 × 1 |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |

---

## 6. 补充说明

### 6.1 与 TC-47（QC大小写标准化）的关系

TC-47（SF0047）验证 `qc` 小写经标准化后变为 `QC`，属于**合法值可正规化**场景。TC-15 为其互补对照：即使经过标准化，`XP` 仍非法，系统须正确拦截。
