# TC-51 行号跳号与不连续 E02 测试用例

## 1. 测试描述

| 项目 | 内容 |
|------|------|
| 物流单号 | SF3190000000051 |
| 场景编号 | TC-51 |
| 场景名称 | 退单表行号出现跳号（缺中间行号）或不从 `00001` 起，触发 E02，物流单号分配失败 |
| 测试目的 | ① 验证同一WMS退单号内行号有跳号（`00001`→`00003`，缺 `00002`）时，触发 E02 ② 验证同一WMS退单号内行号不从 `00001` 起（首行 `00002`）时，触发 E02 ③ 验证 E02 触发后，对应 WMS 退单号标记为"无法分配"，且不产生成功分配明细 ④ 验证不连续相关退单行进入数据异常明细表，便于文员定位并重导/修正 ⑤ 与 TC-48（行号合法正向基线）互为对照 |
| 覆盖规则 | §2.1 行号连续性要求（R011）、§4.1 E02 行号重复或不连续校验（R042） |
| 配置：最大回溯次数 | 200 |
| 配置：调试日志级别 | 关闭 |
| 预期错误码 | E02（两个 WMS 退单号均触发） |
| 是否触发整单回滚 | 否（E02 属于校验阶段失败，未进入分配，不触发整单回滚） |

### 关键触发点

| 触发点描述 | 涉及规则 |
|-----------|---------|
| TK10000510 行号 `00001`、`00003`：排序后序列为 [1,3]，间隙存在（缺 2），不满足连续性 → E02 | §2.1（R011）、§4.1（R042） |
| TK10000511 行号 `00002`、`00003`：排序后序列为 [2,3]，首行非 1（`00001`），不满足"从 `00001` 起" → E02 | §2.1（R011）、§4.1（R042） |
| E02 校验依赖 E01 通过：格式非法的行号（E01）不参与连续性检查，本 TC 所有行号格式均合法，E02 直接生效 | §4.1 校验跳过规则 |

---

## 2. 输入数据

### 2.1 退单表（4行，含两个 WMS 退单号，各自触发 E02）

| WMS退单号 | SKU | 行号 | 数量 | 物流单号 |
|-----------|-----|------|------|---------|
| TK10000510 | H000000051 | 00001 | 5 | SF3190000000051 |
| TK10000510 | H000000051 | 00003 | 5 | SF3190000000051 |
| TK10000511 | H000000051 | 00002 | 3 | SF3190000000051 |
| TK10000511 | H000000051 | 00003 | 3 | SF3190000000051 |

> **TK10000510 说明**：两行行号为 `00001`、`00003`，跳过了 `00002`，构成不连续序列，触发 E02。
>
> **TK10000511 说明**：两行行号为 `00002`、`00003`，未从 `00001` 起，触发 E02。
>
> ⚠️ **DataSet 制作关键**：行号列所有单元格须设为**文本格式**，直接输入 `00001`、`00003`、`00002`、`00003`（含前导零）。

### 2.2 质检库存表（1行）

| 物流单号 | SKU | QC情况 | 批号 | 效期 | 数量 |
|---------|-----|--------|------|------|------|
| SF3190000000051 | H000000051 | ZP | LA01 | 2029/06/15 | 16 |

> **说明**：库存按退单合计填写（5+5+3+3=16），目的是确保 E06/E07 不触发，让 E02 作为唯一错误码出现，便于验证。

---

## 3. 分配前校验结论

| 校验层 | 校验内容 | TK10000510 结果 | TK10000511 结果 |
|--------|---------|----------------|----------------|
| 第1层（E01）行号格式 | 恰好5位且全为数字字符 | 通过（`00001`/`00003` 均合法） | 通过（`00002`/`00003` 均合法） |
| 第1层（E02）行号连续性 | 从 `00001` 起、无跳号的完整连续序列 | **E02**（序列 [1,3]，缺 2） | **E02**（序列 [2,3]，首行非1） |
| 第2层（E06/E07）物流单号一致性 | 两表物流单号集合比对 | 通过 | 通过 |
| 第3层（E08）数量一致性 | 同物流单号+SKU数量合计比对 | 通过（退单合计 16 = 库存 16） | 通过 |
| 第4层（E11）QC碎片 | 0 < T < groupMinQty | 通过（groupMinQty = min(5,5,3,3)=3；ZP T=16，不满足 0 < 16 < 3） | 同左 |

> **结论**：两个 WMS 退单号均触发 E02，整个物流单号 SF3190000000051 下所有行标记为"无法分配"，不进入分配阶段。

---

## 4. 分配过程

**不进行分配**（E02 在校验阶段已标记失败，未进入分配循环）。

---

## 5. 预期结果

### 5.1 分配状态汇总表

| 物流单号 | WMS退单号 | 退单号状态 | 原因 |
|---------|-----------|-----------|------|
| SF3190000000051 | TK10000510 | 无法分配 | E02 - 退单表行号重复或不连续（行号序列不连续，缺 `00002`） |
| SF3190000000051 | TK10000511 | 无法分配 | E02 - 退单表行号重复或不连续（行号不从 `00001` 起，首行为 `00002`） |

### 5.2 成功分配明细表

**不生成**（E02 导致分配失败，无任何成功分配记录）

### 5.3 数据异常明细表

| 来源表 | 原始行号 | 物流单号 | WMS退单号 | SKU | 异常字段名 | 原始值 | 错误码 | 原因说明 |
|--------|----------|---------|-----------|-----|------------|--------|--------|----------|
| 退单表 | 2 | SF3190000000051 | TK10000510 | H000000051 | 行号 | 00001 | E02 | 行号不连续：当前序列为 `00001`、`00003`，缺少 `00002` |
| 退单表 | 3 | SF3190000000051 | TK10000510 | H000000051 | 行号 | 00003 | E02 | 行号不连续：当前序列为 `00001`、`00003`，缺少 `00002` |
| 退单表 | 4 | SF3190000000051 | TK10000511 | H000000051 | 行号 | 00002 | E02 | 行号不从 `00001` 起：当前序列首行为 `00002` |
| 退单表 | 5 | SF3190000000051 | TK10000511 | H000000051 | 行号 | 00003 | E02 | 行号不从 `00001` 起：当前序列首行为 `00002` |

> **说明**：跳号/不从 `00001` 起属于 WMS 退单号组级问题，但为了便于文员定位，本表记录该 WMS 下参与连续性判断的所有格式合法行号行。

### 5.4 调试日志表

**不生成**（配置：调试日志级别 = 关闭）

### 5.5 运行历史记录表

| 字段 | 期望值 |
|------|--------|
| 运行类型 | Full Run |
| 输入：退单表行数 | 4 |
| 输入：质检库存表行数 | 1 |
| 输入：物流单号数 | 1 |
| 校验失败物流单号数 | 1 |
| 分配成功物流单号数 | 0 |
| 分配失败物流单号数 | 0 |
| 错误码分布 | E02 × 2（TK10000510、TK10000511 各一次） |
| 总回溯次数 | 0 |
| 最大单组回溯次数 | 0 |

---

## 6. 补充说明

### 6.1 两种 E02 子情形对比

| 子情形 | 示例（本 TC） | 检测逻辑 |
|--------|------------|---------|
| **跳号**（有间隙）| 行号序列 [00001, 00003]，gap=1 | 排序后：max(序列) - min(序列) + 1 ≠ 行数 |
| **不从 00001 起** | 行号序列 [00002, 00003]，min≠1 | min(序列) ≠ 1（转换为整数后 ≠ 1）|

两种子情形均合并在 E02 中处理，原因说明中通过文字区分。

### 6.2 与 TC-14（E02 行号重复）的关系

| TC | E02 子情形 | 场景 |
|----|-----------|------|
| TC-14 | 行号重复 | 同一退单号两行均为 `00001` |
| **TC-51** | 行号不连续（跳号 / 不从00001起）| 本 TC |

两者同属 E02，但触发路径不同，需独立测试覆盖。
